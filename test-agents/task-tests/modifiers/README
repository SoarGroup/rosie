evaluation: modifiers

Aaron Mininger

This is an evaluation used to test the abiliy of Rosie to take a learned task
and combine it with many different modifiers without needing further instruction. 

# Prerequisites:

* MobileSim java simulator
* Soar built with python3 SML library
* pysoarlib (https://github.com/amininger/pysoarlib) 
* Both the $ROSIE_HOME and $MOBILE_SIM_HOME environment variables set

# Test Script

1. move with start-clause(clocktime)
2. move with start-clause(duration)
3. move with start-clause(condition)
4. move with end-clause(condition) - until 
5. move with conditions
6. move with N times modifier
7. move within counter loop
8. move within while loop
9. move within until loop

# Running

$> make run_evaluation

# Can also run as an automated test
$> make run_test

