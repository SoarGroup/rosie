sp {selection*iterative-deepening*operator-tie*prefer*init*over*increment
   (state <s> ^name selection
              ^top-selection true
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << init-current-evaluation-depth init-remaining-operators >>)
   (<o2> ^name increment-current-evaluation-depth)
-->
   (<s> ^operator <o1> > <o2>)
}