smem --add {
(<lti> ^model-template <mdtm>)
(<mdtm> ^model <model>)
(<model> ^participants <partic>
	^preconditions <preCon>
	^causes <causes>
	^effects <effects>)

#participants

#preconditons 

#causes


#effects

}