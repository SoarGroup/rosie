sp {top-state*elaborate*agent-params*domain*cozmo
   (state <s> ^superstate nil
              ^agent-params <params>)
-->
   (<params> ^domain cozmo)
}

source _agent_source.soar

pushd manage-world-state
source cozmo_source.soar
popd

# cozmo specific tasks
pushd problem-space/action/task-implementations
source cozmo_source.soar
popd

pushd cozmo-tasks
source cozmo-tasks_source.soar
popd
