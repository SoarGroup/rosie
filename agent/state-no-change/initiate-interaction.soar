sp {top-state*state*no-change*propose*initiate-interactino*get-next-task
   (state <s> ^choices none
            ^attribute state
         ^top-state <ts>
         ^superstate <ts>)
   (<ts> ^interaction.status <status>)
-->
   (<s> ^operator <o> +)
   (<o> ^name initiate-interaction
        ^type get-next-task)
}
