# Elaborate child-action from superoperator.action
sp {execute-action*elaborate*child-action*superoperator
   (state <s> ^name execute-action
              ^superstate.operator.action <a>)
-->
   (<s> ^child-action <a>)
}

sp {execute-action*elaborate*action-stack*from*top-state
   (state <s> ^name execute-action
              ^top-state.action-stack <stack>)
-->
   (<s> ^action-stack <stack>)
}
              
