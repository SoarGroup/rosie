#### operator add-procedural-link-to-tcn
# initializes a new smem structure for the task the first time we see it

# propose add-procedural-link-to-tcn if no ^procedural link exists on the task-concept-network
sp {handle-new-task-command*propose*add-procedural-link-to-tcn
   (state <s> ^name handle-new-task-command
              ^task-concept-network <tcn>)
   (<tcn> -^procedural)
-->
   (<s> ^operator <o> +)
   (<o> ^name add-procedural-link-to-tcn)
}

# create the basic semantic structure for the task, 
sp {handle-new-task-command*apply*add-procedural-link-to-tcn
   (state <s> ^name handle-new-task-command
              ^operator.name add-procedural-link-to-tcn
              ^task-concept-network <tcn>)
   (<tcn> ^handle <handle>)
-->
   (<tcn> ^item-type task ^procedural <proc>)
   (<proc> ^op_name (make-constant-symbol (concat |op_| <handle>)) ^subtasks <subs>)
   (<s> ^to-store <tcn> <proc>)
}

        
