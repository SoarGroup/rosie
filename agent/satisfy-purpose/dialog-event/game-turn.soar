sp {action-event*propose*start-turn
   (state <s> ^name dialog-event
              ^purpose <p>)
   (<p> ^type game-turn)
-->
   (<s> ^operator <o> +)
   (<o> ^name start-turn
        ^category svs)
}