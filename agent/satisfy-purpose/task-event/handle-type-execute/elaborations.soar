sp {handle-type-execute*elaborate*interaction-purpose*from*superoperator
   (state <s> ^name handle-type-execute
              ^superstate.operator.interaction-purpose <p>)
-->
   (<s> ^interaction-purpose <p>)
}

sp {handle-type-execute*elaborate*command-message*from*purpose
   (state <s> ^name handle-type-execute
              ^interaction-purpose.parameters.command-message <msg>)
-->
   (<s> ^command-message <msg>)
}

sp {handle-type-execute*elaborate*conditions*from*superoperator
   (state <s> ^name handle-type-execute
              ^interaction-purpose.parameters.conditions <conds>)
-->
   (<s> ^conditions <conds>)
}

sp {handle-type-execute*elaborate*task-stack*from*superstate
   (state <s> ^name handle-type-execute
              ^top-state.task-stack <stack>)
-->
   (<s> ^task-stack <stack>)
}
