
sp {handle-type-execute*prefer*add-procedural-link-to-tcn*over*construct-task-operator
   (state <s> ^name handle-type-execute
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name add-procedural-link-to-tcn)
   (<o2> ^name construct-task-operator)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {handle-type-execute*prefer*task-segment*over*store*smem
   (state <s> ^name handle-type-execute
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << construct-task-operator create-new-task-segment >>)
   (<o2> ^name << add-argument-to-tcn store-smem-concepts >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {handle-type-execute*prefer*add-argument-to-tcn*over*store*smem
   (state <s> ^name handle-type-execute
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << add-argument-to-tcn >>)
   (<o2> ^name << store-smem-concepts >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {handle-type-execute*prefer*mark-argument-missing*over*complete
   (state <s> ^name handle-type-execute
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name mark-argument-missing)
   (<o2> ^name complete-handle-new-task-command)
-->
   (<s> ^operator <o1> > <o2>)
}

