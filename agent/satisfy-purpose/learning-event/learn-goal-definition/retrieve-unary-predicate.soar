
sp {learn-goal-definition*retrieve*smem*unary*predicate*info
   (state <s> ^name learn-goal-definition
              ^generalize-task-goal.task-goal.<gpnum> <gp>)
   (<gp> ^type unary
         ^handle <handle>)
-->
   (<s> ^smem-query <query>)
   (<query> ^cue <cue>
            ^depth 2
            ^destination <s>
            ^result-name unary-predicate-info)
   (<cue> ^handle <handle>)
}

sp {learn-goal-definition*elaborate*generalize-task-goal*unary*predicate*property-handle
   (state <s> ^name learn-goal-definition
              ^generalize-task-goal.task-goal.<gpnum> <gp>
              ^unary-predicate-info <info>)
   (<gp> ^type unary
         ^handle <handle>)
   (<info> ^handle <handle>
           ^property.handle <prop-h>)
-->
   (<gp> ^property-handle <prop-h>)
}

sp {learn-goal-definition*retrieve*smem*unary*predicate*info*conditions
   (state <s> ^name learn-goal-definition
              ^generalize-task-argument <arg>)
   (<arg> ^arg-type predicate-set
          ^type goal-conditions
          ^instantiation.<gpnum> <gp>)
   (<gp> ^type unary
         ^handle <handle>)
-->
   (<s> ^smem-query <query>)
   (<query> ^cue <cue>
            ^depth 2
            ^destination <s>
            ^result-name unary-predicate-info)
   (<cue> ^handle <handle>)
}

sp {learn-goal-definition*elaborate*generalize-task-argument*unary*predicate*property-handle
   (state <s> ^name learn-goal-definition
              ^generalize-task-argument <arg>
              ^unary-predicate-info <info>)
   (<arg> ^arg-type predicate-set
          ^type goal-conditions
          ^instantiation.<gpnum> <gp>)
   (<gp> ^type unary
         ^handle <handle>)
   (<info> ^handle <handle>
           ^property.handle <prop-h>)
-->
   (<gp> ^property-handle <prop-h>)
}

