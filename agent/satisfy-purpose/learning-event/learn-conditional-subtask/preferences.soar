
sp {learn-goal-definition*prefer*generalize-argument*over*generalize-goal
   (state <s> ^name learn-goal-definition
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name generalize-task-argument)
   (<o2> ^name generalize-task-goal)
-->
   (<s> ^operator <o1> > <o2>)
}
