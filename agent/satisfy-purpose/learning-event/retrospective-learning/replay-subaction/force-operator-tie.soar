
sp {replay-subaction*propose*force-operator-tie
   (state <s> ^problem-space.action-space retrospective
              ^superstate.name replay-subaction)
-->
   (<s> ^operator <o> +)
   (<o> ^name force-operator-tie)
}
