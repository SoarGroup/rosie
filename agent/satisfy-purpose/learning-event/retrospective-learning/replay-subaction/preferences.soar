
sp {replay-subaction*prefer*smem-query*over*action*operator
   (state <s> ^name replay-subaction
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name smem-query)
   (<o2> ^action-handle <any>)
-->
   (<s> ^operator <o1> > <o2>)
}
