sp {retrospective-learning*propose*replay-subaction
   (state <s> ^name retrospective-learning
              ^subactions-stack.next { <next> <> none })
   (<next> -^learned true)
-->
   (<s> ^operator <o> +)
   (<o> ^name replay-subaction
        ^subaction <next>)
}
