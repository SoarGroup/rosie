sp {learn-proposal-rule*desired*satisfied*propose*action-operator*superstate
   (state <s> ^impasse no-change
              ^attribute state
              ^desired.satisfied true
              #^action-operator <o>
              ^superstate <ss>)
-->
   (<ss> ^operator <o> +)
   (<o> ^name deliver)
}

