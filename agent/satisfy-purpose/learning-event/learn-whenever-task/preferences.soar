
sp {learn-whenever-task*prefer*change-world-state*over*setup-whenever-state
   (state <s> ^name learn-whenever-task
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name change-world-state)
   (<o2> ^name setup-whenever-state)
-->
   (<s> ^operator <o1> > <o2>)
}
