sp {train-predicate*propose*initiate-interaction*get-predicate-info
   (state <s> ^name train-predicate
              ^predicate <pred>)
   (<pred> ^handle <pred-handle>
          -^property)
-->
   (<s> ^operator <o> + >)
   (<o> ^name initiate-interaction
        ^type get-predicate-info
        ^predicate <pred-handle>)
}