#####################
# train-predicate
#####################

sp {train-on-object*propose*train-predicate
   (state <s> ^name train-on-object
              ^example-object <obj>
              ^predicate <pred>
             -^trained <pred>)
   (<pred> -^{2} <obj2>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name train-predicate
        ^example-object <obj>
        ^predicate <pred>)
}
