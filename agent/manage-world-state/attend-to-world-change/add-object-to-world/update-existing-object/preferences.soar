# Remove the discrepancy-info last (will remove the substate)
sp {update-existing-object*elaborate*worst*preference*remove-discrepancy-info
   (state <s> ^name update-existing-object
              ^operator <o> +)
   (<o> ^name remove-discrepancy-info)
-->
   (<s> ^operator <o> <)
}
