# If the object is not occluded, update its pose

sp {attend-to-perception*moved-object*propose*update-object-pose*not*occluded
   (state <s> ^name attend-to-perception
              ^discrepancy-type moved-object
              ^object-occluded false
              ^object-info <obj-info>)
-->
   (<s> ^operator <o> +)
   (<o> ^name update-object-pose
        ^object-info <obj-info>)
}

sp {
