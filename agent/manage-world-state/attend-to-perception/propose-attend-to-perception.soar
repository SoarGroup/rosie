
sp {anystate*propose*attend-to-perception*if*problem-space*attend-to-perception*yes
   (state <s> ^problem-space.attend-to-perception yes
              ^top-state.perception-monitor.discrepancies.<disc-type> <disc>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name attend-to-perception
        ^discrepancy-type <disc-type>
        ^discrepancy-info <disc>)
}
