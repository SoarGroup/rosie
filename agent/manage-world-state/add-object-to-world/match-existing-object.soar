
# operator match-existing-object
#   see manage-world-state/match-existing-object
# Tries to match the missing object against those already in the world
# Returns a match as ^matches-existing-object <obj-info> or none
sp {attend-to-perception*new-belief-object*propose*match-existing-object
   (state <s> ^name attend-to-perception
              ^discrepancy-type new-belief-object
              ^object-info <obj-info>
             -^matches-existing-object <any>)
-->
   (<s> ^operator <o> +)
   (<o> ^name match-existing-object
        ^source-type belief
        ^source-obj <obj-info>)
}
