# operator update-existing-object
# If ^matches-existing-object <> none, then updates the existing object
# instead of creating a new one
sp {insert-object-into-world*propose*update-existing-object
   (state <s> ^name insert-object-into-world
              ^source-obj <source>
              ^source-type <type>
              ^matches-existing-object { <obj> <> none })
-->
   (<s> ^operator <o> +)
   (<o> ^name update-existing-object
        ^source-obj <source>
        ^source-type <type>
        ^existing-obj <obj>)
}
