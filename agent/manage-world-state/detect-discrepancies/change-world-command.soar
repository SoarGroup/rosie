#########################################################
#
# Discrepancy: change-world-command
#   This isn't an actual discrepancy, 
#     it is for allowing an external script to change the internal world
#   It happens if a ^change-world command appears on the input-link
# Handling: attend-to-perception/change-world-command
#
#########################################################

sp {perception-monitor*detect-discrepancies*change-world-command
   (state <s> ^superstate nil
              ^io.input-link.change-world <cmd>
              ^perception-monitor.discrepancies <discs>) 
-->
   (<discs> ^change-world-command <disc>)
   (<disc> ^command-info <cmd>)
}
