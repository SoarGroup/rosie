sp {train-label*prefer*all*over*complete-train-label
   (state <s> ^name train-label
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^category object-processing
         ^name <> complete-train-label)
   (<o2> ^name complete-train-label)
-->
   (<s> ^operator <o1> > <o2>)
}