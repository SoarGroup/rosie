# preferences
# Make sure complete-identify-object has the lowest preference
sp {identify-object*prefer*all*over*complete-identify-object
   (state <s> ^name identify-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name <> complete-identify-object
         ^category indexing)
   (<o2> ^name complete-identify-object)
-->
   (<s> ^operator <o1> > <o2>)
}