#################################################
# First: Only look at the created/delete objects
#        In case they need to be used as candidates
#################################################

sp {index-object*propose*attend-to-scene-changes
   (state <s> ^name index-object
             -^attended-to-scene-changes)
-->
   (<s> ^operator <o> +)
   (<o> ^name attend-to-scene-changes
        ^category indexing)
}