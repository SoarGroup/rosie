sp {comprehension*apply*process-semantics*DP*object*include*reference-info
   (state <s> ^operator <o>)
   (<o> ^name process-semantics
        ^assigner <asg>
        ^value <obj>)
   (<obj> ^item-type object)
   (<asg> ^structure-type DP
          ^parent-receiver.semantics <sem>
          ^merged-receiver <info>)
-->
   (<sem> ^reference-info <info>)
}

sp {comprehension*apply*merge*PP*copy*object*reference-info
   (state <s> ^operator <o>)
   (<o> ^name merge
        ^receiver.head <head>
        ^assigner <asg>)
   (<head> ^structure-type DP)
   (<asg> ^structure-type PP)
-->
   (<asg> ^pp-obj-reference-info <head>)
}


sp {comprehension*apply*process-semantics*PP*copy*reference-info*onto*relation
   (state <s> ^operator <o>)
   (<o> ^name process-semantics
        ^assigner <asg>
        ^<arg-name> <pred>)
   (<asg> ^structure-type PP
          ^pp-obj-reference-info <info>)
   (<info> ^referent <obj>)
   (<pred> ^2 <obj>)
-->
   (<pred> ^reference-info <info>)
}

sp {apply*process-semantics*relation*copy*reference-info
   (state <s> ^operator <op>)
   (<op> ^name process-semantics
         ^assigner <asg>
         ^attribute relation
         ^relation.{2} <obj>)
   (<asg> ^parent-receiver.semantics <sem>
          ^pp-obj-reference-info <info>)
   (<info> ^referent <obj>)
-->
   (<sem> ^reference-info <info>)
}

