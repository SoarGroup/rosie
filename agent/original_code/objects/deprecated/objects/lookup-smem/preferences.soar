sp {prefer*complete-lookup-smem*over*lookup
   (state <s> ^name lookup-smem
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name complete-lookup-smem)
   (<o2> ^name lookup)
-->
   (<s> ^operator <o1> > <o2>)
}