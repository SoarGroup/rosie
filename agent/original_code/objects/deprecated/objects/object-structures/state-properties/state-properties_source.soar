# These operators deal with properties of an object that come from the visual system

source create-state-property.soar
# when an object is created, adds a property for each state variable to the structure
# This could be a door:open or heat:on

source change-state-property.soar
# If the state of an object changes, this updates the object representation to reflect it
