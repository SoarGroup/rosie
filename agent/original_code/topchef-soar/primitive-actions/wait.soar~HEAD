sp {propose*wait
   (state <s> ^attribute state
              ^choices none
             -^operator.name wait)
-->
   (<s> ^operator <o> +)
   (<o> ^name wait)
}

#sp {propose*dummy1*operator
#   (state <s> ^topstate <any>
#   	      -^problem-space << lgsoar message-interpretation explain >>)
#  -->
#    (<s> ^operator <o> + <)
#    (<o> ^name dummy1
#    	^category primitive-action)
# }

# sp {propose*dummy2*operator
#    (state <s> ^topstate <any>
#    	      -^problem-space << lgsoar message-interpretation explain >>)
# -->
#    (<s> ^operator <o> + <)
#    (<o> ^name dummy2
#    	^category primitive-action)
# }