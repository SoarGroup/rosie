sp {prefer*lookup-next-level*over*complete-lookup-general
   (state <s> ^name lookup-general
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name lookup-next-level)
   (<o2> ^name complete-lookup-general)
-->
   (<s> ^operator <o1> > <o2>)
}