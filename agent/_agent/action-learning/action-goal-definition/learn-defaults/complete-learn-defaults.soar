sp {default-relation*apply*complete-default-relation
   (state <s> ^name learn-defaults
   	      ^superstate <ss>
	      ^operator.name complete-learn-defaults)
   (<ss> ^operator <op>
   	 ^purpose <p>)
-->
   (<ss> ^completed <p>)
}