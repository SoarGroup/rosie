## if there are no more further instructions to be analyzed, retract all proposed operators
sp {selection*episode*retrieval
   (state <s> ^name selection
   	      ^retrieval failure
	      ^superstate <ss>
	      ^quiescence t)
   (<ss> ^operator <op> +)
-->
   (<ss> ^operator <op> -)
}