sp {retrospective-learning-learning*elaborate*indexed-action
   (state <s> ^name retrospective-learning
   	      ^topstate-indexed-action <ia>)
   (<ia> ^name <name>
   	 ^_metadata <met>)
-->
   (<s> ^indexed-action <op>)
   (<op> ^name <name>
   	     ^_metadata <met>
   	     ^action-type composite)
}

### propose*composite*action
sp {retrospective-learning-learning*propose*operator
   (state <s> ^name retrospective-learning
   	      ^indexed-action <ia>)
-->
   (<s> ^operator <ia> + !)
}

### elaborate composite action arguments level 1
sp {retrospective-learning-learning*elaborate*composite-action-operator*level1
   (state <s> ^name retrospective-learning
   	      ^indexed-action <ia>
	      ^operator <op> +
	      ^object <obj>
	      ^duplicates.<val> <obj>)
   (<op> ^name <name>)
   (<ia> ^name <name>
   	 ^<arg> <val>)
-->
   (<op> ^<arg> <obj>)
}

sp {retrospective-learning-learning*elaborate*composite-action-operator*level2*object
   (state <s> ^name retrospective-learning
   	      ^indexed-action <ia>
	      ^operator <op> +
	      ^object <obj>
	      ^dulicates.<val> <obj>)
   (<op> ^name <name>)
   (<ia> ^name <name>
   	 ^<arg>.argument <val>)
-->
   (<op> ^<arg>.argument <obj>)
}

sp {retrospective-learning-learning*elaborate*composite-action-operator*level2*relation
   (state <s> ^name retrospective-learning
   	      ^indexed-action <ia>
	      ^operator <op> +
	      ^object <obj>
	      ^duplicates.<valy> <valx>)
   (<op> ^name <name>
   	 ^<arg> <karg>)
   (<karg> ^argument <valx>)
   (<ia> ^name <name>
   	 ^<arg> <nval>)
   (<nval> ^argument <valy>
   	   ^relation <rel>)
-->
   (<karg> ^relation <rel>)
}

# #sp {retrospective-learning*elaborate*desired



