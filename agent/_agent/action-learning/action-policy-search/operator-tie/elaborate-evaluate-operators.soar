### if evaluate-operators are proposed for operators that have foccussed elements mark them

# sp {elaborate*evaluate-operator*focus*elements*level1
#    (state <s> ^name selection
#    	      ^operator <op> +) 
#    (<op> ^name evaluate-operator
#    	 ^superoperator <sop>)
#    (<sop> ^<arg> <obj>)
#    (<obj> ^focus <any>)
# -->
#    (<op> ^in-focus true
#    	 ^analyzed true)
# }

# sp {elaborate*evaluate-operator*focus*elements*level2
#    (state <s> ^name selection
#    	      ^operator <op> +) 
#    (<op> ^name evaluate-operator
#    	 ^superoperator <sop>)
#    (<sop> ^<arg>.<val> <obj>)
#    (<obj> ^focus <any>)
# -->
#    (<op> ^in-focus true
#    	 ^analyzed true)
# }

# sp {retract*evaluate-operators*not-in-focus
#    (state <s> ^name selection
#    	      ^operator <op> +)
#    (<op> ^name evaluate-operator
#    	 -^in-focus
# 	 ^analyzed true)
# -->
#    (<s> ^operator <op> -)
# }

# sp {retract*evaluate-operators*delay
#    (state <s> ^name selection
#    	      ^operator <op> +)
#    (<op> ^name evaluate-operator)
# -->
#    (<op> ^analyzed true)
# }
   	 

