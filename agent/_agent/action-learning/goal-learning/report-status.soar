sp {action-goal-definition*write*status*superstate
   (state <s> ^name goal-learning
   	      ^operator.name report-status-goal-learning-success
          ^goal-lexical <lex>
	      ^topstate.interaction.status <stat>)
-->
   (<stat> ^performed <perf>)
   (<perf> ^learning-event <ae>)
   (<ae> ^type <lex>)
}

sp {goal-learning*apply*report-status-goal-learning-success*superstate
   (state <s> ^name goal-learning
              ^operator.name report-status-goal-learning-success
              ^goal-description <goal>
              ^superstate <ss>)
-->
   (<ss> ^learned-goal <goal>)
}