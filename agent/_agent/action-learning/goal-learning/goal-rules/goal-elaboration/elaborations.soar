### indexed-action
sp {copy*indexed-action*from*segment
   (state <s> ^name goal-elaboration
   	      ^topstate.interaction.stack.segment.next.purpose.parameters.indexable.result <iop>)
-->
   (<s> ^topstate-indexed-action <iop>
   	^indexed-action <ia>)
}

### copy indexed-action
sp {goal-rules*copy*indexed-action
   (state <s> ^name goal-elaboration
   	      ^topstate-indexed-action <iop>
	      ^indexed-action <ia>)
   (<iop> ^{<attr> <> desired} <val>)
-->
   (<ia> ^<attr> <val>)
}

### elaborate problemspace
# sp {goal-elaboration*elaborate*problemspace
#    (state <s> ^name goal-elaboration
#    	      ^problemspace <ps>)
#  -->
#    (<ps> ^name learning)
# }