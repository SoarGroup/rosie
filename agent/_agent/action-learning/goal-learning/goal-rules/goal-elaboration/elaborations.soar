### indexed-action
sp {copy*indexed-action*from*segment
   (state <s> ^name goal-elaboration
   	      ^superstate.indexed-action <iop>)
-->
   (<s> ^superstate-indexed-action <iop>
   	^indexed-action <ia>
	^name action)
}

### copy indexed-action
sp {goal-elaboration*copy*indexed-action
   (state <s> ^name goal-elaboration
   	      ^superstate-indexed-action <iop>
	      ^indexed-action <ia>)
   (<iop> ^{<attr> <> desired} <val>)
-->
   (<ia> ^<attr> <val>)
}

### write to superstate
sp {goal-elaboration*copy*desired
   (state <s> ^name goal-elaboration
   	      ^superstate-indexed-action <iop>
	      ^indexed-action.desired <d>)
-->
   (<iop> ^desired <d>)
}