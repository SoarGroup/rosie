## propose generate desired 

sp {propose*generate-desired
   (state <s> ^name learn-termination-rule
   	      -^desired <any>
	      ^segment-desired <seg-des>)
-->
   (<s> ^operator <op> + >)
   (<op> ^name generate-desired
   	 ^segment-desired <seg-des>)
}
 

## propose the indexed action
sp {propose*indexed-action*learn-termination-rule
   (state <s> ^name learn-termination-rule
   	      ^indexed-action <iop>
	      -^success <des>)
-->
   (<s> ^operator <iop> +)
}

