sp {evaluate-assignment*prefer*equal*same*evaluation
   (state <s> ^name evaluate-assignment
   	      ^operator <op1> +
	      ^operator <op2> +)
   (<op1> ^name complete-evaluate-assignment
   	  ^evaluation <eval>)
   (<op2> ^name complete-evaluate-assignment
   	  ^evaluation <eval>)
-->
   (<s> ^operator <op1> = <op2>)
}
