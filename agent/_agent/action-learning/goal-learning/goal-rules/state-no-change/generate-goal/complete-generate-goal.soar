sp {generate-goal*apply*complete-generate-goal
   (state <s> ^name generate-goal
   	      ^desired <d>
	      ^superstate.action <ia>
	      ^operator.name complete-generate-goal)
-->
   (<ia> ^desired <d>)
}