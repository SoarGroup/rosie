sp {goal-definition*apply*complete-goal-definition
   (state <s> ^name definition
              ^operator.name complete-goal-definition
              ^superstate <ss>)
-->
   (<ss> ^learned-definition true)
}