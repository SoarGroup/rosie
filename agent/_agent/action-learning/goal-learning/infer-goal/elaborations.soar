sp {infer-goal*elaborate*unchanged-state
   (state <s> ^name infer-goal
              ^topstate.interaction.stack.segment.info.unchanged-state <is>)
-->
   (<s> ^unchanged-state <is>)
}

sp {infer-goal*elaborate*used-items
   (state <s> ^name infer-goal
              ^topstate.interaction.stack.segment.info <info>)
   (<info> ^used-objects <objs>
           ^used-relations <rels>
           ^used-actions <actions>)
-->
   (<s> ^used-objects <objs>
        ^used-relations <rels>
        ^used-actions <actions>)
}

   