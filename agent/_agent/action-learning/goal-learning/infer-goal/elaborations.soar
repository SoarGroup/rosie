sp {infer-goal*elaborate*map*info
   (state <s> ^name infer-goal
              ^superstate.operator.indexed-action._metadata.map-id <map>)
   (<map> ^procedural <pid>
          ^goal <gid>)
-->
   (<s> ^goal-id <gid>
        ^procedural-id <pid>)
}