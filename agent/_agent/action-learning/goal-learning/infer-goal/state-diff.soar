sp {infer-goal*propose*state-diff
   (state <s> ^name infer-goal
              ^initial-state <initial>
              ^final-state <final>
             -^diff-result)
-->
   (<s> ^operator <o> +)
   (<o> ^name state-diff
        ^initial <initial>
        ^final <final>)
}
