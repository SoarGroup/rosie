sp {write*verify*desired*success*superstate
   (state <s> ^name termination
   	      ^operator <op>
	      ^iterator null
	      ^superstate <ss>)
   (<ss> ^desired <des>)
-->
    (<ss> ^nsuccess <des>)
}