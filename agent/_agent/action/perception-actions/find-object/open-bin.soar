sp {find-object*propose*open-bin
   (state <s> ^name find-object
              ^current-action.unopened-bin <bin>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name open-bin
        ^action-type custom
        ^bin <bin>)
}