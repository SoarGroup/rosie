# Stash a object that has been 'used' (placed on the goal or measuring device)
#   No longer need it

sp {find-object*propose*select-subaction-stash-used-object
   (state <s> ^name find-object
              ^used-object <obj>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name select-subaction-stash-used-object
        ^action-type custom
        ^used-object <obj>)
}

sp {find-object*apply*select-subaction-stash-used-object
   (state <s> ^name find-object
              ^operator <o>
              ^current-action <a>)
   (<o> ^name select-subaction-stash-used-object
        ^used-object <obj>)
-->
   (<a> ^subaction <sub>)
   (<sub> ^name stash-object
          ^object <obj>)
}

# TESTING
#sp {find-object*apply*stash-object*used
#   (state <s> ^name find-object
#              ^operator <o>
#              ^current-action <a>)
#   (<o> ^name stash-object
#        ^subaction <sub>)
#-->
#   (<a> ^complete true)
#}