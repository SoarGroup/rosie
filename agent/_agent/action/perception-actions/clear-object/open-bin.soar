sp {clear-object*propose*open-bin
   (state <s> ^name clear-object
              ^current-bin <bin>
             -^current-action.subaction)
   (<bin> ^open false)
-->
   (<s> ^operator <o> + > =)
   (<o> ^name open-bin
        ^action-type custom
        ^bin <bin>)
}