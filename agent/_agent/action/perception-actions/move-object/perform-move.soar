sp {move-object*propose*move-to-location
   (state <s> ^name move-object
              ^object-is-clear true
              ^move-metadata <move>
              ^object-to-move <obj>
              ^destination <dest>
             -^performed-move true)
-->
   (<s> ^operator <o> + >)
   (<o> ^name op_1
        ^_metadata <move>
        ^action-type composite
        ^argument3 <obj>
        ^argument4.argument <dest>)
}

sp {move-object*move-to-location*propose*copy-success-to-superstate
   (state <s> ^name op_1
              ^success <success>
              ^superstate.operator <o>)
   (<o> ^name op_1)
-->
   (<o> ^succeeded true)
}


sp {move-object*copy*moved-to-location
   (state <s> ^name move-object
              ^operator <o>
              ^action-goal <goal>)
   (<o> ^name op_1
        ^succeeded true)
-->
   (<s> ^performed-move true)
}

sp {move-object*prefer*skip*over*move
   (state <s> ^name move-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name skip-move-to-location)
   (<o2> ^name op_1)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {move-object*propose*skip-move-to-location
   (state <s> ^name move-object
              ^move-metadata
             -^performed-move true
             -^operator.name op_1
              ^location-to-measure-at.id <loc>
              ^object-to-measure.id <obj>
              ^topstate.prepositions.prep <prep>)
   (<prep> ^name on
           ^pair <p>)
   (<p> ^a <obj>
        ^b <loc>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name skip-move-to-location)
}

sp {move-object*apply*skip-move-to-location
   (state <s> ^name move-object
              ^operator.name skip-move-to-location)
-->
   (<s> ^performed-move true)
}

