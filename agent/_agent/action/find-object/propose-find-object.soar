sp {action-command*propose*find-object
   (state <s> ^name action
              ^topstate.interaction.stack.segment.purpose <p>)
   (<p> ^type action-command
        ^parameters.verb <verb>)
   (<verb> ^word find
           ^direct-object <obj>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name op_find-object
        ^object-desc <obj>)
   (interrupt)
}

sp {action-command*reject*find-object*move-opeator
   (state <s> ^name action
              ^superstate.operator.indexed-action.name op_1
              ^operator <o1> +)
   (<o1> ^name op_find-object)
-->
   (<s> ^operator <o1> -)
}
              