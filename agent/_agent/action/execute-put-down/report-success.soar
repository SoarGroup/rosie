sp {execute-put-down*propose*report-success
   (state <s> ^name execute-put-down
              ^current-action.command-status complete
              ^held-object.predicates.status live)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}

sp {execute-put-down*apply*report-success
   (state <s> ^name execute-put-down
              ^operator.name report-success
              ^current-action <a>)
-->
   (<a> ^status complete)
}