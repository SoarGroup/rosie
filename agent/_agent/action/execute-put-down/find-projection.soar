sp {execute-put-down*propose*find-projection
   (state <s> ^name execute-put-down
             -^projection-result
              ^held-object <held>
              ^destination <dest>
              ^relation-predicate <pred>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name find-projection
        ^predicate <pred>
        ^arg-list <args>)
   (<args> ^1 <held> ^2 <dest>)
}