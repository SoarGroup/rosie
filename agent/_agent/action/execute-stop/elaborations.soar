sp {execute-stop1*elaborate*current-action
   (state <s> ^name execute-stop1
              ^superstate.current-action <a>)
-->
   (<s> ^current-action <a>)
}

sp {execute-stop1*elaborate*world*superstate
   (state <s> ^name execute-stop1
              ^superstate.world <world>)
-->
   (<s> ^world <world>)
}