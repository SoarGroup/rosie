sp {execute-pick-up*prefer*attend-to-arm*over*report-success
   (state <s> ^name execute-pick-up
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-change)
   (<o2> ^name report-success)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {execute-pick-up*prefer*attend-to-scene*over*failure*operators
   (state <s> ^name execute-pick-up
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-change)
   (<o2> ^name << report-failure send-reset-command remove-command-status >>)
-->
   (<s> ^operator <o1> > <o2>)
}


