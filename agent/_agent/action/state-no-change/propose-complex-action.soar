sp {learn-proposal-rule*propose*indexed-action*execute
   (state <s> ^name learn-task-policy
	      ^superstate <ss>)
   (<ss> ^name action
   	 ^problemspace <ps>
	 ^indexed-action <ia>)
   (<ia> -^applied yes)
-->
   (<ss> ^operator <ia> +)
}

sp {learn-proposal-rule*propose*indexed-action*project
   (state <s> ^name learn-proposal-rule
	      ^superstate <ss>)
   (<ss> ^name action
   	 ^problemspace <ps>
	 ^indexed-action <ia>)
   (<ia> -^applied yes)
-->
   (<s> ^operator <ia> +)
}