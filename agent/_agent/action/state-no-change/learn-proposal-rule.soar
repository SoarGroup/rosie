sp {learn-proposal-rule*propose*indexed-action*project
   (state <s> ^name learn-proposal-rule
              ^superstate <ss>)
   (<ss> ^name action
         ^problem-space <ps>
         ^action-command <cmd>)
   (<ia> -^applied yes)
-->
   (<s> ^operator <cmd> +)
}
