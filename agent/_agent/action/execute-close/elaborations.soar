sp {execute-close*elaborate*allow-wait*true
   (state <s> ^name execute-close)
-->
   (<s> ^allow-wait true)
}

sp {execute-close*elaborate*attend-to-all-scene-change*true
   (state <s> ^name execute-close)
-->
   (<s> ^attend-to-all-scene-changes true)
}
