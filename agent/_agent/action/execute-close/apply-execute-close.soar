sp {topstate*apply*execute-close*send*open*command
   (state <s> ^topstate <top>
              ^operator <o>
              ^io.output-link <ol>)
   (<o> ^name execute-close
        ^arg1.id <id>)
-->
   (<ol> ^set-state <set>)
   (<set> ^id <id>
          ^name door
          ^value closed)
}

sp {topstate*apply*execute-close*mark*current-action*complete
   (state <s> ^topstate <s>
              ^operator <o>
              ^current-action <a>)
   (<o> ^name execute-close
        ^arg1.predicates.door closed)
-->
   (<a> ^status complete)
}
