sp {execute-orient1*elaborate*current-action
   (state <s> ^name execute-orient1
              ^superstate.current-action <a>)
-->
   (<s> ^current-action <a>)
}

sp {execute-orient1*elaborate*world*superstate
   (state <s> ^name execute-orient1
              ^superstate.world <world>)
-->
   (<s> ^world <world>)
}

sp {execute-orient1*elaborate*direction*from*superoperator
   (state <s> ^name execute-orient1
              ^superstate.operator.arg1.id.name <dir>)
-->
   (<s> ^direction <dir>)
}

sp {execute-orient1*elaborate*smem-query-cue*direction
   (state <s> ^name execute-orient1
              ^direction <dir>)
-->
   (<s> ^smem-query-cue <cue>)
   (<cue> ^name <dir>)
}

sp {execute-orient1*elaborate*direction-id*from*retrieval
   (state <s> ^name execute-orient1
              ^direction <dir>
              ^smem-query-cue <cue>
              ^retrieved-lti-cue <ret>)
   (<ret> ^cue <cue>
          ^retrieved-lti <id>)
   (<cue> ^name <dir>)
-->
   (<s> ^direction-id <id>)
}
