sp {action*elaborate*retrieve-lti*action-command*action*id
   (state <s> ^name action
              ^action-command.id <id>
             -^retrieved-lti <id>)
-->
   (<s> ^retrieve-lti <id>)
}

sp {action*elaborate*retrieve-lti*action-command*procedural
   (state <s> ^name action
              ^action-command.id.procedural <proc>
             -^retrieved-lti <proc>)
-->
   (<s> ^retrieve-lti <proc>)
}

sp {action*elaborate*retrieve-lti*action-command*procedural*argument
   (state <s> ^name action
              ^action-command.id.procedural <proc>
             -^retrieved-lti <arg>)
   (<proc> ^{<arg-name> <> op_name} <arg>)
-->
   (<s> ^retrieve-lti <arg>)
}