sp {initiate-fetch1*propose*report-success
   (state <s> ^name initiate-fetch1
              ^object-to-fetch.predicates <preds>
              ^current-waypoint <wp>
              ^starting-waypoint <wp>)
   (<preds> -^arm-status grabbed
             ^visible true)
-->
   (<s> ^operator <o> + >)
   (<o> ^name report-success)
}

sp {initiate-fetch1*apply*report-success
   (state <s> ^name initiate-fetch1
              ^operator.name report-success
              ^top-state.interaction.status <status>)
-->
   (<status> ^performed.action-event.type fetch1)
}