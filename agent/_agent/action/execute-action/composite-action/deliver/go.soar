sp {initiate-deliver1*propose*initiate-go1
   (state <s> ^name initiate-deliver1
              ^object-to-deliver.predicates.arm-status grabbed
              ^delivery-location <loc>
              ^current-waypoint <wp>
              -^delivery-waypoint <wp>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name initiate-go1
        ^action-type composite
        ^arg2 <loc-arg>)
   (<loc-arg> ^type predicate
              ^id.name to1
              ^2 <loc>)
}