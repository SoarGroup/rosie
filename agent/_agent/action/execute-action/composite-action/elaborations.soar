sp {composite-action*elaborate*action-context*superoperator
   (state <s> -^name evaluate-operator
               ^problem-space.name composite-action
              ^superstate.operator <o>)
-->
   (<s> ^action-context <o>)
}

sp {composite-action*elaborate*action-id*from*action-context
   (state <s> ^problem-space.name composite-action
              ^action-context.action-id <id>)
-->
   (<s> ^action-id <id>)
}
