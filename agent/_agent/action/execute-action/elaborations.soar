# elaborate action-operator
sp {execute-action*elaborate*action-operator*superoperator
   (state <s> ^name execute-action
              ^superstate.operator.action-operator <op>)
-->
   (<s> ^action-operator <op>)
}

sp {execute-action*elaborate*action-operator*procedural*arg-name
   (state <s> ^problem-space.action.type execute
              ^action-operator.action-id.procedural <proc>)
   (<proc> ^<arg-name> <arg>)
   (<arg> ^arg-type <arg-type>)
-->
   (<proc> ^arg-name <arg-name>)
}