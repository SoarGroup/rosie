sp {go-to-next-waypoint*propose*go-to-adjacent-area
   (state <s> ^name go-to-next-waypoint
              ^current-location <cur>
              ^current-waypoint <wp>
              ^destination <dest>)
   (<cur> ^sub-map.waypoint <wp>)
   (<dest> ^sub-map <area>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name go-to-adjacent-area
        ^current-location <wp>
        ^area <area>)
}