sp {initiate-fetch*prefer*mark*over*action
   (state <s> ^name initiate-fetch1
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << mark-object-visible mark-starting-waypoint >>)
   (<o2> ^action-type <any>)
-->
   (<s> ^operator <o1> > <o2>)
}