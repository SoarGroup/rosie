sp {execute-composite-action*elaborate*action-context*superoperator
   (state <s> ^problem-space.name execute-composite-action
              ^superstate.operator <o>)
-->
   (<s> ^action-context <o>)
}

sp {action*elaborate*action-id*from*action-context
   (state <s> ^problem-space.action.type <any>
              ^action-context.action-id <id>)
-->
   (<s> ^action-id <id>)
}
