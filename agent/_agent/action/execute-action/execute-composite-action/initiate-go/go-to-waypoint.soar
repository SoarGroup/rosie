sp {initiate-go1*propose*go-to-waypoint
   (state <s> ^name initiate-go1
              ^superstate.operator.arg2.id <wp>
              ^superstate.world.robot.current-waypoint <cur>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name go-to-waypoint
        ^current-location <cur>
        ^waypoint <wp>
        ^action-type composite)
}