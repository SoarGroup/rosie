sp {initiate-go1*propose*report-unknown-location
   (state <s> ^name initiate-go1
              ^superstate.operator <ssop>)
   (<ssop> -^arg2.id
            ^location <loc>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name report-unknown-location
        ^location <loc>)
}

sp {initate-go1*apply*report-unknown-location
   (state <s> ^name initiate-go1
              ^operator <o>
              ^top-state.interaction.status <status>)
   (<o> ^name report-unknown-location
        ^location <loc>)
-->
   (<status> ^failure <f>)
   (<f> ^type unknown-location
        ^location <loc>)
}
