sp {initiate-find*prefer*mark*over*action
   (state <s> ^name initiate-find1
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << mark-starting-waypoint report-success report-failure >>)
   (<o2> ^action-type <any>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {initiate-find*prefer*mark*over*expand-fringe
   (state <s> ^name initiate-find1
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << mark-starting-waypoint report-success report-failure >>)
   (<o2> ^name expand-fringe)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {initiate-find*prefer*action*over*expand-fringe
   (state <s> ^name initiate-find1
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << initiate-go1 search-for-object >>)
   (<o2> ^name expand-fringe)
-->
   (<s> ^operator <o1> > <o2>)
}