sp {execute-composite-action*elaborate*problem-space
   (state <s> -^name << go-to-waypoint >>
              ^superstate.problem-space <sps>
              ^superstate.operator <o>)
   (<sps> ^action.type execute)
   (<o> ^action-type composite)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^name execute-composite-action
         ^action <action>
         ^state <state>)
   (<action> ^type execute
             ^action-type composite)
   (<state> ^type current
            ^world-copy shallow)
}
   
sp {execute-composite-action*elaborate*problem-space*propose-actions*true
   (state <s> ^problem-space <ps>
              ^desired <des>)
   (<ps> ^name execute-composite-action
         ^action <action>)
-->
   (<action> ^propose-actions true)
}