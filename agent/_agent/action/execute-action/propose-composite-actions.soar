sp {execute-action*propose*composite*action-operator
   (state <s> ^problem-space.action.type execute
              ^action-operator <op>)
   (<op> ^action-id.action-type composite)
-->
   (<s> ^operator <op> +)
}

####################### propose initiate-go1 ##################################
sp {action*propose*initiate-go1
   (state <s> ^problem-space.action.propose-actions true)
-->
   (<s> ^operator <o> +)
   (<o> ^name initiate-go1
        ^action-name go1
         ^action-type composite)
}
