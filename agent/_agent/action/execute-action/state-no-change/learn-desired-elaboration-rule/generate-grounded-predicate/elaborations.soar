sp {generate-grounded-predicate*elaborate*current-action*from*superstate
   (state <s> ^name generate-grounded-predicate
              ^superstate.current-action <action>)
-->
   (<s> ^current-action <action>)
}

sp {generate-grounded-predicate*elaborate*action-id*from*current-action
   (state <s> ^name generate-grounded-predicate
              ^current-action.action-id <id>)
-->
   (<s> ^action-id <id>)
}

sp {generate-grounded-predicate*elaborate*predicate*from*superoperator
   (state <s> ^name generate-grounded-predicate
              ^superstate.operator.predicate <pred>)
-->
   (<s> ^predicate <pred>)
}

sp {generate-grounded-predicate*elaborate*world
   (state <s> ^name generate-grounded-predicate
              ^superstate.world <world>)
-->
   (<s> ^world <world>)
}