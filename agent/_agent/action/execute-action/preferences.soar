# If there is an action-command, reject operators that don't match
sp {execute-action*reject*operators*dont*match*action-operator
   (state <s> ^problem-space.action.type execute
              ^action-operator-matches.match-info <info>
              ^operator <o> +)
   (<info> ^operator <o>
           ^op-matched no)
-->
   (<s> ^operator <o> -)
}
