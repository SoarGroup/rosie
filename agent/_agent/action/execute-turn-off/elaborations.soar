sp {execute-turn-off*elaborate*allow-wait*true
   (state <s> ^name execute-turn-off)
-->
   (<s> ^allow-wait true)
}

sp {execute-turn-off*elaborate*attend-to-all-scene-change*true
   (state <s> ^name execute-turn-off)
-->
   (<s> ^attend-to-all-scene-changes true)
}

sp {topstate*elaborate*execute-turn-off*state-predicate*heat
   (state <s> ^topstate <s>
              ^operator <o> +)
   (<o> ^name execute-turn-off
        ^arg1.predicates.name << oven stove >>)
-->
   (<o> ^state-predicate heat)
}