sp {topstate*apply*execute-turn-off*send*turn-off*command
   (state <s> ^topstate <top>
              ^operator <o>
              ^io.output-link <ol>)
   (<o> ^name execute-turn-off
        ^arg1.id <id>
        ^state-predicate <pred>)
-->
   (<ol> ^set-state <set>)
   (<set> ^id <id>
          ^name <pred>
          ^value off)
}

sp {topstate*apply*execute-turn-off*mark*current-action*complete
   (state <s> ^topstate <s>
              ^operator <o>
              ^current-action <a>)
   (<o> ^name execute-turn-off
        ^state-predicate <pred>
        ^arg1.predicates.<pred> off)
-->
   (<a> ^status complete)
}
