sp {topstate*apply*execute-turn-off1*send*turn-off*command
   (state <s> ^topstate <top>
              ^operator <o>
              ^io.output-link <ol>)
   (<o> ^name execute-turn-off1
        ^arg1.id <id>
        ^arg1.predicates.<pred> on)
-->
   (<ol> ^set-state <set>)
   (<set> ^id <id>
          ^name <pred>
          ^value off)
}

sp {topstate*apply*execute-turn-off1*mark*current-action*complete
   (state <s> ^topstate <s>
              ^operator <o>
              ^current-action <a>)
   (<o> ^name execute-turn-off1
        ^arg1.predicates.<pred> off)
-->
   (<a> ^status complete)
}
