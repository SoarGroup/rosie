## prefer augment-goal-definition
sp {generate-grounded-predicate*prefer*augment-goal-definition*lookup-preposition
   (state <s> ^name generate-grounded-predicate
   	      ^operator <op1> +
	      ^operator <op2> +)
   (<op1> ^name augment-goal-definition)
   (<op2> ^name << lookup-preposition retrieve-concept >>)
-->
   (<s> ^operator <op1> > <op2>)
}

sp {generate-grounded-predicate*prefer*lookup-preposition*retrieve-concept
   (state <s> ^name generate-grounded-predicate
   	      ^operator <op1> +
	      ^operator <op2> +)
   (<op1> ^name lookup-preposition)
   (<op2> ^name smem-retrieve-new)
-->
   (<s> ^operator <op1> < <op2>)
}
