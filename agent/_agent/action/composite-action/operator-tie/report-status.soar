### if achieved max-depth, rely on the human instructor to guide task execution
sp {composite-action*operator-tie*achieved-max-depth*apply
   (state <s> ^name selection
   	      ^top-state.interaction.status <status>
	      ^operator.name achieved-max-depth
	      ^quiescence t)
-->
   (<status> ^failure <f>)
   (<f> ^information unknown-subaction
   	^type failure)
}
