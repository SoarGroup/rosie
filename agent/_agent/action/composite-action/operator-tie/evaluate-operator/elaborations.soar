sp {evaluate-operator*elaborate*state-to-copy*supersuperstate
   (state <s> ^name evaluate-operator
              ^superstate.superstate <sss>)
-->
   (<s> ^state-to-copy <sss>)
}

sp {evaluate-operator*elaborate*desired*success
   (state <s> ^name evaluate-operator
              ^problem-space.name composite-action
              ^desired <d>)
   (<d> ^satisfied true)
-->
   (<s> ^success <d>)
}