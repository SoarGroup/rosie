sp {composite-action*elaborate*desired*io
   (state <s> ^problemspace action
   	      ^action-type composite
	      ^ndesired <d>
	      ^io)
-->
   (<s> ^desired <d>)
}

sp {composite-action*elaborate*desired*learning
   (state <s> ^problemspace action
   	      ^action-type composite
	      ^ndesired <d>
	      -^io)
-->
   (<s> ^temp-desired <td>)
}

sp {composite-action*elaborate*desired*learning*track-relation
   (state <s> ^problemspace action
   	      ^action-type composite
	      ^ndesired <d>
	      -^io)
-->
   (<s> ^temp-desired <td>)
}

sp {composite-action*elaborate*desired*learning*achieve-state
   (state <s> ^problemspace action
   	      ^action-type composite
	      ^ndesired <d>
	      -^io)
-->
   (<s> ^temp-desired <td>)
}