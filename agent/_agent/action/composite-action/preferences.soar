### prefer smem-query procedural to everything
sp {composite*action*no-map-id*query
   (state <s> ^problemspace.name action
   	      ^operator <op> +
	      -^map-id)
   (<op> ^name smem-query
   	 ^cue.procedural <any>)
-->
   (<s> ^operator <op> >)
}


### prefer assign-goal assign-action space
sp {composite*action*prefer*assign-goal*assign-actionspace
   (state <s> ^problemspace.name action
   	      ^operator <op1> +
	      ^operator <op2> +)
   (<op1> ^name << assign-goal assign-action-space >>)
   (<op2> ^action-type << primitive composite >>)
-->
   (<s> ^operator <op1> > <op2>)
}