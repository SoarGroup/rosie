sp {execute-pick-up*elaborate*target-object*superoperator
   (state <s> ^name execute-pick-up
              ^superstate.operator.action.arg1 <obj>)
-->
   (<s> ^target-object <obj>)
}

sp {execute-pick-up*elaborate*current-action
   (state <s> ^name execute-pick-up
              ^superstate.current-action <a>)
-->
   (<s> ^current-action <a>)
}

sp {execute-pick-up*elaborate*world*superstate
   (state <s> ^name execute-pick-up
              ^superstate.world <world>)
-->
   (<s> ^world <world>)
}

sp {execute-pick-up*elaborate*attend-to-all-scene-changes*true
   (state <s> ^name execute-pick-up)
-->
   (<s> ^attend-to-all-scene-changes true)
}

sp {execute-pick-up*elaborate*allow-wait*true
   (state <s> ^name execute-pick-up)
-->
   (<s> ^allow-wait true)
}