sp {execute-pick-up*prefer*attend-to-arm*over*complete-pick-up
   (state <s> ^name execute-pick-up
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-change
         ^change.type arm-status-change)
   (<o2> ^name complete-pick-up)
-->
   (<s> ^operator <o1> > <o2>)
}