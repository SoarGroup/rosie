sp {get-goal-definition*apply*complete
   (state <s> ^name get-goal-definition
	      ^operator.name complete-get-goal-definition
	      ^superstate <ss>
	      ^quiescence t) 
   (<ss> ^operator <op>)
-->
   (<ss> ^operator <op> -)
}

sp {get-goal-definition*apply*complete*write*topstate
   (state <s> ^name get-goal-definition
	      ^operator.name complete-get-goal-definition
	      ^topstate.LTIstore <l>
	      ^goal-id <gid>) 
-->
   (<l> ^goal <gid>)
}