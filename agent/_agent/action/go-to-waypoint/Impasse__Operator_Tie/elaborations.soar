
sp {go-to-waypoint*create*state-copy*empty*world
   (state <s> ^name evaluate-operator
              ^problem-space.name go-to-waypoint)
-->
   (<s> ^world <world>)
   (<world> ^robot <robot>
            ^objects <objs>
            ^predicates <preds>)
   (<robot> ^arm <arm>)
}
