sp {waypoint-search*Operator_Tie*apply*evaluate-operator*create*initial*path-cost
   (state <s> ^name selection
              ^operator <o>
              ^superstate.current-location <loc>)
   (<o> ^name evaluate-operator
        ^superproblem-space.name waypoint-search
        ^evaluation <e>)
  -(<e> ^path-cost
        ^current-location)
-->
   (<e> ^path-cost 0
        ^current-location <loc>)
}
