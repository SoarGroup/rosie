
sp {action*go-to-waypoint*apply*report-success
   (state <s> ^name initiate-go1
              ^operator <o>
              ^superstate.world.robot.current-waypoint <wp>
              ^top-state.interaction.status <status>)
   (<o> ^name go-to-waypoint
        ^waypoint <wp>)
-->
   (<status> ^performed.action-event.type go1)
}