sp {go-to-waypoint*elaborate*destination*from*superoperator
   (state <s> ^name go-to-waypoint
              ^superstate.operator.waypoint <wp>)
-->
   (<s> ^destination <wp>
        ^desired.waypoint <wp>)
}

sp {go-to-waypoint*elaborate*problem-space
   (state <s> ^name go-to-waypoint)
-->
   (<s> ^problem-space <p>)
   (<p> ^name waypoing-search
        ^default-state-copy yes
        ^one-level-attributes destination type
        ^two-level-attributes top-state
        #^dont-copy current-location
                            )}