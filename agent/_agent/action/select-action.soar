sp {action*selection*apply*action*create*selected-action
   (state <s> ^problemspace.action <act>
              ^operator <o>)
   (<o> ^phase selection)
-->
   (<s> ^selected-action <sa>)
}

sp {action*selection*apply*action*copy*selected-action*structure
   (state <s> ^problemspace.action <act>
              ^operator <o>
              ^selected-action <sa>)
   (<o> ^name <op-name>
        ^{<att> <> phase} <val>
        ^phase selection)
-->
   (<sa> ^<att> <val>)
}

