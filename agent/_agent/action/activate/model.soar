sp {apply*activate*action-model
   (state <s> ^problemspace.name action
   	      -^action-marker composite
   	      ^operator <op>	
	     -^io
	      ^object <obj>
	      ^off <obj>)
   (<op> ^name op_activate
   	 ^argument1 <obj>)
-->
   (<s> ^off <obj> -
   	^on <obj>)
}

sp {apply*activate*action-model*composite
   (state <s> ^problemspace.name action
   	      ^action-marker composite
   	      ^operator <op>	
	     -^io
	      ^object <obj>
	      ^off <obj>
	      ^superstate <ss>)
   (<op> ^name op_activate
   	 ^argument1 <obj>)
-->
   (<ss> ^off <obj> -
   	^on <obj>)
}