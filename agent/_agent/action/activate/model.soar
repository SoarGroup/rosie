sp {apply*activate*action-model
   (state <s> ^problemspace action
   	      ^operator <op>	
	     -^io
	      ^object <obj>
	      ^off <obj>)
   (<op> ^name op_activate
   	 ^argument1 <obj>)
-->
   (<s> ^off <obj> -
   	^on <obj>)
}