sp {apply*activate*action-model
   (state <s> -^problemspace.action.type << execute none >>
   	      ^operator <op>	
	      ^environment <env>)
   (<env> ^off <obj>)
   (<op> ^name op_activate
   	 ^argument1 <obj>)
-->
   (<env> ^off <obj> -
   	^on <obj>)
}

