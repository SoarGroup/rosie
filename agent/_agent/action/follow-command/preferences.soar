# If there is an action-command, reject operators that don't match
sp {follow-command*reject*operators*dont*match*action-command
   (state <s> ^name follow-command
              ^action-command-matches.match-info <info>
              ^operator <o> +)
   (<info> ^operator <o>
           ^op-matched no)
-->
   (<s> ^operator <o> -)
}