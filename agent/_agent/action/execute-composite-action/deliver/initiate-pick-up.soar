sp {initiate-deliver1*prefer*initiate-pick-up1
   (state <s> ^name initiate-deliver1
              ^object-to-deliver <obj>
              ^current-waypoint <wp>
             -^delivery-waypoint <wp>
              ^operator <o> +)
   (<obj> ^predicates <preds>)
   (<preds> -^arm-status grabbed)
   (<o> ^name initiate-pick-up1
        ^arg1.id <obj>)
-->
   (<s> ^operator <o> >)
}