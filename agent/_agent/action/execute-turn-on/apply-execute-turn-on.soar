sp {topstate*apply*execute-turn-on1*send*turn-on*command
   (state <s> ^topstate <top>
              ^operator <o>
              ^io.output-link <ol>)
   (<o> ^name execute-turn-on1
        ^arg1.id <id>
        ^arg1.predicates.<pred> off)
-->
   (<ol> ^set-state <set>)
   (<set> ^id <id>
          ^name <pred>
          ^value on)
}

sp {topstate*apply*execute-turn-on1*mark*current-action*complete
   (state <s> ^topstate <s>
              ^operator <o>
              ^current-action <a>)
   (<o> ^name execute-turn-on1
        ^arg1.predicates.<pred> on)
-->
   (<a> ^status complete)
}
