sp {topstate*apply*execute-turn-on*send*turn-on*command
   (state <s> ^topstate <top>
              ^operator <o>
              ^io.output-link <ol>)
   (<o> ^name execute-turn-on
        ^arg1.id <id>
        ^arg1.predicates.<pred> off)
-->
   (<ol> ^set-state <set>)
   (<set> ^id <id>
          ^name <pred>
          ^value on)
}

sp {topstate*apply*execute-turn-on*mark*current-action*complete
   (state <s> ^topstate <s>
              ^operator <o>
              ^current-action <a>)
   (<o> ^name execute-turn-on
        ^arg1.predicates.<pred> on)
-->
   (<a> ^status complete)
}
