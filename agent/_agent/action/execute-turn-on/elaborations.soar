sp {execute-turn-on*elaborate*allow-wait*true
   (state <s> ^name execute-turn-on)
-->
   (<s> ^allow-wait true)
}

sp {execute-turn-on*elaborate*attend-to-all-scene-change*true
   (state <s> ^name execute-turn-on)
-->
   (<s> ^attend-to-all-scene-changes true)
}