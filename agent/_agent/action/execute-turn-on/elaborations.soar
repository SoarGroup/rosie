sp {execute-turn-on*elaborate*allow-wait*true
   (state <s> ^name execute-turn-on)
-->
   (<s> ^allow-wait true)
}

sp {execute-turn-on*elaborate*attend-to-all-scene-change*true
   (state <s> ^name execute-turn-on)
-->
   (<s> ^attend-to-all-scene-changes true)
}

sp {topstate*elaborate*execute-turn-on*state-predicate*heat
   (state <s> ^topstate <s>
              ^operator <o> +)
   (<o> ^name execute-turn-on
        ^arg1.predicates.name << oven stove >>)
-->
   (<o> ^state-predicate heat)
}