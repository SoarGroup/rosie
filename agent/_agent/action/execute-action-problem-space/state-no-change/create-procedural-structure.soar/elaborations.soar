sp {create-procedural-structure*elaborate*action-command*from*superoperator
   (state <s> ^name create-procedural-structure
              ^superstate.operator.action-command <cmd>)
-->
   (<s> ^action-command <cmd>)
   (interrupt)
}

sp {create-procedural-structure*elaborate*action-id*from*action-command
   (state <s> ^name create-procedural-structure
              ^action-command.id <id>)
-->
   (<s> ^action-id <id>)
}
