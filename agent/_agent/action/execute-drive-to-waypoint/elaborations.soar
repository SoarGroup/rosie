sp {execute-drive-to-waypoint1*elaborate*waypoint
   (state <s> ^name execute-drive-to-waypoint1
              ^superstate.operator.waypoint <wp>)
-->
   (<s> ^waypoint <wp>)
}

sp {execute-drive-to-waypoint1*elaborate*current-action
   (state <s> ^name execute-drive-to-waypoint1
              ^superstate.current-action <a>)
-->
   (<s> ^current-action <a>)
}

sp {execute-drive-to-waypoint1*elaborate*settings
   (state <s> ^name execute-drive-to-waypoint1)
-->
   (<s> ^allow-wait true
        ^attend-to-all-scene-changes true)
}
              