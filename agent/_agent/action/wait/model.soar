sp {op_wait*state*action-model
   (state <s> -^problemspace.action.type << execute none >>
   	      ^operator <op>
	      ^object <obj>
	      ^environment <env>)
   (<env> ^not-cooked <obj>
   	  ^predicate <pred>)
   (<op> ^type state-change
	 ^{<state-type> << cooked >>} <obj>
	 ^name op_wait)
   (<pred> ^relation.type on
   	   ^a <obj>
	   ^b.smem-rep.adjective stove)
-->
   (<env> ^cooked <obj>
   	^not-cooked <obj> -)
}