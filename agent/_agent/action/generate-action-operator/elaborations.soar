sp {generate-action-operator*elaborate*message*from*superoperator
   (state <s> ^name generate-action-operator
              ^superstate.operator.command-message <msg>)
-->
   (<s> ^command-message <msg>)
}

sp {generate-action-operator*elaborate*action-id
   (state <s> ^name generate-action-operator
              ^command-message.action.id <id>)
-->
   (<s> ^action-id <id>)
}

sp {generate-action-operator*elaborate*retrieve-lti*command-message*action
   (state <s> ^name generate-action-operator
              ^action-id <id>
             -^retrieved-lti <id>
             -^to-store <id>)
-->
   (<s> ^retrieve-lti <id>)
}

sp {generate-action-operator*elaborate*retrieve-lti*goal
   (state <s> ^name generate-action-operator
              ^action-id <id>
             -^retrieved-lti <goal>
             -^to-store <goal>)
   (<id> ^goal <goal>)
-->
   (<s> ^retrieve-lti <proc>)
}

sp {generate-action-operator*elaborate*retrieve-lti*procedural
   (state <s> ^name generate-action-operator
              ^action-id <id>
             -^retrieved-lti <proc>
             -^to-store <proc>)
   (<id> ^procedural <proc>)
-->
   (<s> ^retrieve-lti <proc>)
}

sp {generate-action-operator*elaborate*retrieve-lti*procedural*arguments
   (state <s> ^name generate-action-operator
              ^action-id.procedural.{ <arg-name> <> op_name <> execution-operator } <arg>
             -^retrieved-lti <arg>
             -^to-store <arg>)
-->
   (<s> ^retrieve-lti <arg>)
}