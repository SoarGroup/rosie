### propose goal-definition if the indexed-action is an composite action
sp {propose*get-goal-definition
   (state <s> ^name action  
   	      ^indexed-action._metadata.map-id.goal <gid>
	      -^retrieved-lti <gid>
	      ^problemspace.action.type execute)
-->
   (<s> ^operator <op> + !)
   (<op> ^name get-goal-definition
   	 ^goal-id <gid>)
}


