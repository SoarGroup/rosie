sp {manage-action-stack*prefer*push-action*higher*first
	(state <s> ^name manage-action-stack
            ^operator <o1> +
            ^operator <o2> +)
    (<o1> ^name push-action
          ^action.order <i1>)
    (<o2> ^name push-action
          ^action.order { <i2> < <i1> })
-->
    (<s> ^operator <o1> > <o2>)
}