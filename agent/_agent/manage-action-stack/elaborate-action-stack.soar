sp {top-state*elaborate*actions*top*action
   (state <s> ^superstate nil
              ^action-stack <stack>)
   (<stack> ^next { <action> <> none }
            ^actions <a>)
-->
   (<a> ^action <action>)
}

sp {top-state*elaborate*actions*next*action
   (state <s> ^superstate nil
              ^action-stack.actions <a>)
   (<a> ^action <action>)
   (<action> ^next { <next> <> none })
-->
   (<a> ^action <next>)
}