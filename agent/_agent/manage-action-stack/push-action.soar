sp {manage-action-stack*propose*push-action
   (state <s> ^name manage-action-stack
              ^change-name push
              ^change-info <action>)
-->
   (<s> ^operator <o> +)
   (<o> ^name push-action
        ^action <action>)
}
