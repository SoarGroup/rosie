sp {manage-action-stack*elaborate*action-stack
   (state <s> ^name manage-action-stack
              ^superstate.action-stack <stack>)
-->
   (<s> ^action-stack <stack>)
}

sp {manage-action-stack*elaborate*change-name
   (state <s> ^name manage-action-stack
              ^superstate.operator.change-name <name>)
-->
   (<s> ^change-name <name>)
}

sp {manage-action-stack*elaborate*change-info
   (state <s> ^name manage-action-stack
              ^superstate.operator.change-info <info>)
-->
   (<s> ^change-info <info>)
}