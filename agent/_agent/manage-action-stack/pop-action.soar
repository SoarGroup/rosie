sp {manage-action-stack*propose*pop-action
   (state <s> ^name manage-action-stack
              ^change-name pop
              ^change-info <action>)
-->
   (<s> ^operator <o> +)
   (<o> ^name pop-action
        ^action <action>)
}