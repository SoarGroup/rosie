# 1: attend-to-scene-changes
sp {index-object*greater-preferences*attend-to-scene-changes
   (state <s> ^name index-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-changes)
   (<o2> ^name << attend-to-object add-candidates find-matches >>)
-->
   (<s> ^operator <o1> > <o2>)
}

# 2: add-candidates
sp {index-object*greater-preferences*add-candidates
   (state <s> ^name index-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name add-candidates)
   (<o2> ^name << attend-to-object find-matches >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {index-object*add-candidates*prefer*focus*over*activation*all
   (state <s> ^name index-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name add-candidates
         ^candidate-type focus)
   (<o2> ^name add-candidates
         ^candidate-type << activation all >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {index-object*add-candidates*prefer*activation*over*all
   (state <s> ^name index-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name add-candidates
         ^candidate-type activation)
   (<o2> ^name add-candidates
         ^candidate-type all)
-->
   (<s> ^operator <o1> > <o2>)
}

# 3: attend-to-object
sp {index-object*greater-preferences*attend-to-object
   (state <s> ^name index-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-object)
   (<o2> ^name << find-matches >>)
-->
   (<s> ^operator <o1> > <o2>)
}


