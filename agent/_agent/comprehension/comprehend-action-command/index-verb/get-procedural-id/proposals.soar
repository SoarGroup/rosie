### propose augment-operator-definition
sp {propose*augment-operator-definition
   (state <s> ^name get-procedural-id
   	      ^new-map <nmap>
	      -^assimilated <nmap>)
-->
   (<s> ^operator <op> +)
   (<op> ^name augment-operator-definition
   	 ^new-map <nmap>)
}
