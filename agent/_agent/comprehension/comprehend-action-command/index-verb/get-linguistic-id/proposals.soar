## recursively propose get-linguistic-id if the exact cue is not matched
# sp {get-linguistic-id*propose*relax-cue
#    (state <s> ^name get-linguistic-id
#    	      ^smem.result.failure
# 	      ^cue <cue>)
#    (<cue> ^{<arg>  <> direct-object <> generic <> verb} <any>)
# -->
#    (<s> ^operator <op> +)
#    (<op> ^name get-linguistic-id
#    	 ^cue <cue>
# 	 ^failure specific-relation
# 	 ^relation <arg>)
# }

### propose retrieve-concept to retrieve pp-object in the action-concept-network
sp {propose*pp-object*get-linguistic-id
   (state <s> ^name get-linguistic-id
   	      ^linguistic <ling>)
   (<ling> ^pp-object <ppobj>)
   (<ppobj> -^<any>)
-->
   (<s> ^operator <op> +)
   (<op> ^name retrieve-concept
   	 ^ltid <ppobj>)
}


### propose create-new-mapping if even the relaxed cue fails\
sp {get-linguistic-id*failure
   (state <s> ^name get-linguistic-id
   	      ^smem.result <res>
	      ^original-cue <cue>)
   (<res> ^failure <any>)
-->
   (<s> ^operator <op> +)
   (<op> ^name create-new-mapping
   	 ^category indexing
	 ^cue <cue>)}