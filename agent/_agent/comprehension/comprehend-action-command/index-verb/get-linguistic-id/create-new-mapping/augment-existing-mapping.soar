sp {augment-existing-mapping*store
   (state <s> ^name create-new-mapping
   	      ^superstate <ss>
	      ^smem.command <scomm>)
   (<ss> ^operator <sop>)
   (<sop> ^smem-pointer <sp>
   	  ^argument <arg>
	  ^type <type>)
-->
  (<sp> ^<arg> <narg>)
  (<scomm> ^store <sp> <narg>)
  (<ss> ^new-map <nmap>)
  (<nmap> ^type <type>
  	  ^argument <arg>
	  ^smem-pointer <sp>)
}