sp {propose*count*argument*nonew-map
   (state <s> ^name generate-indexed-operator
	      ^indexed-operator.{<arg> <> name <> _metadata} <obj>
	      -^argument-count)
-->
   (<s> ^operator <op> +)
   (<op> ^name count-arguments
   	 ^<arg> <obj>)
}

## 
sp {retract*multiple*instantiated*arguments
   (state <s> ^name generate-indexed-operator
   	      ^operator <op1> +
	      ^operator <op2> +)
   (<op1> ^name count-arguments
   	  ^<arg> <obj>)
   (<op2> ^name count-arguments
   	  ^<arg> {<obj2> > <obj>})
-->
   (<s> ^operator <op2> -)
}

sp {apply*count-arguments
   (state <s> ^name generate-indexed-operator
   	      ^operator.name count-arguments)
-->
   (<s> ^argument-count 1)
}