sp {interaction*better-preferences*interpret-message
   (state <s> ^name interaction
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name interpret-message)
   (<o2> ^name << interpret-status send-message push-segment pop-segment modify-segment >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {interaction*better-preferences*interpret-status
   (state <s> ^name interaction
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name interpret-status)
   (<o2> ^name << send-message push-segment pop-segment modify-segment >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {interaction*better-preferences*send-message
   (state <s> ^name interaction
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name send-message)
   (<o2> ^name << push-segment pop-segment modify-segment >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {interaction*better-preferences*pop-segment
   (state <s> ^name interaction
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name pop-segment)
   (<o2> ^name << push-segment modify-segment >>)
-->
   (<s> ^operator <o1> > <o2>)
}
