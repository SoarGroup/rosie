## prefer pop-segment to interpret-status
sp {interaction*prefer*pop-segment*interpret-status
   (state <s> ^name interaction
   	      ^operator <op1> +
	      ^operator <op2> +)
   (<op1> ^name interpret-status)
   (<op2> ^name pop-segment)
-->
   (<s> ^operator <op1> < <op2>)
}
   