sp {generate-action-operator*prefer*all*over*store
   (state <s> ^name generate-action-operator
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << create-action-operator copy-command-arg mark-argument-optional >>)
   (<o2> ^name store-concepts)
-->
   (<s> ^operator <o1> > <o2>)
}