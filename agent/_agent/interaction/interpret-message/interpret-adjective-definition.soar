###################################################################
#
# interpret-adjective-definition
#   The instructor gives information about an adjective
#   Construction: *-ADJ-is-DP-T
#   E.g. 'Purple is a color', 'Triangle is a shape'
#
###################################################################

#######
# OPERATOR PROPOSAL
#######
sp {interpret-message*propose*interpret-adjective-definition
   (state <s> ^name interpret-message
              ^instructor-message <msg>)
   (<msg> ^message-type adjective-definition
         -^interpreted true)
-->
   (<s> ^operator <o> +)
   (<o> ^name interpret-adjective-definition
        ^message <msg>)
}

######
# OPERATOR ELABORATIONS
######

# Elaborate useful information from the message onto the operator

sp {interpret-message*elaborate*interpret-adjective-definition*feature-type
   (state <s> ^name interpret-message
              ^operator <o> +)
   (<o> ^name interpret-adjective-definition
        ^message.feature-type <ft>)
-->
   (<o> ^feature-type <ft>)
}

sp {interpret-message*elaborate*interpret-adjective-definition*feature-value
   (state <s> ^name interpret-message
              ^operator <o> +)
   (<o> ^name interpret-adjective-definition
        ^message.feature-value <fv>)
-->
   (<o> ^feature-value <fv>)
}

#####
# OPERATOR APPLICATION
#####

# Mark the message as interpreted (retracts operator)
sp {interpret-message*apply*interpret-adjective-definition*mark*interpreted
   (state <s> ^name interpret-message
              ^operator <o>)
   (<o> ^name interpret-adjective-definition
        ^message <msg>)
-->
   (<s> ^mark-interpreted <msg>)
}

#sp {interpret-message*apply*interpret-adjective-definition*create*segment
#   (state <s> ^name interpret-message
#              ^operator <o>)
#   (<o> ^name interpret-adjective-definition
#        ^feature-type <type>
#        ^feature-value <val>)
#-->
#   (<s> ^new-segment-info <info>)
#   (<info> ^purpose <p>
#           ^originator instructor)
#   (<p> ^type learn-adjective
#        ^parameters <params>
#        ^satisfaction <sat>)
#   (<sat> ^learning-event.type adjective)
#   (<params> ^feature-type <type>
#             ^feature-value <val>)
#}


              