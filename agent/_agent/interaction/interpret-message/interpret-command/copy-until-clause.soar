sp {interpret-command*apply*copy-command-arg*until-clause
   (state <s> ^name interpret-command
              ^operator <o>
              ^action-command <cmd>)
   (<o> ^name copy-command-arg
        ^arg-name { <arg-name> << arg1 arg2 >> }
        ^arg-value <val>)
   (<val> ^until-clause <clause>)
   (<clause> ^arg1.id.output-word <class>)
-->
   (<cmd> ^until-clause <new-clause>)
   (<new-clause> ^name count
             ^parameters <params>)
   (<params> ^count 1
             ^class <class>)
   (<s> ^copied-arg <arg-name>)
}
              