#PR - TODO: Remove this file after testing alternate flow.
#Need to retrieve handle from smem.


# Check if Rosie can see goal of specific game
sp {interpret-message*elaborate*interpret-what-is-question*do-you-see-goal
   (state <s> ^name interpret-message
              ^operator <o> +)
   (<o> ^name interpret-what-is-question
        ^message.arg1 <arg1>)
   (<arg1> ^handle <gtype>)
-->
   (<o> ^detect-game-concepts missionaries-and-cannibals1
        ^gtype <gtype>)       
}


sp {interpret-message*apply*interpret-what-is-question*create*do-you-see-goal
   (state <s> ^name interpret-message
              ^instructor-message <msg>
              ^operator <o>)
   (<o> ^name interpret-what-is-question
        ^detect-game-concepts <game>
        ^gtype <gtype>)
-->        
   (<s> ^detect-game-concepts <game>
        ^gtype <gtype>
        ^incomplete <inc>)
}  

sp {interpret-message*apply*get-no-result-predicates
    (state <s> ^name interpret-message ^operator <o> ^gtype <gtype> ^<gtype> <t> ^incomplete <inc>)
    (<o> ^name interpret-what-is-question
         ^gtype <gtype>)
    (<t> ^condition <con>)
    (<con> ^result.set <set>)
    (<set> -^instance)
    -->
    (<inc> ^condition <con>)
}   

# Figure how to complete interpret-message from here...if you want to go to satisfy purpose or send an outgoing message.