# The check-for-occlusion operator is proposed right in the attend-to-scene-change substate

sp {attend-to-scene-change*check-for-occlusion*elaborate*monitor-obj
   (state <s> ^name attend-to-scene-change
              ^operator <o> +)
   (<o> ^name check-for-occlusion
        ^object <obj>)
-->
   (<o> ^monitor-obj <obj>)
}

sp {attend-to-scene-change*check-for-occlusion*elaborate*parameters
   (state <s> ^name attend-to-scene-change
              ^operator <o> +)
   (<o> ^name check-for-occlusion)
-->
   (<o> ^report-to-topstate true
        ^report-to-superstate false)
}