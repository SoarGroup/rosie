sp {attend-to-scene-change*apply*attend-to-different-feature-val*remove*old
   (state <s> ^name attend-to-scene-change
              ^operator <o>)
   (<o> ^name attend-to-different-feature-val
        ^property <prop>)
   (<prop> ^svs-prop.feature-val <new-val>
           ^wm-prop <wm-prop>)
   (<wm-prop> ^feature-val {<old-val> <> <new-val>})
-->
   (<wm-prop> ^feature-val <old-val> -)
}

sp {attend-to-scene-change*apply*attend-to-different-feature-val*add*new
   (state <s> ^name attend-to-scene-change
              ^operator <o>)
   (<o> ^name attend-to-different-feature-val
        ^property <prop>)
   (<prop> ^svs-prop.feature-val <new-val>
           ^wm-prop <wm-prop>)
-->
   (<wm-prop> ^feature-val <new-val>)
}