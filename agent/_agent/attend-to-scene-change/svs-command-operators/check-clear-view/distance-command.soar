sp {check-clear-view*propose*distance-command
   (state <s> ^name check-clear-view
              ^object-id <ida>
              ^closest-command.result {<idb> <> none}
             -^distance-command.result)
-->
   (<s> ^operator <o> + =)
   (<o> ^name distance-command
        ^category perception
        ^object-a <ida>
        ^object-b <idb>
        ^distance-type distance)
}