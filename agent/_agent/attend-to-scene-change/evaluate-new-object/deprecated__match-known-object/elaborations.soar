sp {match-known-object*elaborate*perception-obj
   (state <s> ^name match-known-object
              ^superstate.operator.perception-obj <perc-obj>)
-->
   (<s> ^perception-obj <perc-obj>)
}

sp {match-known-object*elaborate*monitor-obj
   (state <s> ^name match-known-object
              ^superstate.monitor-object <obj>)
-->
   (<s> ^monitor-obj <obj>)
}

sp {match-known-object*elaborate*contained-threshold
   (state <s> ^name match-known-object
              ^topstate.agent-params.contained-threshold <thresh>)
-->
   (<s> ^contained-threshold <thresh>)
}

sp {match-known-object*elaborate*overlap-command
   (state <s> ^name match-known-object)
-->
   (<s> ^overlap-command <cmd>)
}