sp {match-stale-object*prefer*all*over*complete-match-stale-object
   (state <s> ^name match-stale-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name <> complete-match-stale-object
         ^category perception)
   (<o2> ^name complete-match-stale-object)
-->
   (<s> ^operator <o1> > <o2>)
}
