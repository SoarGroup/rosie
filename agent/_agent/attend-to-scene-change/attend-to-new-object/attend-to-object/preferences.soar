sp {attend-to-object*prefer*create-delete*over*attend
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << create-property delete-property >>)
   (<o2> ^name attend-to-property)
-->
   (<s> ^operator <o1> > <o2>)
}

