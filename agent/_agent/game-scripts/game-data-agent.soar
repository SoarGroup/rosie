trace -l 0
set ROSIE_ENV "tabletop"

set WORLD_USAGE "internal"
set initial_predicates { "on1" "below1" }

smem --set database memory
#smem --set timers three


#epmem --set database memory
source ../init-smem/init-smem_source.soar
source ../_agent_source.soar
#decide set-random-seed
#output agent-writes off
output warnings --off
epmem --off
#stats --track
run