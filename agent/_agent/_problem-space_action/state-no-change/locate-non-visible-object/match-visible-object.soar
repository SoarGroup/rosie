sp {locate-non-visible-object*propose*match-visible-object
   (state <s> ^name locate-non-visible-object
              ^match-info <info>)
   (<info> ^object <obj>
           ^matches <match>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name match-visible-object
        ^object <obj>
        ^visible-object <match>)
}

sp {locate-non-visible-object*apply*match-visible-object*mark*source
   (state <s> ^name locate-non-visible-object
              ^operator <o>)
   (<o> ^name match-visible-object
        ^object <obj>
        ^visible-object <match>)
-->
   (<obj> ^source <match>)
}
