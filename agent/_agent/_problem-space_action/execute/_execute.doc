This folder contains code for the execute action-type:

(state <s> ^problem-space <ps>)
(<ps> ^name action
      ^action.type execute)
      
These rules are for when the agent is actually trying to execute
actions in the real world

