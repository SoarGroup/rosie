sp {action*composite-action*apply*primitive-action*push*action-stack
   (state <s> ^problem-space <ps>
              ^operator <o>
              ^top-state.action-stack.changes <changes>)
   (<ps> ^name action
         ^action-type composite
         ^action.type execute)
   (<o> ^action-type primitive)
-->
   (<changes> ^push <action>)
   (<action> ^first-appearance true
             ^task-subaction true
             ^execution-operator <o>)
}
   