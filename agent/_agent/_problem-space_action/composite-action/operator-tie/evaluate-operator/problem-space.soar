sp {action*selection*evaluate-operator*elaborate*problem-space
   (state <s> ^name evaluate-operator
              ^superstate.problem-space.name action)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^name action
         ^default-state-copy no
         ^default-operator-copy yes
         ^default-desired-copy yes
         ^action <action>
         ^state <state>
         ^learning <learn>)
   (<action> ^type simulate
             ^propose-action-operators true)
   (<state> ^type imagined
            ^world-copy deep)
}

sp {action*selection*evaluate-operator*elaborate*learning*retrospective
   (state <s> ^name evaluate-operator
              ^problem-space.name action
              ^superstate.problem-space.learning.type retrospective
              ^problem-space <ps>)
   (<ps> ^state <state>
         ^learning <learn>)
-->
   (<state> ^subactions-copy deep)
   (<learn> ^type retrospective)
}

sp {action*selection*evaluate-operator*elaborate*learning*exploration
   (state <s> ^name evaluate-operator
              ^problem-space.name action
              ^superstate.problem-space.learning.type exploration
              ^problem-space.learning <learn>)
-->
   (<learn> ^type exploration)
}

