sp {action*state*no-change*propose*learn-task-subaction
   (state <s> ^problem-space.name action
              ^subaction-matched false
              ^child-action.execution-operator <subaction>
              ^action-operator <action>
              ^action-id <action-id>
             -^stored-subaction <subaction>)
   (<subaction> ^action-handle << remember1 wait1 recall1 >>)
   (<action> ^action-type composite)
-->
   (<s> ^operator <o> + )
   (<o> ^name learn-task-subaction
        ^subaction <subaction>
        ^action-operator <action>
        ^action-id <action-id>)
}