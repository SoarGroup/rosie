sp {problem-space*action*elaborate*action*domain*from*agent-params
   (state <s> ^problem-space <ps>
              ^top-state.agent-params.domain <domain>
             -^superstate.problem-space.action.domain)
   (<ps> ^name action
         ^action <action>)
-->
   (<action> ^domain <domain>)
}
  
sp {problem-space*action*elaborate*action*domain*from*superstate*problem-space
   (state <s> ^problem-space <ps>
              ^superstate.problem-space.action.domain <domain>)
   (<ps> ^name action
         ^action <action>)
-->
   (<action> ^domain <domain>)
}