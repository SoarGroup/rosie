sp {initiate-deliver1*prefer*initiate-put-down1
   (state <s> ^name initiate-deliver1
              ^object-to-deliver <obj>
              ^current-waypoint <wp>
              ^delivery-waypoint <wp>
              ^operator <o> +)
   (<obj> ^predicates <preds>)
   (<preds> ^arm-status grabbed)
   (<o> ^name initiate-put-down1
        ^arg1.object <obj>)
-->
   (<s> ^operator <o> >)
}
