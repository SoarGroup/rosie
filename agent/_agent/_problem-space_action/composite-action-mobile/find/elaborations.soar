sp {initiate-find*elaborate*problem-space*propose-actions
   (state <s> ^name initiate-find1
              ^problem-space.action <action>)
-->
   (<action> ^propose-actions true)
}

sp {initiate-find*elaborate*action-context*from*superoperator
   (state <s> ^initiate-find1
              ^superstate.operator <o>)
-->
   (<s> ^action-context <o>)
}

sp {initiate-find*elaborate*object-to-find*from*superoperator
   (state <s> ^name initiate-find1
              ^action-context.arg1.object <obj>)
-->
   (<s> ^object-to-find <obj>)
}

sp {initiate-find*elaborate*current-waypoint*from*top-state
   (state <s> ^name initiate-find1
              ^world.robot.current-waypoint <wp>)
-->
   (<s> ^current-waypoint <wp>)
}

sp {initiate-find*elaborate*starting-waypoint*from*action-context
   (state <s> ^name initiate-find1
              ^action-context.starting-waypoint <wp>)
-->
   (<s> ^starting-waypoint <wp>)
}

sp {initiate-find*elaborate*searched*from*action-context
   (state <s> ^name initiate-find1
              ^action-context.searched <wp>)
-->
   (<s> ^searched <wp>)
}

sp {initiate-find*elaborate*visited*from*action-context
   (state <s> ^name initiate-find1
              ^action-context.visited <wp>)
-->
   (<s> ^visited <wp>)
}

sp {initiate-find*elaborate*visited*current-waypoint
   (state <s> ^name initiate-find1
              ^current-waypoint <wp>)
-->
   (<s> ^visited <wp>)
}
             

sp {initiate-find*elaborate*fringe*current-waypoint
   (state <s> ^name initiate-find1
              ^current-waypoint <wp>)
-->
   (<s> ^fringe <wp>)
}

sp {initiate-find*elaborate*fringe-edge
   (state <s> ^name initiate-find1
              ^fringe.edge.end <wp>
             -^fringe <wp>)
-->
   (<s> ^fringe-edge <wp>)
}
