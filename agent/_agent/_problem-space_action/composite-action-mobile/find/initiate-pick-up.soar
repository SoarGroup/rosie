sp {initiate-find1*prefer*initiate-pick-up1
   (state <s> ^name initiate-find1
              ^object-to-find <obj>
              ^starting-waypoint <wp>
             -^current-waypoint <wp>
              ^operator <o> +)
   (<obj> ^predicates <preds>)
   (<preds> -^arm-status grabbed
             ^visible true)
   (<o> ^name initiate-pick-up1
        ^arg1.object <obj>)
-->
   (<s> ^operator <o> >)
   (<s> ^executing-primitive-action true)
}
