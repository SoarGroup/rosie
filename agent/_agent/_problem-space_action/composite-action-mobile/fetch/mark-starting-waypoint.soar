sp {initiate-fetch*propose*mark-starting-waypoint
   (state <s> ^name initiate-fetch1
              ^action-context <a>)
   (<a> -^starting-waypoint)
-->
   (<s> ^operator <o> + >)
   (<o> ^name mark-starting-waypoint)
}

sp {initiate-fetch*apply*mark-starting-waypoint
   (state <s> ^name initiate-fetch1
              ^operator.name mark-starting-waypoint
              ^world.robot.current-waypoint <wp>
              ^action-context <a>)
-->
   (<a> ^starting-waypoint <wp>)
}
