sp {initiate-fetch1*prefer*initiate-pick-up1
   (state <s> ^name initiate-fetch1
              ^object-to-fetch <obj>
              ^current-waypoint <wp>
              ^fetch-waypoint <wp>
              ^operator <o> +)
   (<obj> ^predicates <preds>)
   (<preds> -^arm-status grabbed
             ^visible true)
   (<o> ^name initiate-pick-up1
        ^arg1.id <obj>)
-->
   (<s> ^operator <o> >)
}