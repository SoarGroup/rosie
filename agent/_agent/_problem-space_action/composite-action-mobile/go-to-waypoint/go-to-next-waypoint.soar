sp {go-to-waypoint*propose*go-to-next-waypoint*all*adjacent*same*map
   (state <s> ^name go-to-waypoint
              ^same-map true
              ^current-location <cur>)
   (<cur> ^map.handle <map-handle>
          ^next <next>)
   (<next> ^map.handle <map-handle>)
-->
   (<s> ^operator <o> +)
   (<o> ^name go-to-next-waypoint
        ^current-location <cur>
        ^waypoint <next>)
}

sp {go-to-waypoint*prefer*go-to-next-waypoint*desired*waypoint
   (state <s> ^name go-to-waypoint
              ^operator <o> +
              ^desired.waypoint.handle <handle>)
   (<o> ^name go-to-next-waypoint
         ^waypoint.handle <handle>)
-->
   (<s> ^operator <o> >)
}
#
#sp {go-to-waypoint*propose*go-to-next-waypoint*destination-area
#   (state <s> ^name evaluate-operator
#              ^problem-space.name waypoint-search
#              ^desired.waypoint.handle <wp-handle>
#              ^world.robot.current-waypoint <cur-wp>)
#   (<cur-wp> ^next <next-wp>)
#   (<neighbor> ^handle <wp-handle>)
#-->
#   (<s> ^operator <o> + = >)
#   (<o> ^name go-to-next-waypoint
#        ^next <next-wp>)
#}
