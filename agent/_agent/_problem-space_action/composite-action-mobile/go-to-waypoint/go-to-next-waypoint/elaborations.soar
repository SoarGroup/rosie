sp {go-to-next-waypoint*elaborate*current-location
   (state <s> ^name go-to-next-waypoint
              ^superstate.operator.current-location <cur-loc>)
-->
   (<s> ^current-location <cur-loc>)
}

sp {go-to-next-waypoint*elaborate*destination
   (state <s> ^name go-to-next-waypoint
              ^superstate.operator.waypoint <wp>)
-->
   (<s> ^destination <wp>)
}

sp {go-to-next-waypoint*elaborate*world*superstate
   (state <s> ^name go-to-next-waypoint
              ^superstate.world <world>)
-->
   (<s> ^world <world>)
}

sp {go-to-next-waypoint*elaborate*current-waypoint
   (state <s> ^name go-to-next-waypoint
              ^world.robot.current-waypoint <wp>)
-->
   (<s> ^current-waypoint <wp>)
}

sp {go-to-next-waypoint*elaborate*current-waypoint*super-waypoint
   (state <s> ^name go-to-next-waypoint
              ^current-waypoint.map.super-waypoint <wp>)
-->
   (<s> ^current-waypoint <wp>)
}
