sp {get-procedural-id*write*count
   (state <s> ^superstate <ss>
   	      ^item-count <count>)
   (<ss> ^name generate-indexed-operator)
  -{(<s> ^item <item>)
    (<item> -^name count-arguments)}
-->
   (<ss> ^argument-count <count>)
}
   	      