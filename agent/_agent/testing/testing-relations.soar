sp {topstate*elaborate*smem-query-cue*on
   (state <s> ^topstate <s>)
-->
   (<s> ^smem-query-cue.name on1)
   (<s> ^smem-query-cue.name in1)
   (<s> ^smem-query-cue.name right-of1)
   (<s> ^smem-query-cue.name left-of1)
}

sp {topstate*elaborate*world*predicates
   :o-support
   (state <s> ^topstate <s>
              ^world-usage external
              ^smem-query-cue <cue>
              ^retrieved-lti-cue <ret>
              ^world.predicates <preds>)
   (<ret> ^cue <cue>
          ^retrieved-lti <lti>)
   (<lti> ^name { <pred-name> << on1 in1 right-of1 left-of1 >>})
-->
   (<preds> ^predicate <p>)
   (<p> ^name <pred-name>
         ^predicate-info <lti>)
   (<s> ^smem-query-cue <cue> -
        ^retrieved-lti-cue <ret> -)
}
           

#sp {topstate*propose*find-projection
#   (state <s> ^topstate <s>
#              ^retrieved-lti-cue.retrieved-lti <pred>
#              ^world.objects.object <obj1>
#              ^world.objects.object <obj2>
#             -^projection-result)
#   (<pred> ^name on1)
#   (<obj1> ^id 11)
#   (<obj2> ^id 1)
#-->
#   (<s> ^operator <o> + <)
#   (<o> ^name find-projection
#        ^arg-list <args>
#        ^predicate <pred>)
#   (<args> ^1 <obj1> ^2 <obj2>)
#}

     