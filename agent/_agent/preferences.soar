sp {topstate*better-preferences*lookup-preposition
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name lookup-preposition)
   (<o2> ^name << interaction comprehension satisfy-purpose action-learning action attend-to-scene-change wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {topstate*better-preferences*interaction
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name interaction)
   (<o2> ^name << comprehension satisfy-purpose action-learning action attend-to-scene-change wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {topstate*better-preferences*comprehension
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name comprehension)
   (<o2> ^name << satisfy-purpose action-learning action attend-to-scene-change wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {topstate*better-preferences*satisfy-purpose
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << satisfy-purpose action action-learning >>)
   (<o2> ^name << attend-to-scene-change wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {topstate*better-preferences*attend-to-scene-change
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-change)
   (<o2> ^name << wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

