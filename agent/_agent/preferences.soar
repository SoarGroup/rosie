sp {topstate*better-preferences*interpretation
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^kind interpretation)
   (<o2> ^name << push-segment pop-segment interpret-message index-parameters satisfy-purpose attend-to-scene wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}
   

sp {topstate*better-preferences*push*pop*segment
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << push-segment pop-segment >>)
   (<o2> ^name << interpret-message index-parameters satisfy-purpose attend-to-scene wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {topstate*better-preferences*interpret-message
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name interpret-message)
   (<o2> ^name << index-parameters satisfy-purpose attend-to-scene wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {topstate*better-preferences*index-parameters
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name index-parameters)
   (<o2> ^name << satisfy-purpose attend-to-scene wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {topstate*better-preferences*satisfy-purpose
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name satisfy-purpose)
   (<o2> ^name << attend-to-scene wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}


sp {topstate*better-preferences*attend-to-scene
   (state <s> ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene)
   (<o2> ^name << wait >>)
-->
   (<s> ^operator <o1> > <o2>)
}