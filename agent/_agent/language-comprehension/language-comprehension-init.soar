sp {language-comprehension*elaborate-operator*language-comprehension-init
   (state <s> ^superstate nil)
-->
   (<s> ^world-usage internal # determines whether uses simulator/world or an internal world. values  << internal external >>
        ^create-trace-file false # << true false >>
     # ^world <s> #comment out if internal. Confusion between where ^objects are - off top-state or off top-state.world
        )              
}

srand 1
#watch -c
#watch -L 1
