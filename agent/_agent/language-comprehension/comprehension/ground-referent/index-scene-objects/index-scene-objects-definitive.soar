sp {anystate*apply*index-scene-objects*definite*mark*generated
   (state <s> ^operator <o>)
   (<o> ^name index-scene-objects
        ^candidate-set <cand>
        ^specifier definite)
-->
   (<cand> ^generated-candidates true)
}

sp {anystate*apply*index-scene-objects*definite*create*candidates
   (state <s> ^operator <o>
              ^topstate.world.objects.object <obj>)
   (<o> ^name index-scene-objects
        ^candidate-set <cand>
        ^specifier definite)           
-->
   (<cand> ^proto-referent.object <obj>)
}