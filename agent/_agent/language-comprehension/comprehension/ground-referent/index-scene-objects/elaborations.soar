sp {index-scene-objects*elaborate*candidate-set*copy
   (state <s> ^name index-scene-objects
              ^superstate.operator.candidate-set <cand>)
-->
   (<s> ^candidate-set <cand>)
}

sp {index-scene-object*elaborate*specifier*copy
   (state <s> ^name index-scene-objects
              ^superstate.operator.specifier <spec>)
-->
   (<s> ^specifier <spec>)
}

sp {index-scene-object*elaborate*specifier*default
   (state <s> ^name index-scene-objects
              ^superstate.operator <o>)
   (<o> -^specifier)
-->
   (<s> ^specifier definite)
}
