sp {anystate*apply*index-scene-objects*indefinite*mark*generated
   (state <s> ^operator <o>)
   (<o> ^name index-scene-objects
        ^candidate-set <cand>
        ^specifier indefinite)
-->
   (<cand> ^generated-candidates true)
}

sp {anystate*apply*index-scene-objects*indefinite*create*candidates
   (state <s> ^operator <o>
              ^top-state.world.object <obj>)
   (<o> ^name index-scene-objects
        ^candidate-set <cand>
        ^specifier indefinite)
-->
   (<cand> ^proto-referent.object <obj>)
}