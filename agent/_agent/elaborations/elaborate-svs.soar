sp {svs*elaborate*svs*objects*ids*to*int 
   (state <s> #^superstate nil
   	          ^svs.spatial-scene <sc>)
   (<sc> ^child <C>)
   (<C> ^id {<obja1> <> eye <> arm}) 	# AM: Added so the eye (camerA) didn't get an id
 --> 
   (<C> ^id-int (int <obja1>))
 }

sp {svs*elaborate*svs*objects*type*node
   (state <s> #^superstate nil
   	          ^svs.spatial-scene <sc>)
   (<sc> ^child <C>)
 --> 
   (<C> ^type node)
 }