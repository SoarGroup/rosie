sp {topstate*interaction-stack*elaborate*segments*top
   (state <s> ^topstate <s>
              ^interaction.stack <stack>)
   (<stack> ^segments <segs>)
   (<segs> ^segment.prev <prev>)
-->
   (<segs> ^segment <prev>)
}