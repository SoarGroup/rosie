# For a composite action, right now just immediately validate
#   (Later, we'll actually do something interesting if we 
#     learn the proposal rules in hierarchical tasks)
sp {execute-action*propose*validate-composite-action
  (state <s> ^name execute-action
             ^current-action <a>)
  (<a> -^validated true
        ^execution-operator.action-type composite)
-->
  (<s> ^operator <o> + >)
  (<o> ^name validate-composite-action)
}

sp {execute-action*apply*validate-composite-action
  (state <s> ^name execute-action
             ^operator.name validate-composite-action
             ^current-action <a>)
-->
  (<a> ^validated true
       ^start-of-execution true)
}
