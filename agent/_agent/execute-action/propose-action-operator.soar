# if primitive action, propose action-operator if validated
sp {execute-action*propose*action-operator*primitive
   (state <s> ^name execute-action
              ^action-operator <op>
              ^current-action.validated true)
   (<op> ^action-type primitive)
-->
   (<s> ^operator <op> +)
}

# if composite action, propose action-operator
sp {execute-action*propose*action-operator*composite
   (state <s> ^name execute-action
              ^action-operator <op>)
   (<op> ^action-type composite)
-->
   (<s> ^operator <op> +)
}