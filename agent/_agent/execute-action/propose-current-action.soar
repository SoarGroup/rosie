# Once the action is validated, propose its execution-operator
sp {execute-action*propose*current-action
   (state <s> ^name execute-action
               ^current-action <a>)
   (<a> ^execution-operator <o>
        ^validated true)
-->
   (<s> ^operator <o> + >)
}

