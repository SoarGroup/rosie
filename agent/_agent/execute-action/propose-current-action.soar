sp {execute-action*propose*current-action*composite
   (state <s> ^name execute-action
              ^current-action.execution-operator <o>)
   (<o> ^action-type composite)
-->
   (<s> ^operator <o> + >)
}

sp {execute-action*propose*current-action*primitive
   (state <s> ^name execute-action
              ^current-action <a>)
   (<a> ^execution-operator <o>
        ^validated true)
   (<o> ^action-type primitive)
-->
   (<s> ^operator <o> + >)
}