sp {execute-composite-action*apply*primitive-action*push*action-stack*learning-needed
   (state <s> ^problem-space.name execute-composite-action
              ^operator <o>
              ^top-state.action-stack.changes <changeS>)
   (<o> ^action-type primitive)
   (<stack> ^changes <changes>)
-->
   (<changes> ^push <action>)
   (<action> ^first-appearance true
             ^task-subaction true
             ^execution-operator <o>)
}
   