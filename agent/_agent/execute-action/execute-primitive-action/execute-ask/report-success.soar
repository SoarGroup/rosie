sp {execute-ask*propose*report-success
   (state <s> ^problem-space.name execute-primitive-action
              ^action-handle ask1
              ^added-answer <ans>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name report-success)
}

sp {execute-ask*apply*report-success*mark*complete
   (state <s> ^problem-space.name execute-primitive-action
              ^action-handle ask1
              ^operator.name report-success
              ^current-action <a>)
-->
   (<a> ^status complete)
}

sp {execute-ask*apply*report-success*mark*parent*action*asked
   (state <s> ^problem-space.name execute-primitive-action
              ^action-handle ask1
              ^operator.name report-success
              ^sentence <sent>
              ^added-answer <ans>
              ^parent-action <a>)
-->
   (<a> ^asked <sent>
        ^added <ans>)
}
