sp {execute-go-to-location1*propose*go-to-waypoint
   (state <s> ^problem-space.name execute-primitive-action
              ^action-handle go-to-location1
              ^desired-waypoint <wp>)
-->
   (<s> ^operator <o> + )
   (<o> ^name go-to-waypoint
        ^waypoint <wp>
        ^action-type composite)
}

sp {execute-go-to-location1*elaborate*go-to-waypoint*current-waypoint
   (state <s> ^problem-space.name execute-primitive-action
              ^action-handle go-to-location1
              ^operator <o> +
              ^superstate.world.robot.current-waypoint <wp>)
   (<o> ^name go-to-waypoint)
-->
   (<o> ^current-waypoint <wp>)
}