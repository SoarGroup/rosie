sp {op_find*propose*report-failure
  (state <s> ^name op_find1
             ^target-object <obj>)
-->
  (<s> ^operator <o> + <)
  (<o> ^name report-failure
       ^failure-type cant-find-object
       ^failure-info <obj>)
}

             
