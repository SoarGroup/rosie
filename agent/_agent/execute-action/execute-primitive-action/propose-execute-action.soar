# If there is a child-action, propose execute-action
sp {execute-primitive-action*propose*execute-action*child-action
   (state <s> ^problem-space.name execute-primitive-action
              ^child-action <action>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name execute-action
        ^action <action>)
}