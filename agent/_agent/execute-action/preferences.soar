sp {execute-action*prefer*remove-first-app-flag*over*propose*action
   (state <s> ^name execute-action
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name remove-first-appearance-flag)
   (<o2> ^action-handle <any>)
-->
   (<s> ^operator <o1> > <o2>)
}