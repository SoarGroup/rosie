sp {top-state*apply*execute-action*pop*action
   (state <s> ^top-state <s>
              ^operator <o>
              ^action-stack.changes <changes>)
   (<o> ^name execute-action
        ^action <action>)
   (<action> ^status complete)
-->
   (<changes> ^pop <action>)
}
