# If the action has status complete, pop the action
sp {any-state*apply*execute-action*pop*action
   (state <s> ^operator <o>
              ^top-state.action-stack.changes <changes>)
   (<o> ^name execute-action
        ^action <action>)
   (<action> ^status complete)
-->
   (<changes> ^pop <action>)
}
