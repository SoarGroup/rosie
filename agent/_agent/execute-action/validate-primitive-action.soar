# if trying to execute a primitive action, propose the action operators
#   and verify that the action is valid (matches a proposed operator)

sp {execute-action*primitive*elaborate*problem-space*propose-actions*true
   (state <s> ^name execute-action
              ^problem-space.action <action>
              ^action-operator.action-type primitive
              -^current-action.validated true)
-->
   (<action> ^propose-action-operators true)
}

# If an operator matches and is selected, mark validated
sp {execute-action*primitive*apply*execute-operator*mark*validated
   (state <s> ^name execute-action
              ^operator <o>
              ^current-action <action>)
   (<o> ^action-type primitive)
-->
   (<action> ^validated true)
}