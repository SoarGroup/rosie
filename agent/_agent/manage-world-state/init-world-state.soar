sp {topstate*apply*init-agent*create*world-state
   (state <s> ^topstate <s>
              ^operator.name init-agent
              ^world-usage external)
-->
   (<s> ^world <w>)
   (<w> ^objects <objs>
        ^predicates <preds>
        ^robot <rob>)
   (<rob> ^arm <arm>)
}

sp {topstate*apply*init-agent*create*scene-monitor
   (state <s> ^topstate <s>
              ^operator.name init-agent)
-->
   (<s> ^scene-monitor <mon>)
   (<mon> ^scene-changes <sc>)
   (<sc> ^change-info <info>)
}