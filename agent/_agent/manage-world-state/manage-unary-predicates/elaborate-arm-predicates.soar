# elaborate predicate indicated=pointed

sp {topstate*elaborate*world*object*pointed*predicate
   (state <s> ^topstate <s>
              ^io.input-link.pointed-object <id>
              ^world.objects.object <obj>)
   (<obj> ^id <id>
          ^predicates <preds>)
-->
   (<preds> ^indicated pointed)
}

# elaborate predicate grabbed

sp {topstate*elaborate*world*object*grabbed*predicate
   (state <s> ^topstate <s>
              ^io.input-link.pointed-object <id>
              ^world.robot.arm.grabbed <obj>)
   (<obj> ^predicates <preds>)
-->
   (<preds> ^arm-status grabbed)
}

# elaborate predicate moving

sp {topstate*elaborate*world*object*grabbed*predicate
   (state <s> ^topstate <s>
              ^io.input-link.pointed-object <id>
              ^world.robot.arm.moving <obj>)
   (<obj> ^predicates <preds>)
-->
   (<preds> ^arm-status moving)
}