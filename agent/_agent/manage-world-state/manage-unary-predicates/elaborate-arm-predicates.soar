# elaborate predicate indicated=pointed

sp {topstate*elaborate*world*object*pointed*predicate
   (state <s> ^topstate <s>
              ^io.input-link.pointed-object <id>
              ^world.objects.object <obj>)
   (<obj> ^id <id>
          ^predicates <preds>)
-->
   (<preds> ^indicated pointed)
}

