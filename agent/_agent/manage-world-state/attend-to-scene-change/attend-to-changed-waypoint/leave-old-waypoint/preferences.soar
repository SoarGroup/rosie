sp {leave-old-waypoint*prefer*remove-unconfirmed-objects*over*others
   (state <s> ^name leave-old-waypoint
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name remove-unconfirmed-objects)
   (<o2> ^name << copy-object-metric-info remove-old-information >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {leave-old-waypoint*prefer*copy-object-metric-info*over*remove-old-information
   (state <s> ^name leave-old-waypoint
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name copy-object-metric-info)
   (<o2> ^name remove-old-information)
-->
   (<s> ^operator <o1> > <o2>)
}