sp {match-known-object*prefer*all*over*complete
   (state <s> ^name match-known-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << extract-distances match-candidate >>)
   (<o2> ^name complete-match-known-object)
-->
   (<s> ^operator <o1> > <o2>)
}