# Remove the change-info last (will remove the substate)
sp {update-existing-object*elaborate*worst*preference*remove-change-info
   (state <s> ^name update-existing-object
              ^operator <o> +)
   (<o> ^name remove-change-info)
-->
   (<s> ^operator <o> <)
}