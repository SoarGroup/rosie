sp {match-existing-object*propose*matches-none
   (state <s> ^name match-existing-object)
-->
   (<s> ^operator <o> +)
   (<o> ^name matches-none)
}

sp {match-existing-object*apply*matches-none
   (state <s> ^name match-existing-object
              ^operator.name matches-none
              ^superstate <ss>)
-->
   (<ss> ^matches-existing-object none)
}