sp {match-world-object*elaborate*object-handle*superoperator
   (state <s> ^name match-world-object
              ^superstate.operator.object-handle <handle>)
-->
   (<s> ^object-handle <handle>)
}

sp {match-world-object*elaborate*object-predicates*superoperator
   (state <s> ^name match-world-object
              ^superstate.operator.object-predicates <preds>)
-->
   (<s> ^object-predicates <preds>)
}

sp {match-world-object*elaborate*svs-obj*superoperator
   (state <s> ^name match-world-object
              ^superstate.operator.svs-obj <svs-obj>)
-->
   (<s> ^svs-obj <svs-obj>)
} 

sp {match-world-object*elaborate*match-visible*superoperator
   (state <s> ^name match-world-object
              ^superstate.operator.match-visible <bool>)
-->
   (<s> ^match-visible <bool>)
}