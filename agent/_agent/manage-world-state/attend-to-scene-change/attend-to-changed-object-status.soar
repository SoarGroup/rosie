sp {anystate*apply*attend-to-scene-change*changed-object-status*add*new
   (state <s> ^operator <o>)
   (<o> ^name attend-to-scene-change
        ^change <c>)
   (<c> ^type changed-object-status
        ^object-info.wm-obj.predicates <wm-preds>
        ^new-status <new>)
-->
   (<wm-preds> ^status <new>)
}

sp {anystate*apply*attend-to-scene-change*changed-object-status*remove*old
   (state <s> ^operator <o>)
   (<o> ^name attend-to-scene-change
        ^change <c>)
   (<c> ^type changed-object-status
        ^object-info.wm-obj.predicates <wm-preds>
        ^old-status <old>)
   (<wm-preds> ^status <old>)
-->
   (<wm-preds> ^status <old> -)
}

#sp {anystate*apply*attend-to-scene-change*changed-object-status*live
#   (state <s> ^operator <o>)
#   (<o> ^name attend-to-scene-change
#        ^change <c>)
#   (<c> ^type changed-object-status
#        ^object.wm-obj <obj>
#        ^new-status live)
#-->
#   (<obj> ^visible true
#          ^visible false -)
#}
#
#sp {anystate*apply*attend-to-scene-change*changed-object-status*stale
#   (state <s> ^operator <o>)
#   (<o> ^name attend-to-scene-change
#        ^change <c>)
#   (<c> ^type changed-object-status
#        ^object.wm-obj <obj>
#        ^new-status stale)
#-->
#   (<obj> ^visible false
#          ^visible true -)
#}