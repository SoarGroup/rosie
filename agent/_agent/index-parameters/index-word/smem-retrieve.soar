# Retrieve each sense
sp {index-word*elaborate*retrieve-lti*sense
   (state <s> ^name index-word
              ^retrieved-word.sense <sense>)
-->
   (<s> ^retrieve-lti <sense>)
}

# Retrieve each property
sp {index-word*propose*smem-retrieve*property
   (state <s> ^name index-word
              ^retrieved-word.sense.property <prop>)
-->
   (<s> ^retrieve-lti <prop>)
}