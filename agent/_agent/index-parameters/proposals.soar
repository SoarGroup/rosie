sp {index-parameters*propose*index-property
   (state <s> ^name index-parameters
              ^parameters.indexable <i>)
   (<i> ^type property
       -^internal)
-->
   (<s> ^operator <o> + =)
   (<o> ^name index-property
        ^property <i>)
}

sp {index-parameters*propose*index-word
   (state <s> ^name index-parameters
              ^parameters.indexable <i>)
   (<i> ^type word
       -^internal)
-->
   (<s> ^operator <o> + =)
   (<o> ^name index-word
        ^word <i>)
}

sp {index-parameters*propose*index-object
   (state <s> ^name index-parameters
              ^parameters.indexable <i>)
   (<i> ^type object
       -^internal)
-->
   (<s> ^operator <o> + =)
   (<o> ^name index-object
        ^object <i>)
}