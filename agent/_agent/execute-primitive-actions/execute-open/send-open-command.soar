sp {execute-open*propose*send-open-command
   (state <s> ^name execute-open1
              ^target-object <obj>
             -^current-action.open-command <cmd>)
-->
   (<s> ^operator <o> +)
   (<o> ^name send-open-command
        ^object <obj>)
}

sp {execute-open*apply*send-open-command
   (state <s> ^name execute-open1
              ^operator <o>
              ^top-state.io.output-link <out>
              ^current-action <a>)
   (<o> ^name send-open-command
        ^object.handle <obj-handle>)
-->
   (<out> ^set-state <set>)
   (<set> ^object-handle <obj-handle>
          ^name door
          ^value open)
   (<a> ^open-command <set>)
}
   
              
              