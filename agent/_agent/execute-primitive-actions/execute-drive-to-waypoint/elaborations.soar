sp {execute-drive-to-waypoint1*elaborate*waypoint
   (state <s> ^name execute-drive-to-waypoint1
              ^superstate.operator.waypoint <wp>)
-->
   (<s> ^waypoint <wp>)
}

sp {execute-drive-to-waypoint1*elaborate*wall-side
   (state <s> ^name execute-drive-to-waypoint1
              ^superstate.operator.side <side>)
-->
   (<s> ^wall-side <side>)
}

sp {execute-drive-to-waypoint1*elaborate*intermediate-turn
   (state <s> ^name execute-drive-to-waypoint1
              ^superstate.operator.intermediate-turn <yaw>)
-->
   (<s> ^intermediate-turn <yaw>)
}

