sp {go-to-waypoint*propose*go-to-waypoint*on*supermap
   (state <s> ^name go-to-waypoint
             -^same-map
              ^current-location.map.super-waypoint <cur-super>
              ^desired.waypoint.map.super-waypoint <des-super>)
-->
   (<s> ^operator <o> +)
   (<o> ^name go-to-waypoint
        ^current-location <cur-super>
        ^waypoint <des-super>)
}
