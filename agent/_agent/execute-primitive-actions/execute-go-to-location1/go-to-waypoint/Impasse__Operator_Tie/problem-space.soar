sp {go-to-waypoint*operator-tie*elaborate*name*selection
   (state <s> ^impasse tie
              ^superstate.name go-to-waypoint)
-->
   (<s> ^name selection)
}

sp {waypoint-search*selection*elaborate*problem-space
   (state <s> ^name selection
              ^superstate.problem-space.name waypoint-search)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^name waypoint-search
         ^defaule-state-copy no)
}