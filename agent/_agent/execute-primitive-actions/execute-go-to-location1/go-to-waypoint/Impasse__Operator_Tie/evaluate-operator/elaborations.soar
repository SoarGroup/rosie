sp {waypoint-search*evaluate-operator*copy*current-waypoint*superstate
   (state <s> ^name evaluate-operator
              ^problem-space.name waypoint-search
              ^superstate.current-waypoint <wp>)
-->
   (<s> ^current-waypoint <wp>)
}

sp {waypoint-search*evaluate-operator*copy*desired*from*superstate
   (state <s> ^name evaluate-operator
              ^problem-space.name waypoint-search
              ^superstate.desired <des>)
-->
   (<s> ^desired <des>)
}

sp {waypoint-search*evaluate-operator*elaborate*desired*success*waypoint
   (state <s> ^name evaluate-operator
              ^problem-space.name waypoint-search
              ^desired <d>
              ^current-waypoint.handle <handle>)
   (<d> ^waypoint.handle <handle>)
-->
   (<s> ^success <d>)
}
