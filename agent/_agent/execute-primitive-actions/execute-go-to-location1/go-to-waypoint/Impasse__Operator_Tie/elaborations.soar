sp {waypoint-search*selection*copy*current-waypoint*superstate
   (state <s> ^name selection
              ^problem-space.name waypoint-search
              ^superstate.current-waypoint <wp>)
-->
   (<s> ^current-waypoint <wp>)
}

sp {waypoint-search*selection*copy*desired*superstate
   (state <s> ^name selection
              ^problem-space.name waypoint-search
              ^superstate.desired <des>)
-->
   (<s> ^desired <des>)
}