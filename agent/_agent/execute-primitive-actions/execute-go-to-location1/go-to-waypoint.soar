sp {execute-go-to-location1*propose*go-to-waypoint
   (state <s> ^name execute-go-to-location1
              ^desired-waypoint <wp>
              ^superstate.world.robot.current-waypoint <cur>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name go-to-waypoint
        ^current-location <cur>
        ^waypoint <wp>
        ^action-type composite)
}