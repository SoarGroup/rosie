sp {top-state*elaborate*execute-turn-off1*state-predicate*heat
   (state <s> ^top-state <s>
              ^operator <o> +)
   (<o> ^name execute-turn-off1
        ^arg1.predicates.name << oven stove >>)
-->
   (<o> ^state-predicate heat)
}

sp {execute-turn-off*elaborate*state
   (state <s> ^name execute-turn-off1)
-->
   (<s> ^attend-to-all-scene-changes true
        ^allow-wait true)
}