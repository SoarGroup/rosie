sp {top-state*apply*execute-close1*send*open*command
   (state <s> ^top-state <top>
              ^operator <o>
              ^io.output-link <ol>)
   (<o> ^name execute-close1
				 ^arg1.id.handle <obj-handle>)
-->
   (<ol> ^set-state <set>)
	(<set> ^object-handle <obj-handle>
          ^name door
          ^value closed)
}

sp {top-state*apply*execute-close1*mark*current-action*complete
   (state <s> ^top-state <s>
              ^operator <o>
              ^current-action <a>)
   (<o> ^name execute-close1
        ^arg1.id.predicates.door closed)
-->
   (<a> ^status complete)
}
