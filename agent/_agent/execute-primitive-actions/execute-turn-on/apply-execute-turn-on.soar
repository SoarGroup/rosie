sp {top-state*apply*execute-turn-on1*send*turn-on*command
   (state <s> ^top-state <top>
              ^operator <o>
              ^io.output-link <ol>)
   (<o> ^name execute-turn-on1
        ^arg1.id.handle <obj-handle>
        ^arg1.id.predicates.<pred> off)
-->
   (<ol> ^set-state <set>)
	(<set> ^object-handle <obj-handle>
          ^name <pred>
          ^value on)
}

sp {top-state*apply*execute-turn-on1*mark*current-action*complete
   (state <s> ^top-state <s>
              ^operator <o>
              ^current-action <a>)
   (<o> ^name execute-turn-on1
        ^arg1.id.predicates.<pred> on)
-->
   (<a> ^status complete)
}
