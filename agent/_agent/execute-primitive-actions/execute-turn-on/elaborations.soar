sp {execute-turn-on*elaborate*target-object*from*superoperator
   (state <s> ^name execute-turn-on1
              ^superstate.operator.arg1.id <obj>)
-->
   (<s> ^target-object <obj>)
}

sp {execute-turn-on*elaborate*state-predicate*heat
   (state <s> ^name execute-turn-on1
              ^target-object.predicates.name << oven stove >>)
-->
   (<s> ^state-predicate heat)
}
