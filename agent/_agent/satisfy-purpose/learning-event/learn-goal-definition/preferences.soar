sp {learn-goal-definition*prefer*add-goal-predicate*over*store
   (state <s> ^name learn-goal-definition
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name add-goal-predicate)
   (<o2> ^name store-goal)
-->
   (<s> ^operator <o1> > <o2>)
}
   
sp {learn-goal-definition*prefer*smem*operators
   (state <s> ^name learn-goal-definition
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << smem-query smem-retrieve-new >>)
   (<o2> ^name << create-goal-structure add-goal-predicate add-matched-argument add-default-argument store-goal >>)
-->
   (<s> ^operator <o1> > <o2>)
}