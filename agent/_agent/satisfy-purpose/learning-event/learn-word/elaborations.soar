# Copies the word onto the substate
sp {learn-word*elaborate*word
   (state <s> ^name learn-word
              ^superstate.operator.word <word>)
-->
   (<s> ^word <word>)
}

sp {learn-word*elaborate*property
   (state <s> ^name learn-word
              ^superstate.purpose.parameters.indexable <i>)
   (<i> ^type property)
-->
   (<s> ^property <i>)
}