sp {retrospective-learning*prefer*copy-context*over*subactions
   (state <s> ^name retrospective-learning
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name copy-action-context)
   (<o2> ^name << copy-subaction-info retrieve-subaction-episode >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {retrospective-learning*prefer*copy-world*over*retrieve-subaction-episodes
   (state <s> ^name retrospective-learning
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name copy-world)
   (<o2> ^name retrieve-subaction-episode)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {retrospective-learning*prefer*copy*subaction*over*retrieve
   (state <s> ^name retrospective-learning
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name copy-subaction-info)
   (<o2> ^name retrieve-subaction-episode)
-->
   (<s> ^operator <o1> > <o2>)
}