## if there are no more further instructions to be analyzed, retract all proposed operators
sp {retrospective-learning*selection*retrieval-failure*retract
   (state <s> ^problem-space.name retrospective-learning
              ^name selection
              ^epmem-retrieval failure
              ^superstate <ss>
              ^quiescence t)
   (<ss> ^operator <o> +)
-->
   (<ss> ^operator <o> -)
}