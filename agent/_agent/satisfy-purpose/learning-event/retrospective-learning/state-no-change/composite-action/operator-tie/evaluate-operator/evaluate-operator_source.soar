sp {retrospective-learning*evaluate-operator*elaborate*operator*duplicate-of
   (state <s> ^name evaluate-operator
              ^operator <copy-op> +
              ^copy-operator-map.<src-op> <copy-op>)
-->
   (<copy-op> ^duplicate-of <src-op>)
}

sp {retrospective-learning*evaluate-operator*elaborate*look-ahead-operator
   (state <s> ^name evaluate-operator
              ^superstate.operator <ss-op>
              ^copy-operator-map.<src-op> <copy-op>)
   (<ss-op> ^superoperator <src-op>)
-->
   (<s> ^look-ahead-operator <src-op>
        ^untried-tied-operator <src-op>)
}

# Souce file "elaborations.soar"
source elaborations.soar

# Souce file "problem-space.soar"
source problem-space.soar

# Souce file "apply-action.soar"
source apply-action.soar

source copy-subactions.soar