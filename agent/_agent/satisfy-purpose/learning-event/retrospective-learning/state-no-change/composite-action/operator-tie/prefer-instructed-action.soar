sp {retrospective-learning*selection*prefer*evaluate-operator*match
   (state <s> ^name selection
              ^problem-space.name retrospective-learning
              ^operator-matches.match-info <info>
              ^operator <o> +)
   (<info> ^op-matched yes
           ^operator <op>)
   (<o> ^name evaluate-operator
        ^superoperator <op>)
-->
   (<s> ^operator <o> >)
}
