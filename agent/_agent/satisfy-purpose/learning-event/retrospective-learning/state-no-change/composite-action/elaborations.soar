sp {retrospective-learning*composite-action*copy-subactions
   (state <s> -^name evaluate-operator
               ^problem-space.name composite-action
              ^superstate.subactions <subs>)
-->
   (<s> ^subactions <subs>)
}

sp {retrospective-learning*composite-action*elaborate*desired*satisfied*superstate
   (state <s> ^problem-space.name composite-action
              ^superstate <ss>
              ^desired.satisfied true)
   (<ss> ^problem-space.name retrospective-learning)
-->
   (<ss> ^desired.satisfied true)
}