#sp {retospective-learning*elaborate*smem-query*action
#   (state <s> ^name retrospective-learning
#              ^action-operator.action-handle <action-handle>)
#-->
#  (<s> ^smem-query <query>)
#  (<query> ^cue <cue>
#            ^depth 8)
#  (<cue> ^handle <action-handle>)
#}
