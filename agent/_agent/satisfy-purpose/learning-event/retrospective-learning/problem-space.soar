sp {retrospective-learning*elaborate*problem-space
   (state <s> ^name retrospective-learning)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^name retrospective-learning
         ^action <action>
         ^state <state>
         ^learning <learning>)
     (<action> ^type simulate)
     (<learning> ^type exploitation)
     (<state> ^type retrospective)
}

sp {retrospective-learning*elaborate*state*world-copy*deep
   (state <s> ^name retrospective-learning
              ^problem-space.state <state>
              ^initial-episode.world <world>)
-->
   (<state> ^world-copy deep
            ^world-to-copy <world>)
}