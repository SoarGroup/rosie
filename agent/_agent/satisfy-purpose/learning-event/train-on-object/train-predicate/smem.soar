sp {train-predicate*elaborate*retrieve*predicate
   (state <s> ^name train-predicate
              ^predicate.id <pred>)
   (<pred> -^name)
-->
   (<s> ^nretrieve-lti <pred>)
}

sp {train-predicate*elaborate*retrieve*predicate*property
   (state <s> ^name train-predicate
              ^predicate.id.property <prop>)
   (<prop> -^name)
-->
   (<s> ^nretrieve-lti <prop>)
}

