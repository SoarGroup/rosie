sp {interpret-command*elaborate*parameters
   (state <s> ^name interpret-command
              ^superstate.operator.parameters <params>)
-->
   (<s> ^parameters <params>)
}

# Elaborate argument-attributes
sp {interpret-command*elaborate*metadata
   (state <s> ^name interpret-command)
-->
   (<s> ^_metadata <met>)
   (<met> ^argument-attributes <arg-attr>)
}

sp {interpret-command*elaborate*argument*depth*one
   (state <s> ^name interpret-command
              ^_metadata <met>)
   (<met> ^map-id.procedural

#### elaborate attributes
sp {interpret-command*elaborate*argument-attributes*do
   (state <s> ^name interpret-command
   	      ^_metadata <met>)
   (<met> ^map-id.linguistic.direct-object <so>
   	  ^map-id.procedural.<arg> <so>
	  ^argument-attributes <ft>)
-->
   (<ft> ^<arg> <fnew>)
}

sp {interpret-command*elaborate*argument-attributes*ppo
   (state <s> ^name interpret-command
   	      ^_metadata <met>)
   (<met> ^map-id.linguistic.pp-object <ppo>
   	  ^map-id.procedural.<arg> <proco>
	  ^argument-attributes <ft>)
   (<ppo> ^object <so>)
   (<proco> ^argument <so>)
-->
   (<ft> ^<arg> <fnew>)
   (<fnew> ^argument <o>)
}

sp {interpret-command*copy*object*attributes*level1
   (state <s> ^name interpret-command	
   	      ^_metadata <met>
	      ^indexable-parameter <param>)
   (<met> ^map-id <map>
   	  ^argument-attributes <ft>)
   (<map> ^linguistic.direct-object <dobj>
   	  ^procedural.<arg> <dobj>)
   (<param> ^direct-object.indexable.result.sense <sense>)
   (<sense> ^label <label>
   	    ^property.name <attribute>)
   (<ft> ^<arg> <val>)
-->
   (<val> ^<attribute> <label>)
}

sp {interpret-command*copy*object*attributes*level2
   (state <s> ^name interpret-command	
   	      ^_metadata <met>
	      ^indexable-parameter <param>)
   (<met> ^map-id <map>
   	  ^argument-attributes <ft>)
   (<map> ^linguistic.pp-object.object <dobj>
   	  ^procedural.<arg>.<arg2> <dobj>)
   (<param> ^pp-object.object.indexable.result.sense <sense>)
   (<sense> ^label <label>
   	    ^property.name <attribute>)
   (<ft> ^<arg>.<arg2> <val>)
-->
   (<val> ^<attribute> <label>)
}
   

### elaborate indexable parameter
sp {elaborate*initialized
   (state <s> ^name interpret-command
   	      ^superstate.operator <sop>)
   (<sop> ^name interpret-command
   	  ^initialized <i>)
-->
   (<s> ^initialized <i>)
}


