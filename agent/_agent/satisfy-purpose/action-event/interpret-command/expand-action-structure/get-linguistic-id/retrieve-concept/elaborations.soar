# elaborate recursive retrieve
sp {elaborate*recursive*retrieve
   (state <s> ^name retrieve-concept
   	      ^superstate.operator.recursive true)
-->
   (<s> ^recursive true)
}

# elaborate depth limited retrieve
sp {elaborate*depth*retrieve
   (state <s> ^name retrieve-concept
   	      ^superstate.operator.depth <dep>)
-->
   (<s> ^depth <dep>)
}


# elaborate bit flip
sp {elaborate*bit-flip*retrieve
   (state <s> ^name retrieve-concept)
 -->
   (<s> ^bit flip)
}