sp {action-event*propose*interpret-command
   (state <s> ^name action-event
              ^purpose.parameters <params>)
-->
   (<s> ^operator <o> +)
   (<o> ^name interpret-command
        ^command-info <params>)
}