sp {dialog-event*propose*game-start
   (state <s> ^name dialog-event
              ^purpose <p>)
   (<p> ^type game-start
        ^parameters.game-name <name>)
-->
   (<s> ^operator <o> +)
   (<o> ^name game-start
        ^category svs
	    ^game-name <name>)
}