sp {learn-defaults*store*defaults
   (state <s> ^name learn-defaults
   	      ^learn-default-for <f>
	      ^smem.command <scomm>
	       ^topstate.verb-parameters.use-default-argumentation true) ### model variation
   (<f> ^goal-predicate-example <gp>
   	^predicate-definition <def>
	^predicate-grounding <predicate>)
  (<def> ^<attr> <val>)
  (<gp> ^<attr> <gval>)
  (<predicate> -^<attr>)
  (<gval> ^smem-rep <rep>)
-->
  (<val> ^default <rep>)
  (<scomm> ^store <val> <rep>)
}