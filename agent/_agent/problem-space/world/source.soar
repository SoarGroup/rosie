# Default world to copy is from the superstate

sp {problem-space*world*source*elaborate*default
   (state <s> ^problem-space <ps>
              ^superstate.world <world>)
   (<ps> ^world <w>)
   -{(<w> ^source <> <world>)}
-->
   (<w> ^source <world>)
}