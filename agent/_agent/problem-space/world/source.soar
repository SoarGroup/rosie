# problem-space.world.source <world>
#   Specifies the world to be copied (defaults to superstate.world)

# If no other world specified, use superstate.world
sp {problem-space*world*source*elaborate*default
   (state <s> ^problem-space <ps>
              ^superstate.world <world>)
   (<ps> ^world <w>)
   -{(<w> ^source <> <world>)}
-->
   (<w> ^source <world>)
}