sp {problem-space*state*duplicate-of*elaborate*default
   (state <s> ^problem-space.state <state>
              ^superstate <ss>)
   -{(<state> ^duplicate-of <> <ss>)}
-->
   (<state> ^duplicate-of <ss>)
}