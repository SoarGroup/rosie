sp {problem-space*state*elaborate*state-to-copy*default*superstate
   (state <s> ^problem-space.state <state>
              ^superstate <ss>)
   (<state> -^state-to-copy <> <ss>)
-->
   (<state> ^state-to-copy <ss>)
}