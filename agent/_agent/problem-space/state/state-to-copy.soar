sp {problem-space*state*elaborate*state-to-copy*default*superstate
   (state <s> ^problem-space.state.{ << world-copy desired-copy operator-copy >>} <any>
              ^superstate <ss>)
   (<s> -^state-to-copy <> <ss>)
-->
   (<s> ^state-to-copy <ss>)
}