sp {action*simulate*search*success*propose*learn-task-subaction
   (state <s> ^problem-space <ps>
              ^superstate.name search-for-next-action
              ^action-operator <action>
              ^action-id <action-id>
              ^successful-operator <subaction>
             -^subaction-structure)
   (<ps> ^name action
         ^action.type simulate)
-->
  (<s> ^operator <o> + >)
   (<o> ^name learn-task-subaction
        ^subaction <subaction>
        ^action-operator <action>
        ^action-id <action-id>)
   
(<s> ^operator <subaction> -)
}