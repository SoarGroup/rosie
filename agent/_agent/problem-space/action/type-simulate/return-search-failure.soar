sp {action*simulate*propose*return-search-failure*search-for-next-action
   (state <s> ^problem-space <ps>
              ^superstate.name search-for-next-action
              ^action-search-failed true)
   (<ps> ^name action
         ^action.type simulate)
-->
   (<s> ^operator <o> + >)
   (<o> ^name return-search-failure)
}

sp {action*simulate*apply*return-search-failure*search-for-next-action
   (state <s> ^problem-space <ps>
              ^operator.name return-search-failure
              ^superstate <ss>)
   (<ps> ^name action
         ^action.type simulate)
   (<ss> ^name search-for-next-action)
-->
   (<ss> ^action-search-failed true)
}