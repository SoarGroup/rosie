sp {action*simulate*search-for-next-action*propose*return-subaction-structure
   (state <s> ^problem-space <ps>
              ^superstate.name search-for-next-action
              ^subaction-structure <sub>
              ^successful-operator <sub-op>)
   (<ps> ^name action
         ^action.type simulate)
-->
   (<s> ^operator <o> + >)
   (<o> ^name return-subaction-structure
        ^subaction-structure <sub>)

   (<s> ^operator <sub-op> -)
}

sp {action*simulate*search-for-next-action*apply*return-subaction-structure
   (state <s> ^problem-space <ps>
              ^operator <o>
              ^superstate <ss>)
   (<ps> ^name action
         ^action.type simulate)
   (<o> ^name return-subaction-structure
        ^subaction-structure <sub>)
-->
   (<ss> ^subaction-structure <sub>)
}