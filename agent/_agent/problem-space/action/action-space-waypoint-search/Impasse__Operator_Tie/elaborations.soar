sp {action-space*waypoint-search*operator-tie*elaborate*name*selection
   (state <s> ^superstate.problem-space.action-space waypoint-search
              ^impasse tie
              ^attribute operator)
-->
   (<s> ^name selection)
}

sp {action-space*waypoint-search*selection*copy*current-waypoint*superstate
   (state <s> ^name selection
              ^problem-space.action-space waypoint-search
              ^superstate.current-waypoint <wp>)
-->
   (<s> ^current-waypoint <wp>)
}

sp {action-space*waypoint-search*selection*copy*desired*superstate
   (state <s> ^name selection
              ^problem-space.action-space waypoint-search
              ^superstate.desired <des>)
-->
   (<s> ^desired <des>)
}