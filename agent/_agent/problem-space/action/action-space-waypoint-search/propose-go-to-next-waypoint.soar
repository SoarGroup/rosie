sp {action-space*waypoint-search*propose*go-to-next-waypoint
   (state <s> ^problem-space.action-space waypoint-search
              -^name selection
              ^current-waypoint.next <next>)
-->
   (<s> ^operator <o> +)
   (<o> ^name go-to-next-waypoint
        ^next-waypoint <next>)
}