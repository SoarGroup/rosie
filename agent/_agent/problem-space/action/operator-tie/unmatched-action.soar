sp {unmatched-action*propose*report-unmatched-action
   (state <s> ^name unmatched-action)
-->
   (<s> ^operator <o> + >)
   (<o> ^name report-unmatched-action)
}

sp {unmatched-action*apply*report-unmatched-action*report-failure
   (state <s> ^name unmatched-action
              ^operator.name report-unmatched-action
              ^top-state.interaction.status <status>)
-->
   (<status> ^failure <f>)
   (<f> ^type no-proposed-action)
}
