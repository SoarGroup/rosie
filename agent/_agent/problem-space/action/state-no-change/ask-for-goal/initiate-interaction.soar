sp {ask-for-goal*propose*initiate-interaction*ask-for-goal
   (state <s> ^name ask-for-goal
              ^superstate.action-operator <aop>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name initiate-interaction
        ^type ask-for-goal
        ^action-operator <aop>)
}
