sp {action*problem-space*execute*propose*store-subaction-link
   (state <s> ^problem-space.name action
              ^problem-space.action.type execute
              ^subaction-lti <lti>
              ^action-id.procedural <proc>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name store-subaction-link
        ^subaction <lti>)
}

sp {action*problem-space*execute*apply*store-subaction-link
   (state <s> ^problem-space.name action
              ^problem-space.action.type execute
              ^operator <o>
              ^action-id.procedural <proc>
              ^subaction-lti <lti>
              ^smem.command <cmd>)
   (<o> ^name store-subaction-link
        ^subaction <lti>)
-->
   (<proc> ^subaction <lti>)
   (<s> ^subaction-lti <lti> -)
   (<s> ^store-concept <proc>)
}