sp {op_put-down1*internal*propose*report-success
   (state <s> ^name op_put-down1
              ^problem-space.action.type execute
              ^held-object.predicates.arm-status not-grabbed)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}

sp {op_put-down1*internal*apply*report-success
   (state <s> ^name op_put-down1
              ^operator.name report-success
              ^current-action <a>)
-->
   (<a> ^status complete)
}

