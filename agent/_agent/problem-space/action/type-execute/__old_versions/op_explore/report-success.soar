sp {execute-explore*propose*report-success*until-clause
   (state <s> ^name op_explore1
              ^problem-space.action.type execute
             -^child-action
              ^until-clause.satisfied true)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}

sp {execute-explore*propose*report-success
   (state <s> ^name op_explore1
             -^until-clause
              ^unvisited-waypoints false)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}