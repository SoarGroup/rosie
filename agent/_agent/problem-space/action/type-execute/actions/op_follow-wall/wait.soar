sp {op_follow-wall1*propose*wait*during*command*execution
   (state <s> ^name op_follow-wall1
              -^operator.name wait
              ^current-action.{ << follow-wall-command stop-command >> } <cmd>)
   (<cmd> ^status << sent executing >>)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
              
