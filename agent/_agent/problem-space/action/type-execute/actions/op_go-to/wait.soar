sp {op_go-to1*propose*wait*during*commands
   (state <s> ^name op_go-to1
              ^problem-space.action.type execute
              -^operator.name wait
              ^current-action.{ << drive-command drive-forward-command >> } <cmd>)
   (<cmd> ^status << sent executing >>)
-->
   (<s> ^operator <o> +)
   (<o> ^name wait)
}
