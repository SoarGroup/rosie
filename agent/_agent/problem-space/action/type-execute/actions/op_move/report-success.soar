sp {op_move1*apply*op_put-down1*elaborate*status*complete
   (state <s> ^name op_move1
              ^operator.name op_put-down1
              ^child-action.status complete
              ^current-action <a>)
-->
   (<a> ^status complete)
}