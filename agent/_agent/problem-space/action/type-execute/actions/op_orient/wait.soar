sp {op_orient1*propose*wait*during*command
   (state <s> ^name op_orient1
              -^operator.name wait
              ^current-action.orient-command <cmd>)
   (<cmd> -^status)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
              