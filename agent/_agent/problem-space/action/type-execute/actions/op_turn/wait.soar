sp {op_turn1*propose*wait*during*command
   (state <s> ^name op_turn1
              -^operator.name wait
              ^current-action.turn-command <cmd>)
   (<cmd> -^status)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
              