sp {op_pick-up1*tabletop*propose*wait*during*commands
   (state <s> ^name op_pick-up1
              -^operator.name wait
              ^current-action.{ << pick-up-command reset-command >> } <cmd>)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}