sp {op_go-to-next-waypoint1*propose*wait*during*commands
   (state <s> ^name op_go-to-next-waypoint1
              -^operator.name wait
              ^current-action.{ << face-command drive-command >> } <cmd>)
   (<cmd> ^status << sent executing >>)
-->
   (<s> ^operator <o> +)
   (<o> ^name wait)
}
