sp {op_stop1*propose*wait*during*command
   (state <s> ^name op_stop1
              -^operator.name wait
              ^current-action.stop-command <cmd>)
   (<cmd> ^status << sent executing >> )
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
              
