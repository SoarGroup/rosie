#sp {op_go-to-waypoint1*operator-tie*elaborate*name*selection
#   (state <s> ^impasse tie
#              ^superstate.name op_go-to-waypoint1)
#-->
#   (<s> ^name selection
#        ^default-problem-space no
#        ^problem-space <ps>)
#   (<ps> ^name waypoint-search
#	     ^default-state-copy no
#         ^world.copy-type shallow)
#}

sp {waypoint-search*selection*elaborate*problem-space
   (state <s> ^impasse tie
              ^superstate.name op_go-to-waypoint1)
-->
   (<s> ^name selection
        ^problem-space <ps>)
   (<ps> ^name waypoint-search
         ^default-state-copy no
         ^world.copy-type shallow)
}
