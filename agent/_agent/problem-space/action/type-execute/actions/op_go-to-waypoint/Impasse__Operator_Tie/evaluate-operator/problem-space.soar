sp {waypoint-search*evaluate-operator*elaborate*problem-space*waypoint-search
   (state <s> ^name evaluate-operator
              ^superstate.problem-space.name waypoint-search)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^name waypoint-search
         ^default-state-copy no
         ^world.copy-type shallow)
}
