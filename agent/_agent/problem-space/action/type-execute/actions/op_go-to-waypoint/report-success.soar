sp {op_go-to-waypoint1*propose*report-success*at*desired*waypoint
   (state <s> ^name op_go-to-waypoint1
              ^problem-space.action.type execute
              -^child-action
               ^current-waypoint <wp>
               ^desired.waypoint <wp>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name report-success)
}
