sp {action*state*no-change*propose*learn-task-subaction
   (state <s> ^problem-space.name action
              ^action-matched false
              ^child-action <child>
              ^action-operator <action>
              ^action-id <action-id>)
   (<child> -^validated true
             ^execution-operator <subaction>)
   (<subaction> ^learn-proposal true)
-->
  (<s> ^operator <o> + >)
   (<o> ^name learn-task-subaction
        ^subaction <subaction>
        ^action-operator <action>
        ^action-id <action-id>)
}
