sp {action*search*prefer*smem*over*others
   (state <s> ^problem-space <ps>
              ^operator <o1> +
              ^operator <o2> +)
   (<ps> ^name action
         ^action.type search)
   (<o1> ^name smem-query)
   (<o2> ^name <> smem-query)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {action*search*prefer*return*subaction*over*proposal
   (state <s> ^problem-space <ps>
              ^operator <o1> +
              ^operator <o2> +)
   (<ps> ^name action
         ^action.type search)
   (<o1> ^name return-subaction-structure)
   (<o2> ^action-handle <h>)
-->
   (<s> ^operator <o1> > <o2>)
}