sp {action*search*propose*return-subaction-structure
   (state <s> ^problem-space <ps>
              ^subaction-structure <sub>)
   (<ps> ^name action
         ^action.type search)
-->
   (<s> ^operator <o> + >)
   (<o> ^name return-subaction-structure
        ^subaction-structure <sub>)
}

sp {action*search*apply*return-subaction-structure
   (state <s> ^problem-space <ps>
              ^operator <o>
              ^superstate <ss>)
   (<ps> ^name action
         ^action.type search)
   (<o> ^name return-subaction-structure
        ^subaction-structure <sub>)
-->
   (<ss> ^subaction-structure <sub>)
}