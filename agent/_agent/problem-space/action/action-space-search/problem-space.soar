sp {action-space*search*elaborate*problem-space
   (state <s> ^superstate.problem-space.action-space search
             -^default-problem-space no)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^name action
         ^action-space search
         ^default-state-copy no
         ^action <action>
         ^learning <learn>
         ^world <world>
         ^perception <perc>)
   (<action> ^type simulate)
   (<perc> ^attend-to-all-perception no)
   (<learn> ^type exploration)
}

sp {action-space*search*elaborate*problem-space*top*search
   (state <s> ^problem-space <ps>
              ^action-operator <o>
              ^superstate.operator <o>)
   (<ps> ^action-space search
         ^action <action>
         ^world <world>)
-->
   (<action> ^propose-action-operators true)
   (<world> ^copy-type shallow)
}