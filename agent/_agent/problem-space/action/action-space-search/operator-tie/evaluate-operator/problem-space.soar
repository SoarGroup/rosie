sp {action-space*search*selection*evaluate-operator*elaborate*problem-space
   (state <s> ^name evaluate-operator
              ^problem-space <ps>)
   (<ps> ^action-space search
         ^action <action>)
-->
   (<action> ^propose-action-operators true)
}
