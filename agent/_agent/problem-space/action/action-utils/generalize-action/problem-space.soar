sp {generalize-action*elaborate*problem-space
   (state <s> ^name generalize-action
             -^name learn-goal-definition)
-->
   (<s> ^default-problem-space no
        ^problem-space <ps>)
#   (<ps> ^learning.use chunking)
}