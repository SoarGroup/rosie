sp {action*execute*SNC*propose*ask-for-goal
   (state <s> ^name action-execute-SNC
             -^action-id.goal
             -^desired)
-->
   (<s> ^operator <o> +)
   (<o> ^name ask-for-goal)
}
              