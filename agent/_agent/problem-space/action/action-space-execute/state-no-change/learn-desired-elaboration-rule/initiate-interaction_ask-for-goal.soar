sp {learn-desired-elaboration-rule*propose*initiate-interaction*ask-for-goal
   (state <s> ^name learn-desired-elaboration-rule
              ^action-id <aid>
              ^action-operator <aop>)
   (<aid> -^goal)
-->
   (<s> ^operator <o> + >)
   (<o> ^name initiate-interaction
        ^type ask-for-goal
        ^action-operator <aop>)
}
