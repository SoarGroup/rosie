# Elaborate the problem-space when the superstate in an execute action problem-space
sp {action-space*execute*elaborate*problem-space
   (state <s> ^superstate <ss>
             -^default-problem-space no)
   (<ss> ^problem-space <ss-ps>
         ^operator <ss-op>)
   (<ss-ps> ^name action
            ^action-space execute)
   (<ss-op> ^action-handle <h>)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^name action
         ^action-space execute
         ^action <action>
         ^learning <learn>
         ^world <world>
         ^perception <perc>)
   (<action> ^type execute)
   (<world> ^copy-type shallow)
}
            
## For a composite action, propose action operators if there is a desired structure (goal) 
##   with at least 1 predicate
#sp {action*problem-space*execute*composite*elaborate*problem-space*propose-action-operators
#   (state <s> ^problem-space <ps>
#              ^desired <des>
#             -^superstate.operator.name op_find1
#             -^child-action
#             -^default-problem-space no)
#   (<ps> ^name action
#         ^action <a>)
#   (<a> ^type execute)
#-->
#   (<a> ^propose-action-operators true)
#}
