sp {action*execute*propose*refresh
   (state <s> ^problem-space.name action
              ^problem-space.action.type execute
              ^refresh true)
-->
   (<s> ^operator <o> + >)
   (<o> ^name refresh)
   }

sp {action*execute*apply*refresh
   (state <s> ^problem-space.name action
              ^problem-space.action.type execute
              ^operator.name refresh)
-->
   (<s> ^refresh true -)
}