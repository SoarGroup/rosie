sp {action-space*retrospective*selection*prefer*evaluate-operator*matches*subaction*1*arg
   (state <s> ^name selection
              ^problem-space.action-space retrospective
              ^operator <o> +
              ^subactions.next <sub>)
   (<o> ^name evaluate-operator
        ^superoperator <action>)
   (<sub> ^action-handle <handle>
          ^arg1 <sub-arg1>
          -^arg2)
   (<sub-arg1> ^arg-type object
               ^id <obj>)
   (<action> ^action-handle <handle>
             ^arg1 <arg1>
             -^arg2)
   (<arg1> ^arg-type object
           ^id <obj>)
-->
   (<s> ^operator <o> >)
}

sp {action-space*retrospective*selection*prefer*evaluate-operator*matches*subaction*arg2
   (state <s> ^name selection
              ^problem-space.action-space retrospective
              ^operator <o> +
              ^subactions.next <sub>)
   (<o> ^name evaluate-operator
        ^superoperator <action>)
   (<sub> ^action-handle <handle>
         -^arg1
          ^arg2 <sub-arg2>)
   (<sub-arg2> ^arg-type predicate
               ^handle <pred-handle>
               ^2 <obj2>)
   (<action> ^action-handle <handle>
            -^arg1
             ^arg2 <arg2>)
   (<arg2> ^arg-type predicate
           ^handle <pred-handle>
           ^2 <obj2>)
-->
   (<s> ^operator <o> >)
}

