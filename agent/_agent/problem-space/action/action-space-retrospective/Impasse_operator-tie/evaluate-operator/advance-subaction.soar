# When the look-ahead-operator is applied, advance the subaction
sp {action-space*retrospective*evaluate-operator*apply*look-ahead-operator*advance*subaction
   (state <s> ^name evaluate-operator
              ^problem-space.action-space retrospective
              ^look-ahead-operator <o>
              ^operator <o>
              ^subactions <subs>)
   (<subs> ^next <cur>)
   (<cur> ^next <next>)
-->
   (<subs> ^next <cur> -
           ^next <next>)
}
