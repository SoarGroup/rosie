sp {create-condition*apply*create-return-type
   (state <s> ^name create-condition
   		  	  ^return-type <rt>
			  ^operator <o>)
   (<o> ^name create-return-type
   		^condition <c>)
-->
   (<c> ^return-type <rt>)
}