#impasse operator no-change

#if there is an impasse with an operator no-change, then the goal for 
#retrieving a relevant model should be that operator

#the agent is asking "I don't know how to activate the light switch, 
#is there a relevant model I can retrieve?"

sp {elaborate*moveGoalOpNoChange
(state <s1> ^type state
		^superstate <s>
		^impasse no-change
		^attribute operator)
(<s> ^operator <op> +)
(<op> ^name <tskname>
	  ^arg1 <arg1>
	  ^predicate <pred>)
-->
(<s1> ^goal <gl>)
(<gl> ^task <tsk>
	^type operator)
(<tsk> ^name <tskname>
	   ^finished false
	   ^operator <x>)
(<x> ^arg1 <arg1>
	 ^predicate <pred>)
}

sp {elaborate*moveModel
(state <s1> ^type state
		^superstate <s>
		^impasse no-change)
-->
(<s1> ^model <md>)
(<md> ^preconditions false
	  ^cause false)
}

sp {elaborate*copyWorld
(state <s1> ^type state
		^superstate <s>)
(<s> ^world <world>)
-->
(<s1> ^world <world>)
}

