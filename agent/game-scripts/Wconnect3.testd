the name of the game is connect-x-3
load init-connect-3
ok
you can move an available clear red block onto a clear location,if a block is not on a location then it is available,if a location is not below an object then it is clear
load final-connect-3
the goal is that three of the captured locations are in a line,ok,if a location is below a red block then it is captured,if the locations are linear then they are in a line
load fail-connect-3
if three of the occupied locations are in a line then you lose,ok,if a location is below a blue block then it is occupied,if the locations are linear then they are in a line
done
no