
#sp {op_go-to-next-waypoint*reject*select-next-goal*havent*completed*traversal
#   (state <s> ^name op_go-to-next-waypoint1
#              ^operator <o> +
#              ^desired <des>
#              ^current-task-segment <seg>)
#   (<o> ^name select-next-goal)
#   (<seg> ^position-set.next <> none)
#   (<des> -^item-type start-goal)
#-->
#   (<s> ^operator <o> -)
#}

# TODO: REMOVE (I don't think it's needed)
#sp {op_go-to-next-waypoint1*reject*attend-to-new-waypoint*if*driving
#   (state <s> ^name op_go-to-next-waypoint1
#              ^operator <o> +
#              ^current-task-segment.drive-command <cmd>)
#   (<o> ^name attend-to-perception
#        ^change.type changed-waypoint)
#   (<cmd> ^status << sent executing >>)
#-->
#   (<s> ^operator <o> -)
#}
