### Internally modeling the drive task ###

sp {problem-space*action*internal*elaborate*op_drive1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_drive1)
-->
   (<o> ^dont-learn-action-model true)
}

