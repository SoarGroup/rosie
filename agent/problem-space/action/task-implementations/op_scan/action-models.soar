### Internally modeling the scan task ###

sp {problem-space*action*internal*elaborate*op_scan1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_scan1)
-->
   (<o> ^dont-learn-action-model true)
}

