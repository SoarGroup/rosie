### Internally modeling the close task ###

# Change the door2 predicate from open2 to closed2
sp {problem-space*action*internal*apply*op_close1*change*door2
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name op_close1
         ^arg1.id.predicates <preds>)
   (<preds> ^door2 open2)
-->
   (<preds> ^door2 open2 -
            ^door2 closed2)
}
