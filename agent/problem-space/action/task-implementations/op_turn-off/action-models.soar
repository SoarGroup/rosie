### Internally modeling the turn-off task ###

# Change the activation1 predicate from on2 to off2
sp {problem-space*action*internal*apply*op_turn-off1*change*activation1
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name op_turn-off1
         ^arg1.id.predicates <preds>)
   (<preds> ^activation1 on2)
-->
   (<preds> ^activation1 on2 -
            ^activation1 off2)
}
