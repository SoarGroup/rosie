### propose op_find1 during task planning ###

# propose if not confirmed
sp {problem-space*action*propose-subtasks*propose*op_find1
   (state <s> ^problem-space <ps>
              ^world <w>)
   (<ps> ^name action
         ^propose-subtasks true)
   (<w> ^objects.object <obj>)
   (<obj> ^predicates <preds>)
   (<preds> ^is-confirmed1 not-confirmed1
            ^category << object person >>)
-->
   (<s> ^operator <op> +)
   (<op> ^name op_find1 
         ^item-type task-operator
         ^task-handle find1
         ^perform multiple
         ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}

#sp {problem-space*action*propose-subtasks*propose*op_find1*different*location
#   (state <s> ^problem-space <ps>
#              ^world <w>)
#   (<ps> ^name action
#         ^propose-subtasks true)
#   (<w> ^robot.current-location <loc>
#        ^objects.object <obj>
#        ^predicates.predicate <in-pred>)
#   (<obj> ^predicates <preds>)
#   (<preds> ^category << object person >>)
#   (<in-pred> ^handle in1)
#   -{(<in-pred> ^instance <i>)
#     (<i> ^1 <obj> ^2 <loc>)}
#-->
#   (<s> ^operator <op> +)
#   (<op> ^name op_find1 
#         ^item-type task-operator
#         ^task-handle find1
#         ^perform multiple
#         ^arg1 <arg1>)
#   (<arg1> ^arg-type object
#           ^id <obj>)
#}
