### propose op_find1 during task planning ###

# propose if not visible
sp {problem-space*action*propose-subtasks*propose*op_find1*has*door
   (state <s> ^problem-space <ps>
              ^world.objects.object <obj>)
   (<ps> ^name action
         ^propose-subtasks true)
   (<obj> ^predicates <preds>)
   (<preds> ^is-visible1 not-visible1
            ^category object)
-->
   (<s> ^operator <op> +)
   (<op> ^name op_find1 
         ^item-type task-operator
         ^task-handle find1
         ^perform multiple
         ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}

