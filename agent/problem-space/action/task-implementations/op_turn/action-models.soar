### Internally modeling the turn task ###

sp {problem-space*action*internal*elaborate*op_turn1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_turn1)
-->
   (<o> ^dont-learn-action-model true)
}

