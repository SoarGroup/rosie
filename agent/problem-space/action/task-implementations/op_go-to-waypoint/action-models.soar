### Internally modeling the go-to1 task ###

sp {problem-space*action*internal*elaborate*op_go-to-waypoint1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_go-to-waypoint1)
-->
   (<o> ^dont-learn-action-model true)
}

