### Internally modeling the press task ###

sp {problem-space*action*internal*elaborate*op_press1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_press1)
-->
   (<o> ^dont-learn-action-model true)
}

