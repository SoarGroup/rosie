### Internally modeling the start-leading task ###

# Add the predicate is-following1=following1

sp {problem-space*action*internal*apply*op_start-leading1*add*is-following1
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name op_start-leading1
         ^arg1.id.predicates <preds>)
   (<preds> -^is-following1)
-->
   (<preds> ^is-following1 following1)
}

sp {problem-space*action*internal*apply*op_start-leading1*change*is-following1
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name op_start-leading1
         ^arg1.id.predicates <preds>)
   (<preds> ^is-following1 not-following1)
-->
   (<preds> ^is-following1 not-following1 -
            ^is-following1 following1)
}

