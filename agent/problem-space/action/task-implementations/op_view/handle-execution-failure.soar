
# If the face command was unsuccessful, return a failure
sp {op_view*propose*handle-failure*unsatisfied-goal
   (state <s> ^name op_view1
              ^problem-space.subproblem execute
             -^child-task-segment
              ^target-object.predicates.is-confirmed1 not-confirmed1)
-->
   (<s> ^operator <o> +)
   (<o> ^name handle-execution-failure
        ^type lost-object)
}


