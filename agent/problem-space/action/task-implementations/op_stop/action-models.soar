### Internally modeling the stop task ###

sp {problem-space*action*internal*elaborate*op_stop1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_stop1)
-->
   (<o> ^dont-learn-action-model true)
}

