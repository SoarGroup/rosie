### Internally modeling the label task ###

sp {problem-space*action*internal*elaborate*op_label1*known-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_label1)
-->
   (<o> ^known-action-model true)
}

### Action Models don't work here
## Copy predicates from arg2 to arg1
#sp {problem-space*action*internal*apply*op_label1*copy*predicate
#   (state <s> ^problem-space.name action
#              ^problem-space.execution-type internal
#              ^operator <o>)
#   (<o> ^name op_label1
#        ^task-handle label1
#        ^arg1.id.predicates <p1>
#        ^arg2.id.predicates <p2>)
#   (<p2> ^{ <prop> << modifier1 category1 sentence >> } <pred>)
#   (<p1> -^<prop> <pred>)
#-->
#   (<p1> ^<prop> <pred>)
#}
#
## Mark arg2 object as merged-into arg1 object
#sp {problem-space*action*internal*apply*op_label1*mark*arg2*object*marged-into
#   (state <s> ^problem-space.name action
#              ^problem-space.execution-type internal
#              ^operator <o>)
#   (<o> ^name op_label1
#        ^task-handle label1
#        ^arg1.id <arg1-obj>
#        ^arg2.id <arg2-obj>)
#-->
#   (<arg2-obj> ^merged-into <arg1-obj>)
#}
#
## Delete arg2 from the world
#sp {problem-space*action*internal*apply*op_label1*remove*arg2*object
#   (state <s> ^problem-space.name action
#              ^problem-space.execution-type internal
#              ^operator <o>
#              ^world.objects <objs>)
#   (<o> ^name op_label1
#        ^task-handle label1
#        ^arg2.id <obj>)
#   (<objs> ^object <obj>)
#-->
#   (<objs> ^object <obj> -)
#}
#
## Change any predicates from arg2 object to arg1 object
#sp {problem-space*action*internal*apply*op_label1*change*predicate*instances
#   (state <s> ^problem-space.name action
#              ^problem-space.execution-type internal
#              ^operator <o>
#              ^world.predicates.predicate.instance <i>)
#   (<o> ^name op_label1
#        ^task-handle label1
#        ^arg1.id <arg1-obj>
#        ^arg2.id <arg2-obj>)
#   (<i> ^<num> <arg2-obj>)
#-->
#   (<i> ^<num> <arg2-obj> -
#        ^<num> <arg1-obj>)
#}
