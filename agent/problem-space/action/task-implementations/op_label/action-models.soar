### Internally modeling the label task ###

sp {problem-space*action*internal*elaborate*op_label1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_label1)
-->
   (<o> ^dont-learn-action-model true)
}


