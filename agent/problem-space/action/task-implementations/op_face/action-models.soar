### Internally modeling the face task ###

sp {problem-space*action*internal*elaborate*op_face1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_face1)
-->
   (<o> ^dont-learn-action-model true)
}

