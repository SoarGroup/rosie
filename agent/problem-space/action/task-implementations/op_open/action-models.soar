### Internally modeling the open task ###

# Change the door2 predicate from closed2 to open2
sp {problem-space*action*internal*apply*op_open1*change*door2
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name op_open1
         ^arg1.id.predicates <preds>)
   (<preds> ^door2 closed2)
-->
   (<preds> ^door2 closed2 -
            ^door2 open2)
}
