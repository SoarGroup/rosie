### Internally modeling the stop-leading task ###

# Set the predicate is-following1=not-following1
sp {problem-space*action*internal*apply*op_stop-leading1*change*is-following1
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name op_stop-leading1
         ^arg1.id.predicates <preds>)
   (<preds> ^is-following1 following1)
-->
   (<preds> ^is-following1 following1 -
            ^is-following1 not-following1)
}

