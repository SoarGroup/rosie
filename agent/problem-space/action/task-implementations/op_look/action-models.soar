### Internally modeling the look task ###

sp {problem-space*action*internal*elaborate*op_look1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_look1)
-->
   (<o> ^dont-learn-action-model true)
}

