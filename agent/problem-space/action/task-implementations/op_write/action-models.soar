
sp {action*apply*write*do*action-model
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name op_write1
         ^arg1.handle <val>
         ^arg2.{2}.predicates <preds>)
-->
   (<preds> ^value <val>)
}
