### Internally modeling the write task ###

sp {problem-space*action*internal*elaborate*op_write1*known-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_write1)
-->
   (<o> ^known-action-model true)
}

sp {problem-space*action*internal*apply*op_write1*add*value
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name << op_write1 send-write-command >>
         ^arg1.handle <val>
         ^arg2.{2}.predicates <preds>)
-->
   (<preds> ^value <val>)
}
