### Internally modeling the lift task ###

sp {problem-space*action*internal*elaborate*op_lift1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_lift1)
-->
   (<o> ^dont-learn-action-model true)
}

