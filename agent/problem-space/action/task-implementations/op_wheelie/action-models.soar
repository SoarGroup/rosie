### Internally modeling the wheelie task ###

sp {problem-space*action*internal*elaborate*op_wheelie1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_wheelie1)
-->
   (<o> ^dont-learn-action-model true)
}

