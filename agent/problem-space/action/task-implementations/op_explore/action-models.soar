### Internally modeling the explore task ###

sp {problem-space*action*internal*elaborate*op_explore1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_explore1)
-->
   (<o> ^dont-learn-action-model true)
}

