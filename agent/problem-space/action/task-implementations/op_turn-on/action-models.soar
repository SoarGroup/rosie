### Internally modeling the turn-on task ###

# Change the predicate from off2 to on2
sp {problem-space*action*internal*apply*op_turn-on1*add*on2*predicate
   (state <s> ^problem-space.name action
              ^problem-space.execution-type internal
              ^operator <op>)
   (<op> ^name << op_turn-on1 send-turn-on-command >>
         ^arg1.id.predicates <preds>)
   (<preds> ^<prop> off2)
-->
   (<preds> ^<prop> off2 -
            ^<prop> on2)
}

