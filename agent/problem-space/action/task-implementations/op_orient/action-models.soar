### Internally modeling the orient task ###

sp {problem-space*action*internal*elaborate*op_orient1*dont-learn-action-model
   (state <s> ^problem-space.execution-type internal
              ^operator <o> +)
   (<o> ^name op_orient1)
-->
   (<o> ^dont-learn-action-model true)
}

