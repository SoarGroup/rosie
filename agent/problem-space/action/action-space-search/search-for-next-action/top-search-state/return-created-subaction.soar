sp {search-for-next-action*top-search-state*propose*return-created-subaction
   (state <s> ^problem-space.action-space search
              ^top-search-state true
              ^created-subaction <sub-h>
              ^successful-operator <sub-op>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name return-created-subaction
        ^created-subaction <sub-h>)
}

sp {search-for-next-action*top-search-state*apply*return-created-subaction*copy*created-subaction*superstate
   (state <s> ^problem-space.action-space search
              ^top-search-state true
              ^operator <o>
              ^superstate <ss>)
   (<o> ^name return-created-subaction
        ^created-subaction <sub-h>)
-->
   (<ss> ^created-subaction <sub-h>)
}
