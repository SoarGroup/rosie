sp {search-for-next-action*prefer*result*operators*over*action-operators
   (state <s> ^name search-for-next-action
              ^operator <o1> +
              ^operator <o2> +
              ^action-operator <o2>)
   (<o1> ^name << return-created-subaction initiate-interaction >>)
-->
   (<s> ^operator <o1> > <o2>)
}
