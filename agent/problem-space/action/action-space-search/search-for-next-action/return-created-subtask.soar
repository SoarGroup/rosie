sp {search-for-next-action*propose*return-created-subtask
   (state <s> ^name search-for-next-action
              ^created-subtask <sub-h>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name return-created-subtask
        ^created-subtask <sub-h>)
}

sp {search-for-next-action*apply*return-created-subtask*copy*2*levels
   (state <s> ^name search-for-next-action
              ^operator <o>
              ^quiescence t
              ^superstate <ss>)
   (<o> ^name return-created-subtask
        ^created-subtask <sub-h>)
   (<ss> ^superstate <sss>)
-->
   (<ss> ^created-subtask <sub-h>)
   (<sss> ^created-subtask <sub-h>)
}
