sp {action-space*search*top-search-state*propose*return-search-failure
   (state <s> ^problem-space.action-space search
              ^top-search-state true
              ^action-search-failed true)
-->
   (<s> ^operator <o> + >)
   (<o> ^name return-search-failure)
}

sp {action-space*search*apply*return-search-failure*report*action-search-failed*superstate
   (state <s> ^problem-space.action-space search
              ^operator.name return-search-failure
              ^superstate <ss>)
   (<ps> ^name action
         ^action-space search)
-->
   (<ss> ^action-search-failed true)
}