sp {learn-task-action-model*elaborate*problem-space
   (state <s> ^name learn-task-action-model
              ^problem-space.world <w>)
-->
   (<w> ^copy-type shallow)
}

sp {learn-task-action-model*elaborate*learning*use*chunking
   (state <s> ^name learn-task-action-model
              ^problem-space.learning <l>)
-->
   (<l> ^use chunking)
}
