### @REFACTOR
sp {problem-space*action*preferences*reject*all*operators*if*unleared*desired
  (state <s> ^problem-space.subproblem execute
             ^task-operator <task-op>
             ^superstate.operator <task-op>
            -^desired
            ^operator <o> +)
   (<task-op> ^current-subgoal)
-->
  (<s> ^operator <o> -)
}

sp {action*task-operator*reject*perform*once*subtask*if*performed
   (state <s> ^problem-space.name action
              ^operator <o> +
              ^task-operator.performed <sub-handle>)
   (<o> ^subtask-handle <sub-handle>
        ^perform once)
-->
   (<s> ^operator <o> -)
}

