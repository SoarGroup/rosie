sp {op_go*propose*report-success
   (state <s> ^name op_go1
              ^current-action.go-command.status success)
-->
   (<s> ^operator <o> + >)
   (<o> ^name report-success)
}

sp {op_go*apply*report-success*add*status
   (state <s> ^name op_go1
              ^operator.name report-success
              ^current-action <a>)
-->
   (<a> ^status complete)
}

sp {op_go*apply*report-success*remove*command
   (state <s> ^name op_go1
              ^operator.name report-success
              ^current-action <a>
              ^io.output-link <out>)
   (<a> ^go-command <cmd>)
   (<out> ^<cmd-name> <cmd>)
-->
   (<a> ^go-command <cmd> -)
   (<out> ^<cmd-name> <cmd> -)
}
              
