sp {subproblem*search*evaluate-operator*turn*on*chunking
   (state <s> ^name evaluate-operator
              ^problem-space <ps>)
   (<ps> ^name action
         ^subproblem search
         ^learning <l>)
-->
   (<l> ^use chunking)
}
