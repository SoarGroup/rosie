sp {subproblem*search*apply*achieved-max-depth*report*subtask-search-failed*superstate
   (state <s> ^name selection
              ^problem-space <ps>
              ^operator.name achieved-max-depth
              ^quiescence t
              ^superstate <ss>)
   (<ps> ^name action
         ^subproblem search)
-->
   (<ss> ^subtask-search-failed true)
}

