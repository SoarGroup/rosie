#sp {subproblem*search*selection*apply*init-current-evaluation-depth*max
#   (state <s> ^name selection
#              ^problem-space.subproblem search
#              ^top-selection true
#              ^superstate.desired.pred-count <any>
#              ^operator <o>
#              ^agent-params.subtask-search-depth <d>)
#   (<o> ^name init-current-evaluation-depth)
#-->
#   # Change to this set the iterative-deepening max search depth
#   (<s> ^max-evaluation-depth <d>)
#}

sp {subproblem*search*selection*apply*init-current-evaluation-depth*level*1
   (state <s> ^name selection
              ^problem-space <ps>
              ^top-selection true
              ^operator.name init-current-evaluation-depth
              ^superstate.desired.pred-count <any>
              ^agent-params.subtask-search-depth-task-objs-only <n>)
   (<ps> ^subproblem search
         ^search-level 1)
-->
   # Change to this set the iterative-deepening max search depth
   (<s> ^max-evaluation-depth <n>)
}

sp {subproblem*search*selection*apply*init-current-evaluation-depth*level*2
   (state <s> ^name selection
              ^problem-space <ps>
              ^top-selection true
              ^operator.name init-current-evaluation-depth
              ^superstate.desired.pred-count <any>
              ^agent-params.subtask-search-depth-task-objs-partial <n>)
   (<ps> ^subproblem search
         ^search-level 2)
-->
   # Change to this set the iterative-deepening max search depth
   (<s> ^max-evaluation-depth <n>)
}

sp {subproblem*search*selection*apply*init-current-evaluation-depth*level*3
   (state <s> ^name selection
              ^problem-space <ps>
              ^top-selection true
              ^operator.name init-current-evaluation-depth
              ^superstate.desired.pred-count <any>
              ^agent-params.subtask-search-depth-all-ops <n>)
   (<ps> ^subproblem search
         ^search-level 3)
-->
   # Change to this set the iterative-deepening max search depth
   (<s> ^max-evaluation-depth <n>)
}

sp {subproblem*search*selection*apply*init-current-evaluation-depth*zero*no*goal
   (state <s> ^name selection
              ^problem-space.subproblem search
              ^top-selection true
             -^superstate.desired.pred-count
              ^operator <o>)
   (<o> ^name init-current-evaluation-depth)
-->
   (<s> ^max-evaluation-depth 0)
}
