
sp {learn-task-goal*prefer*generalize-argument*over*generalize-goal
   (state <s> ^name learn-task-goal
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name generalize-task-argument)
   (<o2> ^name generalize-task-goal)
-->
   (<s> ^operator <o1> > <o2>)
}
