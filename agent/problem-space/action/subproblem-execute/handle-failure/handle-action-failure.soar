sp {subproblem*execute*propose*handle-action-failure
   (state <s> ^problem-space.subproblem execute
              ^failure <f>)
   (<f> ^type <type>)
-->
   (<s> ^operator <o> + > =)
   (<o> ^name handle-action-failure
        ^failure <f>)
}
