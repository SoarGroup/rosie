sp {subproblem*execute*propose*push-task-segment*new-task-segment*on*stack
   (state <s> ^problem-space.subproblem execute
              ^task-stack.new-task-segment <seg>
             -^child-task-segment)
-->
   (<s> ^operator <o> +)
   (<o> ^name push-task-segment
        ^segment <seg>)
}

sp {subproblem*execute*propose*push-task-segment*new-task-segment*on*state
   (state <s> ^problem-space.subproblem execute
              ^new-task-segment <seg>)
-->
   (<s> ^operator <o> +)
   (<o> ^name push-task-segment
        ^segment <seg>)
}

