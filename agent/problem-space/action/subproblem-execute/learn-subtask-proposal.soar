# @DEL: don't need this flag anymore (probably)
#sp {subproblem*execute*apply*learned-subtask-proposal*mark*learned*and*store
#   (state <s> ^problem-space.subproblem execute
#              ^operator <o>
#              ^task-concept-network.procedural.subtasks.subtask <sub-tcn>
#              ^smem.command <cmd>)
#   (<o> ^task-handle <h>
#        ^subtask-handle <sub-h>)
#   (<sub-tcn> ^handle <sub-h>
#            -^learned-proposal true)
#-->
#   (<sub-tcn> ^learned-proposal true)
#   (<cmd> ^store <sub-tcn>)
#}
          
sp {subproblem*execute*apply*subtask*remove*unlearned
   (state <s> ^problem-space.name action
              ^problem-space.subproblem execute
              ^operator.subtask-handle <sub-handle>
              ^current-task-segment <seg>)
   (<seg> ^learn-subtask-proposal <sub-handle>)
-->
   (<seg> ^learn-subtask-proposal <sub-handle> -)
}
