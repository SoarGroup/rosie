# If no task-concept-network, retrieve from semantic memory
sp {subproblem*execute*problem-space*elaborate*smem-query
   (state <s> ^problem-space.subproblem execute
              ^task-operator <task-op>
             -^task-concept-network)
   (<task-op> ^task-handle <task-handle>)
-->
   (<s> ^smem-query <query>)
   (<query> ^cue <cue>
            ^depth 8
            ^destination <s>
            ^result-name task-concept-network)
   (<cue> ^handle <task-handle>)
}

