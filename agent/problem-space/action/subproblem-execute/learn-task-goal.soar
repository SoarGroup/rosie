sp {subproblem*execute*propose*learn-task-goal
   (state <s> ^problem-space.subproblem execute
              ^current-task-segment <seg>)
   (<seg> ^new-task-goal <goal>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name learn-task-goal
        ^task-goal <goal>)
}
