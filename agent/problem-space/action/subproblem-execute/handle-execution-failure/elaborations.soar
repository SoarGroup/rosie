
sp {handle-execution-failure*elaborate*failure-type*from*superoperator
   (state <s> ^name handle-execution-failure
              ^superstate.operator.type <type>)
-->
   (<s> ^failure-type <type>)
}

sp {handle-execution-failure*elaborate*task-context*superstate
   (state <s> ^name handle-execution-failure
              ^superstate.task-operator <task-op>
              ^superstate.current-task-segment <seg>)
-->
   (<s> ^task-context <context>)
   (<context> ^task-operator <task-op>
              ^task-segment <seg>)
}

