sp {learn-subaction-policy*propose*push-procedural-subgoal*learning*failure
   (state <s> ^name learn-subaction-policy
              ^retrospective-learning-result failure
              ^subaction-context <context>
             -^procedural-learning-result)
-->
   (<s> ^operator <o> + >)
   (<o> ^name push-procedural-subgoal
        ^subaction-context <context>)
}
