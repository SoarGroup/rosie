sp {learn-subaction-policy*prefer*smem-query*over*action*operator
   (state <s> ^name learn-subaction-policy
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name smem-query)
   (<o2> ^action-handle <any>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {learn-subaction-policy*prefer*return-learning-result*over*action*operator
   (state <s> ^name learn-subaction-policy
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name return-learning-result)
   (<o2> ^action-handle <any>)
-->
   (<s> ^operator <o1> > <o2>)
}
