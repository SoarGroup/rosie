
# When the look-ahead-operator is applied, advance the subtask
sp {action-space*retrospective*evaluate-operator*propose*force-operator-tie
   (state <s> ^name evaluate-operator
              ^problem-space.action-space retrospective)
-->
   (<s> ^operator <o> +)
   (<o> ^name force-operator-tie)
}
