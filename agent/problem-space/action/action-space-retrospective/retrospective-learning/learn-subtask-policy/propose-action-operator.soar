sp {learn-subtask-policy*propose*composite-action
   (state <s> ^name learn-subtask-policy
              ^action-operator <o>)
-->
   (<s> ^operator <o> + >)
}

sp {learn-subtask-policy*elaborate*action-operator*with*created-subgoal
   (state <s> ^name learn-subtask-policy
              ^operator <o> +
              ^action-operator <o>
              ^created-procedural-subgoal <subgoal>)
-->
   (<o> ^current-subgoal <subgoal>)
}
