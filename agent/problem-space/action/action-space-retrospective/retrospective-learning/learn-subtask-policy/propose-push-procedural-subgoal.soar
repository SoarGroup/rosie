sp {learn-subtask-policy*propose*push-procedural-subgoal*learning*failure
   (state <s> ^name learn-subtask-policy
              ^retrospective-learning-result failure
              ^subtask-context <context>
             -^created-procedural-subgoal)
-->
   (<s> ^operator <o> + >)
   (<o> ^name push-procedural-subgoal
        ^subtask-context <context>)
}
