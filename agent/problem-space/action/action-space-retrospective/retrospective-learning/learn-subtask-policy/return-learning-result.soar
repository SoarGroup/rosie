sp {learn-subtask-policy*propose*return-learning-result*policy*success
   (state <s> ^name learn-subtask-policy
              ^retrospective-learning-result success)
-->
   (<s> ^operator <o> + >)
   (<o> ^name return-learning-result
        ^result success)
}

#sp {learn-subtask-policy*propose*return-learning-result*procedural*success
#   (state <s> ^name learn-subtask-policy
#              ^procedural-learning-result success)
#-->
#   (<s> ^operator <o> + > =)
#   (<o> ^name return-learning-result
#        ^result success)
#}

sp {learn-subtask-policy*apply*return-learning-result*copy*to*superstate
   (state <s> ^name learn-subtask-policy
              ^operator <o>
              ^subtask-context <sub>)
   (<o> ^name return-learning-result
        ^result <res>)
-->
   (<sub> ^learning-result <res>)
}
