sp {action-space*retrospective*action-operator*elaborate*problem-space
   (state <s> ^superstate.operator <o>
              ^superstate.action-operator <o>
              ^problem-space <ps>)
   (<ps> ^name action
         ^action-space retrospective
         ^world <world>
         ^subtasks <subs>)
-->
   (<ps> ^execution-type internal)
   (<world> ^copy-type shallow)
   (<subs> ^copy-type shallow)
}

sp {learn-subtask-policy*action-operator*force*state-no-change*if*no*goal
   (state <s> ^superstate.name learn-subtask-policy
              ^superstate.operator.task-handle <h>
             -^desired
              ^operator <o> +)
-->
   (<s> ^operator <o> -)
}
