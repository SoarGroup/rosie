sp {retrospective-learning*propose*learn-subaction-policy
   (state <s> ^name retrospective-learning
              ^subactions-stack.next { <next> <> none })
   (<next> -^learning-result)
-->
   (<s> ^operator <o> +)
   (<o> ^name learn-subaction-policy
        ^subaction <next>)
}
