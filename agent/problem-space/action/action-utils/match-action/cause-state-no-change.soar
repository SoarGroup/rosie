sp {match-action*reject*all*operators*if*unmatched
   (state <s> ^match-action <ma>
              ^operator <o> +)
   (<o> -^always-propose true)
   (<ma> -^matches-action <a>)
-->
   (<s> ^operator <o> -)
   (<s> ^learning-chunking-rule true)
}
   
