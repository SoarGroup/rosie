sp {generalize-task-operator*generalize-task-argument*prefer*procedural*over*default
  (state <s> ^name generalize-task-operator
             ^generalize-task-argument <arg>
             ^operator <o1> +
             ^operator <o2> +)
  (<o1> ^name generalize-task-argument
        ^argument <arg>
        ^match-type procedural)
  (<o2> ^name generalize-task-argument
        ^argument <arg>
        ^match-type default)
-->
  (<s> ^operator <o1> > <o2>)
}

sp {generalize-task-operator*prefer*generalize-task-argument*over*complete-generalize-task-operator
  (state <s> ^name generalize-task-operator
             ^operator <o1> +
             ^operator <o2> +)
  (<o1> ^name generalize-task-argument)
  (<o2> ^name complete-generalize-task-operator)
-->
  (<s> ^operator <o1> > <o2>)
}

