sp {generalize-task-operator*elaborate*problem-space
   (state <s> ^name generalize-task-operator
             -^name learn-goal-definition)
-->
   (<s> ^default-problem-space no
        ^problem-space <ps>)
#   (<ps> ^learning.use chunking)
}