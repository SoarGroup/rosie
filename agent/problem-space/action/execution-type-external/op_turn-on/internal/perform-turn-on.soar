sp {op_turn-on1*internal*propose*perform-turn-on
   (state <s> ^name op_turn-on1
              ^agent-params.domain internal
              ^problem-space.execution-type external
              ^target-object.predicates.activation1 off2)
-->
   (<s> ^operator <o> +)
   (<o> ^name perform-turn-on)
}

sp {op_turn-on1*internal*apply*perform-turn-on
   (state <s> ^name op_turn-on1
              ^agent-params.domain internal
              ^problem-space.execution-type external
              ^operator.name perform-turn-on
              ^object-info.input-link-obj.property <il-prop>)
   (<il-prop> ^property-handle activation1
              ^values <vals>)
-->
   (<vals> ^off2 1.00 -
           ^on2 1.00)
}

sp {op_turn-on1*internal*apply*perform-turn-on*microwave*time
   (state <s> ^name op_turn-on1
              ^agent-params.domain internal
              ^problem-space.execution-type external
              ^operator.name perform-turn-on
              ^target-object.predicates.name microwave1
              ^io.input-link.time.total-secs <secs>
              ^object-info.input-link-obj.property <il-prop>)
   (<il-prop> ^property-handle activation1)
-->
   (<il-prop> ^turn-off-time (+ <secs> 2000))
}

sp {op_turn-on1*internal*prefer*attend-to-scene-change*over*perform-turn-on
   (state <s> ^name op_turn-on1
              ^agent-params.domain internal
              ^problem-space.execution-type external
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << push-task-segment pop-task-segment attend-to-scene-change >>)
   (<o2> ^name perform-turn-on)
-->
   (<s> ^operator <o1> > <o2>)
}
