sp {op_turn-on1*apply*pop-task-segment*failure*mark*subtask-failure
   (state <s> ^name op_turn-on1
              ^operator <o>)
   (<o> ^name pop-task-segment
        ^segment <seg>)
   (<seg> ^status failure
          ^task-operator <task-op>)
   (<task-op> ^name << op_find1 op_approach1 primitive-turn-on >>
              ^task-handle <h>)
-->
   (<s> ^subtask-failure <h>)
}

sp {op_turn-on1*propose*handle-execution-failure
   (state <s> ^name op_turn-on1
              ^subtask-failure <h>
              ^current-task-segment <seg>)
-->
   (<s> ^operator <o> + = >)
   (<o> ^name handle-execution-failure
        ^category failure-handling
        ^task-segment <seg>
        ^subtask-handle <h>)
}
