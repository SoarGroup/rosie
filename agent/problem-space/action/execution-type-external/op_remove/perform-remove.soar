sp {op_remove1*internal*propose*perform-remove
   (state <s> ^name op_remove1
              ^problem-space.execution-type external
              ^target-object <obj>
              ^top-state.world.objects.object <obj>)
-->
   (<s> ^operator <o> +)
   (<o> ^name perform-remove)
}

sp {op_remove1*internal*apply*perform-remove*change*holding-object
   (state <s> ^name op_remove1
              ^problem-space.execution-type external
              ^operator.name perform-remove
              ^top-state.world.objects <objs>
              ^target-object <obj>)
   (<objs> ^object <obj>)
-->
   (<s> ^removed-target <obj>)
   (<objs> ^object <obj> -)
}

sp {op_remove1*internal*prefer*attend-to-scene-change*over*perform-remove
   (state <s> ^name op_remove1
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-change)
   (<o2> ^name perform-remove)
-->
   (<s> ^operator <o1> > <o2>)
}