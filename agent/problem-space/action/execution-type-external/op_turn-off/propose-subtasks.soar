sp {op_turn-off1*propose*op_find1
   (state <s> ^name op_turn-off1
              ^problem-space.execution-type external
              ^target-object <obj>)
   (<obj> ^predicates <preds>)
   (<preds> ^is-visible1 not-visible1)
-->
   (<s> ^operator <o> +)
   (<o> ^name op_find1
        ^item-type task-operator
        ^task-handle find1
        ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}

sp {op_turn-off1*propose*op_approach1
   (state <s> ^name op_turn-off1
              ^problem-space.execution-type external
              ^target-object <obj>)
   (<obj> ^predicates <preds>)
   (<preds> ^is-visible1 visible1
            ^is-reachable1 not-reachable1)
-->
   (<s> ^operator <o> +)
   (<o> ^name op_approach1
        ^item-type task-operator
        ^task-handle approach1
        ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}

sp {op_turn-off1*propose*primitive-turn-off
   (state <s> ^name op_turn-off1
              ^problem-space.execution-type external
              ^target-object <obj>)
   (<obj> ^predicates <preds>)
   (<preds> ^is-visible1 visible1
            ^is-reachable1 reachable1)
-->
   (<s> ^operator <o> +)
   (<o> ^name primitive-turn-off
        ^item-type task-operator
        ^task-handle primitive-turn-off1
        ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}
