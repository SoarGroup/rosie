sp {primitive-turn-on*external*elaborate*problem-space*attend-to-all-perception
   (state <s> ^name primitive-turn-on
              ^problem-space.execution-type external
              ^problem-space.perception <p>)
-->
   (<p> ^attend-to-all-perception yes)
}

sp {primitive-turn-on*elaborate*target-object*superoperator
   (state <s> ^name primitive-turn-on
              ^task-operator.arg1.id <obj>)
-->
   (<s> ^target-object <obj>)
}

sp {primitive-turn-on*elaborate*executing-command*true
   (state <s> ^name primitive-turn-on
              ^current-task-segment.turn-on-command.status << sent executing >>)
-->
   (<s> ^executing-command true)
}

sp {primitive-turn-on*elaborate*propose-wait*true
   (state <s> ^name primitive-turn-on
              ^executing-command true)
-->
   (<s> ^propose-wait true)
}
