#### Different failure cases that can arise during turn-on

#### What if turn-on fails twice? 

sp {primitive-turn-on*propose*handle-execution-failure
   (state <s> ^name primitive-turn-on
              ^current-task-segment <seg>)
   (<seg> ^turn-on-command <cmd>)
   (<cmd> ^status << failure interrupted >>)
-->
   (<s> ^operator <o> + = >)
   (<o> ^name handle-execution-failure
        ^category failure-handling
        ^task-segment <seg>
        ^command <cmd>)
}

sp {primitive-turn-on*propose*handle-command-failure
   (state <s> ^name primitive-turn-on
              ^current-task-segment <seg>)
   (<seg> ^turn-on-command <cmd>)
   (<cmd> ^status error)
-->
   (<s> ^operator <o> + = >)
   (<o> ^name handle-command-failure
        ^category failure-handling
        ^task-segment <seg>
        ^command <cmd>)
}
