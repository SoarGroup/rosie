
sp {op_open*prefer*attend-to-scene-change*over*commands
   (state <s> ^name op_open1
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-change)
   (<o2> ^name << send-open-command simulate-open-command >>)
-->
   (<s> ^operator <o1> > <o2>)
}
