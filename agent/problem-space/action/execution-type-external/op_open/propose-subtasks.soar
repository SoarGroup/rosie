sp {op_open1*propose*op_find1
   (state <s> ^name op_open1
              ^problem-space.execution-type external
              ^target-object <obj>)
   (<obj> ^predicates <preds>)
   (<preds> ^visible false)
-->
   (<s> ^operator <o> +)
   (<o> ^name op_find1
        ^item-type task-operator
        ^task-handle find1
        ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}

sp {op_open1*propose*op_approach1
   (state <s> ^name op_open1
              ^problem-space.execution-type external
              ^target-object <obj>)
   (<obj> ^predicates <preds>)
   (<preds> ^visible true
            ^inreach not-inreach)
-->
   (<s> ^operator <o> +)
   (<o> ^name op_approach1
        ^item-type task-operator
        ^task-handle approach1
        ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}

sp {op_open1*propose*primitive-open
   (state <s> ^name op_open1
              ^problem-space.execution-type external
              ^target-object <obj>)
   (<obj> ^predicates <preds>)
   (<preds> ^visible true
            ^inreach inreach)
-->
   (<s> ^operator <o> +)
   (<o> ^name primitive-open
        ^item-type task-operator
        ^task-handle primitive-open1
        ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}
