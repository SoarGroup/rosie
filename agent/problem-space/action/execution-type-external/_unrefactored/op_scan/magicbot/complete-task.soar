sp {op_scan1*apply*complete-aciton*success*360*remove*unseen*in*predicates
  (state <s> ^name op_scan1
             ^operator <o>
             ^world.robot.current-location <loc>
             ^world.predicates.predicate <in-pred>
            -^until-clause)
  (<o> ^name complete-task
       ^status success)
  (<in-pred> ^handle in1
             ^instance <i>)
  (<i> ^1 <obj> ^2 <loc>)
  (<obj> -^predicates.is-confirmed1 confirmed1
         -^predicates.is-visible1 visible1)
-->
  (<in-pred> ^instance <i> -)
}

sp {op_scan1*apply*complete-task*failure*remove*unseen*in*predicates
  (state <s> ^name op_scan1
             ^operator <o>
             ^world.robot.current-location <loc>
             ^world.predicates.predicate <in-pred>)
  (<o> ^name complete-task
       ^status failure)
  (<in-pred> ^handle in1
             ^instance <i>)
  (<i> ^1 <obj> ^2 <loc>)
  (<obj> -^predicates.is-confirmed1 confirmed1
         -^predicates.is-visible1 visible1)
-->
  (<in-pred> ^instance <i> -)
}

