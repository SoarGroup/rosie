sp {op_pick-up1*magicbot*propose*op_approach
   (state <s> ^name op_pick-up1
              ^problem-space.execution-type external
              ^distance-to-object > 1.
              ^target-object <obj>)
  -{(<s> ^operator <o> +)
    (<o> ^name attend-to-scene-change)}
-->
   (<s> ^operator <o> +)
   (<o> ^name op_approach1
        ^action-handle approach1
        ^arg1 <arg1>)
   (<arg1> ^arg-type object
           ^id <obj>)
}
