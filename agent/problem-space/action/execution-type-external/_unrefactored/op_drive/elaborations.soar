###### SUCCESS ######

# elaborate goal (desired) at satisfied - drive command reports it was successful
sp {op_drive1*elaborate*desired*satisfied
   (state <s> ^name op_drive1
              ^problem-space.execution-type external
              ^current-task-seg.drive-command.status success)
-->
   (<s> ^desired.satisfied true)
}

###### FAILURE ######

# execution-failure: The drive command got a failure status
sp {op_drive1*elaborate*failure*execution-failure
   (state <s> ^name op_drive1
              ^problem-space.execution-type external
              ^current-task-seg.drive-command.status { <status> << error failure interrupted >> })
-->
   (<s> ^failure <f>)
   (<f> ^type execution-failure
        ^command drive
        ^error-status <status>)
}
