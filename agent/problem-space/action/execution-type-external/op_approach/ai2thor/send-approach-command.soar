sp {op_approach1*propose*send-approach-command
   (state <s> ^name op_approach1
              ^problem-space.execution-type external
              ^target-object.handle <h>
             -^current-task-segment.approach-command)
-->
   (<s> ^operator <o> + )
   (<o> ^name send-approach-command
        ^object-handle <h>)
}

sp {op_approach1*apply*send-approach-command*send
   (state <s> ^name op_approach1
              ^operator <o>
              ^io.output-link <out>
              ^current-task-segment <seg>)
   (<o> ^name send-approach-command
        ^object-handle <h>)
-->
   (<out> ^perform-action <pa>)
   (<pa> ^name approach
         ^object <h>)
   (<seg> ^approach-command <pa>)
}
