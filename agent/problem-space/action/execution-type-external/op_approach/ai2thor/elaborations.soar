sp {op_approach1*elaborate*problem-space*attend-to-all-perception*yes
   (state <s> ^name op_approach1
              ^problem-space.execution-type external
              ^problem-space.perception <p>)
-->
   (<p> ^attend-to-all-perception yes)
}

sp {op_approach1*elaborate*target-object*superoperator
   (state <s> ^name op_approach1
              ^problem-space.execution-type external
              ^task-operator.arg1.id <obj>)
-->
   (<s> ^target-object <obj>)
}


sp {op_approach1*elaborate*executing-command*true
   (state <s> ^name op_approach1
              ^current-task-segment.approach-command.status << sent executing >>)
-->
   (<s> ^executing-command true)
}

sp {op_approach1*elaborate*allow-wait*true
   (state <s> ^name op_approach1
              ^executing-command true)
-->
   (<s> ^propose-wait true)
}

#### GOAL SATISFACTION ####
sp {op_approach1*elaborate*desired*satisfied
   (state <s> ^name op_approach1
             -^executing-command true
              ^current-task-segment.approach-command.status success
              ^target-object.predicates.inreach inreach)
-->
   (<s> ^desired.satisfied true)
}

