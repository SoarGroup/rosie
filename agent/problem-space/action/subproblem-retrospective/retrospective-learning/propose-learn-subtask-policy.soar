sp {retrospective-learning*propose*learn-subtask-policy
   (state <s> ^name retrospective-learning
              ^subtasks-stack.next { <next> <> none })
   (<next> -^learning-result)
-->
   (<s> ^operator <o> +)
   (<o> ^name learn-subtask-policy
        ^subtask <next>)
}
