sp {deduce-task-goal*apply*primitive*action*advance*subtask
   (state <s> ^name deduce-task-goal
              ^operator <o>
              ^subtasks <subs>)
   (<o> ^task-handle <h>)
   (<subs> ^next <sub>)
   (<sub> ^next <next>)
-->
   (<subs> ^next <sub> -)
   (<subs> ^next <next>)
}
   
