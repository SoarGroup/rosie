# When the look-ahead-operator is applied, advance the subtask
sp {subproblem*retrospective*evaluate-operator*apply*look-ahead-operator*advance*subtask
   (state <s> ^name evaluate-operator
              ^problem-space.subproblem retrospective
              ^look-ahead-operator <o>
              ^operator <o>
              ^subtasks <subs>)
   (<subs> ^next <cur>)
   (<cur> ^next <next>)
-->
   (<subs> ^next <cur> -
           ^next <next>)
}
