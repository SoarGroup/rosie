sp {learn-subtask-policy*prefer*smem-query*over*task*operator
   (state <s> ^name learn-subtask-policy
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name smem-query)
   (<o2> ^item-type task-operator)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {learn-subtask-policy*prefer*return-learning-result*over*task*operator
   (state <s> ^name learn-subtask-policy
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name return-learning-result)
   (<o2> ^item-type task-operator)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {learn-subtask-policy*prefer*push-procedural-subgoal*over*task*operator
   (state <s> ^name learn-subtask-policy
              ^operator <o1> +
              ^operator <o2> +
              ^task-operator <o2>)
   (<o1> ^name push-procedural-subgoal)
-->
   (<s> ^operator <o1> > <o2>)
}
