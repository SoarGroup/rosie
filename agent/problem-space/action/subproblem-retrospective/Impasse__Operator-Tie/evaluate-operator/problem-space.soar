# Uncomment to turn on chunking evaluate-operator
sp {subproblem*retrospective*evaluate-operator*turn*on*chunking
   (state <s> ^name evaluate-operator
              ^problem-space <ps>)
   (<ps> ^name action
         ^subproblem retrospective
         ^learning <l>)
-->
   (<l> ^use chunking)
}
