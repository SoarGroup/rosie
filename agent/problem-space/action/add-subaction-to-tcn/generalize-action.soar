
# Use the generalize-action operator (found in action-utils) 
# to generate a generalized smem rep for the action based on the subaction operator

sp {add-subaction-to-tcn*elaborate*generalize-action
   (state <s> ^name add-subaction-to-tcn
              ^task-concept-network.procedural <proc>
              ^action-operator <aop>
              ^subaction <sub>
              ^subaction-tcn <sub-tcn>)
-->
   (<s> ^generalize-action <ga>)
   (<ga> ^source-action <sub>
         ^source-tcn <sub-tcn>
         ^reference-action <aop>
         ^reference-tcn-proc <proc>)
}
