sp {task-state-no-change*prefer*learn-subtask-proposal*over*search-for-next-subtask
   (state <s> ^name task-state-no-change
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name learn-subtask-proposal)
   (<o2> ^name search-for-next-subtask)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {task-state-no-change*prefer*learn-task-action-model*over*search-for-next-subtask
   (state <s> ^name task-state-no-change
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name learn-task-action-model)
   (<o2> ^name search-for-next-subtask)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {task-state-no-change*prefer*remove-tcn*over*subtasks
   (state <s> ^name task-state-no-change
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name remove-tcn)
   (<o2> ^name << learn-subtask-proposal learn-task-action-model search-for-next-subtask >>)
-->
   (<s> ^operator <o1> > <o2>)
}
