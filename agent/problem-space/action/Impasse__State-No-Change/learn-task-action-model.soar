sp {action-state-no-change*propose*learn-task-action-model
   (state <s> ^name action-state-no-change
              ^superstate.learn-action-model true
              ^superstate.desired <des>)
   -{(<des> -^predicate <any>)}
-->
   (<s> ^operator <o> + =)
   (<o> ^name learn-task-action-model)
}

sp {action-state-no-change*apply*learn-task-action-model*when*learned*remove*flag*superstate
   (state <s> ^name action-state-no-change
              ^operator.name learn-task-action-model
              ^quiescence t
              ^learned-action-model true
              ^superstate <ss>)
   (<ss> ^learn-action-model true)
-->
   (<ss> ^learn-action-model true -)
}
