sp {learn-task-action-model-substate*learn-task-action-model*1*rel
   (state <s> ^name learn-task-action-model-substate
              ^action-operator <aop>
              ^superstate <ss>
              ^desired <des>)
   (<ss> ^operator <aop>
         ^superstate <sss>
         ^problem-space <ps>
         ^world.predicates.predicate <pred>)
   (<ps> ^name action
         ^execution-type internal)
   (<des> ^predicate <p1>
         -^predicate { <p2> <> <p1> })
   (<p1> ^type relation
         ^handle <ph>
         ^1 <obj1>
         ^2 <obj2>)
   (<pred> ^handle <ph>)
   -{(<pred> ^instance <i>)
     (<i> ^1 <obj1> ^2 <obj2>)}
-->
    (<pred> ^instance <i>)
    (<i> ^1 <obj1> ^2 <obj2>)
}

sp {learn-task-action-model-substate*report*learning-finished*superstate
   (state <s> ^name learn-task-action-model-substate
              ^action-operator <aop>
              ^superstate <ss>
              ^desired <des>
              ^quiescence t)
-->
   (<ss> ^learning-finished true)
}

