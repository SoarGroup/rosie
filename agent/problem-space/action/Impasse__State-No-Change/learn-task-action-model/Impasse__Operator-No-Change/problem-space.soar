#sp {action*execution-type*internal*ONC*elaborate*problem-space
#   (state <s> ^superstate.problem-space.execution-type internal
#              ^superstate.operator.action-handle <h>
#              ^superstate.world <w>)
#-->
#   (<s> ^default-problem-space no)
#
#   (<s> ^problem-space <ps>)
#   (<ps> ^name action
#         ^execution-type internal
#         ^world <world>
#         ^learning <learn>)
#   (<world> ^copy-type deep
#            ^source <w>)
#}

sp {action*execution-type*internal*ONC*elaborate*learning*use*chunking
   (state <s> ^superstate.name learn-task-action-model
              ^attribute operator
              ^impasse no-change
              ^problem-space.learning <l>)
-->
   (<l> ^use chunking)
}
