sp {action*simulate*ONC*elaborate*learning*use*chunking
   (state <s> ^superstate.name learn-task-action-model
              ^attribute operator
              ^impasse no-change
              ^problem-space.learning <l>)
-->
   (<l> ^use chunking)
}
