sp {search-for-next-action*propose*force-operator-tie
   (state <s> ^problem-space.action-space search
              ^top-search-state true)
-->
   (<s> ^operator <o> +)
   (<o> ^name force-operator-tie)
}

sp {selection*apply*evaluate-operator*force-operator-tie*failure
   (state <s> ^name selection
              ^operator <o>
              ^evaluation <e>)
   (<o> ^name evaluate-operator
        ^evaluation <e>)
   (<e> ^superoperator.name force-operator-tie)
-->
   (<e> ^symbolic-value depth-exceeded)
}
