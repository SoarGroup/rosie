
sp {search-for-next-action*propose*force-tie-operator
   (state <s> ^problem-space.action-space search
              ^top-search-state true)
-->
   (<s> ^operator <o> +)
   (<o> ^name force-tie-operator)
}

sp {selection*apply*evaluate-operator*force-tie-operator*failure
   (state <s> ^name selection
              ^operator <o>
              ^evaluation <e>)
   (<o> ^name evaluate-operator
        ^evaluation <e>)
   (<e> ^superoperator.name force-tie-operator)
-->
   (<e> ^symbolic-value depth-exceeded)
}
