sp {action*execution-type*internal*OpNC*learn-task-action-model*elaborate*learning*use*chunking
   (state <s> ^name learn-task-action-model
              ^problem-space.learning <l>)
-->
   (<l> ^use chunking)
}
