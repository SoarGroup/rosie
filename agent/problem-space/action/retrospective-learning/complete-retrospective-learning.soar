
sp {retrospective-learning*propose*complete-retrospective-learning
   (state <s> ^name retrospective-learning)
-->
   (<s> ^operator <o> + < =)
   (<o> ^name complete-retrospective-learning)
}

sp {retrospective-learning*apply*complete-retrospective-learning
   (state <s> ^name retrospective-learning
              ^operator.name complete-retrospective-learning
              ^top-state.interaction.status <status>)
-->
   (<status> ^performed.learning-event.type retrospective)
}

