sp {handle-command-failure*elaborate*action-segment*superoperator
   (state <s> ^name handle-command-failure
              ^superstate.operator.action <a>)
-->
   (<s> ^action-segment <a>)
}

sp {handle-command-failure*elaborate*failed-command*superoperator
   (state <s> ^name handle-command-failure
              ^superstate.operator.command <cmd>)
-->
   (<s> ^failed-command <cmd>)
}
