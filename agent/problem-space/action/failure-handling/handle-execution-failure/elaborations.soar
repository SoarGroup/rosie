sp {handle-execution-failure*elaborate*action-segment*superoperator
   (state <s> ^name handle-execution-failure
              ^superstate.operator.action <a>)
-->
   (<s> ^action-segment <a>)
}

sp {handle-execution-failure*elaborate*failed-command*superoperator
   (state <s> ^name handle-execution-failure
              ^superstate.operator.command <cmd>)
-->
   (<s> ^failed-command <cmd>)
}