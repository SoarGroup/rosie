sp {handle-missing-object-failure*elaborate*action-segment*superoperator
   (state <s> ^name handle-missing-object-failure
              ^superstate.operator.action <a>)
-->
   (<s> ^action-segment <a>)
}

sp {handle-missing-object-failure*elaborate*missing-object*superoperator
   (state <s> ^name handle-missing-object-failure
              ^superstate.operator.object <obj>)
-->
   (<s> ^missing-object <obj>)
}
