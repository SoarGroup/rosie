sp {handle-missing-object-failure*elaborate*task-segment*superoperator
   (state <s> ^name handle-missing-object-failure
              ^superstate.operator.task-segment <a>)
-->
   (<s> ^task-segment <a>)
}

sp {handle-missing-object-failure*elaborate*missing-object*superoperator
   (state <s> ^name handle-missing-object-failure
              ^superstate.operator.object <obj>)
-->
   (<s> ^missing-object <obj>)
}

sp {handle-missing-object-failure*elaborate*silent*superoperator
   (state <s> ^name handle-missing-object-failure
              ^superstate.operator.silent <flag>)
-->
   (<s> ^silent <flag>)
}
