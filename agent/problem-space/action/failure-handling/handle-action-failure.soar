sp {action-space*execute*propose*handle-action-failure
   (state <s> ^problem-space.action-space execute
              ^failure <f>)
   (<f> ^type <type>)
-->
   (<s> ^operator <o> + > =)
   (<o> ^name handle-action-failure
        ^failure <f>)
   (interrupt)
}
