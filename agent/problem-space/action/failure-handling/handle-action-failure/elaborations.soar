sp {handle-action-failure*elaborate*task-segment*superoperator
   (state <s> ^name handle-action-failure
              ^superstate.operator.task-segment <seg>)
-->
   (<s> ^task-segment <seg>)
}

sp {handle-action-failure*elaborate*failed-action-name*superoperator
   (state <s> ^name handle-action-failure
              ^superstate.operator.failed-action-name <name>)
-->
   (<s> ^failed-action-name <name>)
}

sp {handle-action-failure*elaborate*silent*superoperator
   (state <s> ^name handle-action-failure
              ^superstate.operator.silent <flag>)
-->
   (<s> ^silent <flag>)
}
