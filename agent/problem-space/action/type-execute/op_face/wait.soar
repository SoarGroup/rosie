sp {op_face1*propose*wait*during*command
   (state <s> ^name op_face1
              ^problem-space.action.type execute
              -^operator.name wait
              ^current-action.orient-command <cmd>)
   (<cmd> ^status << sent executing >>)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
              
