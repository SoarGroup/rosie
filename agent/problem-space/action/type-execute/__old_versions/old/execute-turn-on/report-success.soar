sp {op_turn-on*propose*report-success
   (state <s> ^name op_turn-on1
              ^target-object.predicates.activation1 on2)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}

sp {op_turn-on*apply*report-success*add*status
   (state <s> ^name op_turn-on1
              ^operator.name report-success
              ^current-action <a>)
-->
   (<a> ^status complete)
}

sp {op_turn-on*apply*report-success*remove*command
   (state <s> ^name op_turn-on1
              ^operator.name report-success
              ^current-action <a>
              ^io.output-link <out>)
   (<a> ^turn-on-command <cmd>)
   (<out> ^<cmd-name> <cmd>)
-->
   (<a> ^turn-on-command <cmd> -)
   (<out> ^<cmd-name> <cmd> -)
}
