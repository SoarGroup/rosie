sp {op_pick-up1*internal*propose*propose*report-success
   (state <s> ^name op_pick-up1
              ^problem-space.action.type execute
              ^target-object.predicates.arm-status grabbed)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}

sp {op_pick-up1*internal*apply*report-success*mark*finished
   (state <s> ^name op_pick-up1
              ^operator.name report-success
              ^current-action <a>)
-->
   (<a> ^status complete)
}
