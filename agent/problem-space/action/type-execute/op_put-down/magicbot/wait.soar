sp {op_put-down1*magicbot*propose*wait
   (state <s> ^name op_put-down1
              ^problem-space.action.type execute
              -^operator.name wait
               ^current-action.{ << face-command drive-command put-down-command >> }.status << sent executing >>)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
