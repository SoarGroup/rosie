sp {problem-space*action*type-execute*common*propose*wait*while*executing
   (state <s> ^problem-space <ps>
              ^executing-command true
             -^operator.name wait)
   (<ps> ^name action
         ^action.type execute)
-->
   (<s> ^operator <o> + < =)
   (<o> ^name wait)
}
