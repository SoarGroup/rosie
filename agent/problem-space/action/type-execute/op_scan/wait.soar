sp {op_scan1*propose*wait*during*command
   (state <s> ^name op_scan1
              ^problem-space.action.type execute
              -^operator.name wait
              ^current-action.{ << turn-command stop-command >> } <cmd>)
   (<cmd> ^status << sent executing >>)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
              
