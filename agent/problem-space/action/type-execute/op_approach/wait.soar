sp {op_approach1*magicbot*propose*wait
   (state <s> ^name op_approach1
              ^problem-space.action.type execute
              -^operator.name wait
               ^current-action.{ << drive-command face-command >> }.status << sent executing >>)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}
