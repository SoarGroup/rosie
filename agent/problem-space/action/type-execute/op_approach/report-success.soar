sp {op_approach1*propose*report-success*finished-drive
   (state <s> ^name op_approach1
              ^current-action.drive-command.status success)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}

sp {op_approach1*propose*report-success*already*close
   (state <s> ^name op_approach1
              ^current-action.face-command.status success
              -^current-action.drive-command
              ^calculations.distance <= 1.5)
-->
   (<s> ^operator <o> +)
   (<o> ^name report-success)
}