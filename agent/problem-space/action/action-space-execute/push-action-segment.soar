sp {action-space*execute*propose*push-action-segment*new-action-segment*on*stack
   (state <s> ^problem-space.action-space execute
              ^action-stack.new-action-segment <seg>
             -^child-action)
-->
   (<s> ^operator <o> +)
   (<o> ^name push-action-segment
        ^segment <seg>)
}

sp {action-space*execute*propose*push-action-segment*new-action-segment*on*state
   (state <s> ^problem-space.action-space execute
              ^new-action-segment <seg>)
-->
   (<s> ^operator <o> +)
   (<o> ^name push-action-segment
        ^segment <seg>)
}

