# Elaborate the problem-space when the superstate in an execute action problem-space

sp {action-space*execute*elaborate*problem-space*world*copy-type
   (state <s> ^problem-space <ps>)
   (<ps> ^name action
         ^action-space execute
         ^world <world>)
-->
   (<world> ^copy-type shallow)
}
            
