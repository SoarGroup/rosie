# If there is a child action on the stack, propose it
sp {action-space*execute*propose*child-task-seg
   (state <s> ^problem-space.action-space execute
              ^child-task-seg <a>)
   (<a> ^task-operator <ex>)
-->
   (<s> ^operator <ex> +)
}
