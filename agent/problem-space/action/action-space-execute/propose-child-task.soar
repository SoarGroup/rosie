# If there is a child task on the stack, propose it
sp {action-space*execute*propose*child-task-segment
   (state <s> ^problem-space.action-space execute
              ^child-task-segment <a>)
   (<a> ^task-operator <ex>)
-->
   (<s> ^operator <ex> +)
}
