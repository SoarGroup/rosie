# @DEL: don't need this flag anymore (probably)
#sp {action-space*execute*apply*learned-subaction-proposal*mark*learned*and*store
#   (state <s> ^problem-space.action-space execute
#              ^operator <o>
#              ^task-concept-network.procedural.subactions.subaction <sub-tcn>
#              ^smem.command <cmd>)
#   (<o> ^task-handle <h>
#        ^subtask-handle <sub-h>)
#   (<sub-tcn> ^handle <sub-h>
#            -^learned-proposal true)
#-->
#   (<sub-tcn> ^learned-proposal true)
#   (<cmd> ^store <sub-tcn>)
#}
          
sp {action-space*execute*apply*subaction*remove*unlearned
   (state <s> ^problem-space.name action
              ^problem-space.action-space execute
              ^operator.subtask-handle <sub-handle>
              ^current-action <a>)
   (<a> ^learn-subaction-proposal <sub-handle>)
-->
   (<a> ^learn-subaction-proposal <sub-handle> -)
}
