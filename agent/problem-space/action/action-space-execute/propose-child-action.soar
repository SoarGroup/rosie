# If there is a child action on the stack, propose it with a best preference
sp {action-space*execute*propose*child-action
   (state <s> ^problem-space.action-space execute
              ^child-action <a>)
   (<a> ^execution-operator <ex>)
-->
   (<s> ^operator <ex> +)
}
