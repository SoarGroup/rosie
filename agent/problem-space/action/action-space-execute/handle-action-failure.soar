sp {action*type-execute*propose*handle-action-failure
   (state <s> ^problem-space.action-space execute
             -^name handle-action-failure
              ^failure <f>)
   (<f> ^type <type>)
-->
   (<s> ^operator <o> + > =)
   (<o> ^name handle-action-failure
        ^failure <f>)
   (interrupt)
}