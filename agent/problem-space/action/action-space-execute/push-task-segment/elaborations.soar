sp {problem-space*action*push-task-segment*elaborate*task-stack
   (state <s> ^name push-task-segment
              ^superstate.task-stack <stack>)
-->
   (<s> ^task-stack <stack>)
}

sp {problem-space*action*push-task-segment*elaborate*segment-to-push
   (state <s> ^name push-task-segment
              ^superstate.operator.segment <seg>)
-->
   (<s> ^segment-to-push <seg>)
}

sp {problem-space*action*push-task-segment*elaborate*attend-to-scene-change
   (state <s> ^name push-task-segment)
-->
   (<s> ^problem-space <ps>)
   (<ps> ^perception.attend-to-all-perception yes)
}
