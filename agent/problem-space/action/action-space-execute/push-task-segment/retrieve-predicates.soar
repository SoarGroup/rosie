
sp {problem-space*action*push-task-segment*elaborate*smem-query*predicate
   (state <s> ^name push-task-segment
              ^segment-to-push.task-operator <a>
             -^predicate-lti.handle <pred-handle>)
   (<a> ^<arg-name> <arg>)
   (<arg> ^arg-type predicate
          ^handle <pred-handle>)
-->
   (<s> ^smem-query <q>)
   (<q> ^cue.handle <pred-handle>
        ^depth 4
        ^destination <s>
        ^result-name predicate-lti)
}

