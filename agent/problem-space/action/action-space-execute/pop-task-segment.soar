sp {action-space*execute*propose*pop-task-segment*child-task-seg*finished
   (state <s> ^problem-space.action-space execute
              ^child-task-seg <seg>)
   (<seg> ^status <status>)
-->
   (<s> ^operator <o> +)
   (<o> ^name pop-task-segment
        ^segment <seg>)
}
