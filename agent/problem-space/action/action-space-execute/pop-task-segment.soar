sp {action-space*execute*propose*pop-task-segment*child-action*finished
   (state <s> ^problem-space.action-space execute
              ^child-action <seg>)
   (<seg> ^status <status>)
-->
   (<s> ^operator <o> +)
   (<o> ^name pop-task-segment
        ^segment <seg>)
}
