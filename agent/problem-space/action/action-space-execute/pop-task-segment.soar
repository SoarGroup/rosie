sp {action-space*execute*propose*pop-task-segment*child-task-segment*finished
   (state <s> ^problem-space.action-space execute
              ^child-task-segment <seg>)
   (<seg> ^status <status>)
-->
   (<s> ^operator <o> +)
   (<o> ^name pop-task-segment
        ^segment <seg>)
}
