sp {problem-space*action*pop-action-segment*elaborate*task-stack
   (state <s> ^name pop-action-segment
              ^superstate.task-stack <stack>)
-->
   (<s> ^task-stack <stack>)
}

sp {problem-space*action*pop-action-segment*elaborate*action-to-pop
   (state <s> ^name pop-action-segment
              ^superstate.operator.segment <segment>)
-->
   (<s> ^segment-to-pop <segment>)
}
