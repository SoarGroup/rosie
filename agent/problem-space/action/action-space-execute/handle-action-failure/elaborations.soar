sp {action-space*execute*handle-action-failure*elaborate*current-action
   (state <s> ^name handle-action-failure
              ^superstate.current-action <a>)
-->
   (<s> ^current-action <a>)
}

sp {action-space*execute*handle-action-failure*elaborate*failure-type
   (state <s> ^name handle-action-failure
              ^superstate.operator.failure.type <type>)
-->
   (<s> ^failure-type <type>)
}

sp {action-space*execute*handle-action-failure*elaborate*failure-info
   (state <s> ^name handle-action-failure
              ^superstate.operator.failure.info <info>)
-->
   (<s> ^failure-info <info>)
}

sp {action-space*execute*handle-action-failure*elaborate*failure-info*none
   (state <s> ^name handle-action-failure
              ^superstate.operator.failure <f>)
   (<f> -^info <info>)
-->
   (<s> ^failure-info none)
}