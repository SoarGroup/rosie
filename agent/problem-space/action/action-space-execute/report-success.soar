sp {action-space*execute*propose*report-success*desired*satisfied
   (state <s> ^problem-space.action-space execute
              ^desired.satisfied true)
-->
   (<s> ^operator <o> + >)
   (<o> ^name report-success)
}

sp {action-space*execute*apply*report-success*mark*status*success
   (state <s> ^problem-space.action-space execute
              ^operator.name report-success
              ^current-action <a>)
-->
   (<a> ^status success)
}

sp {action-space*execute*apply*report-success*remove*output*commands
   (state <s> ^problem-space.action-space execute
              ^operator.name report-success
              ^current-action <a>
              ^io.output-link <out>)
   (<a> ^<cmd-name> <cmd>)
   (<out> ^<out-name> <cmd>)
-->
   (<out> ^<out-name> <cmd> -)
}