# If no task-concept-network, retrieve from semantic memory
sp {action-space*execute*problem-space*elaborate*smem-query
   (state <s> ^problem-space.action-space execute
              ^action-operator <action-op>
             -^task-concept-network)
   (<action-op> ^task-handle <task-handle>)
-->
   (<s> ^smem-query <query>)
   (<query> ^cue <cue>
            ^depth 8
            ^destination <s>
            ^result-name task-concept-network)
   (<cue> ^handle <task-handle>)
}

