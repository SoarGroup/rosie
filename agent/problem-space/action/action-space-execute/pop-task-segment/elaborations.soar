sp {problem-space*action*pop-task-segment*elaborate*task-stack
   (state <s> ^name pop-task-segment
              ^superstate.task-stack <stack>)
-->
   (<s> ^task-stack <stack>)
}

sp {problem-space*action*pop-task-segment*elaborate*segment-to-pop
   (state <s> ^name pop-task-segment
              ^superstate.operator.segment <segment>)
-->
   (<s> ^segment-to-pop <segment>)
}
