sp {action-space*execute*propose*pop-action-segment*current-action*finished
   (state <s> ^problem-space.action-space execute
              ^current-action <seg>)
   (<seg> ^status <status>)
-->
   (<s> ^operator <o> +)
   (<o> ^name pop-action-segment
        ^segment <seg>)
}
