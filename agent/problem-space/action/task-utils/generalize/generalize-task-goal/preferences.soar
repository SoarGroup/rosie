sp {task-utils*generalize-task-goal*prefer*generalize-task-argument*over*complete-generalize-task-goal
  (state <s> ^name generalize-task-goal
             ^operator <o1> +
             ^operator <o2> +)
  (<o1> ^name generalize-task-argument)
  (<o2> ^name complete-generalize-task-goal)
-->
  (<s> ^operator <o1> > <o2>)
}

