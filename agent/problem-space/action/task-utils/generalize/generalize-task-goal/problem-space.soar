sp {task-utils*generalize-task-goal*elaborate*problem-space
   (state <s> ^name generalize-task-goal
             -^name learn-goal-definition)
-->
   (<s> ^default-problem-space no
        ^problem-space <ps>)
#   (<ps> ^learning.use chunking)
}
