sp {task-utils*generalize-task-operator*prefer*generalize-task-argument*over*complete-generalize-task-operator
  (state <s> ^name generalize-task-operator
             ^operator <o1> +
             ^operator <o2> +)
  (<o1> ^name generalize-task-argument)
  (<o2> ^name complete-generalize-task-operator)
-->
  (<s> ^operator <o1> > <o2>)
}

