### GENERALIZE CLAUSE 
### (until-clause after-clause)

sp {task-utils*generalize-task-argument*elaborate*generalize-task-argument*clause*as*predicate-set
   (state <s> ^generalize-task-argument <ga>)
   (<ga> ^arg-type << until-clause after-clause >>
         ^instantiation <i>)
-->
   (<s> ^generalize-task-argument <gps>)
   (<gps> ^arg-type predicate-set
          ^instantiation <i>)
   (<ga> ^gen-pred-set <gps>)
}

sp {task-utils*generalize-task-argument*elaborate*generalize-task-argument*clause*result*from*predicate-set
   (state <s> ^generalize-task-argument <ga>)
   (<ga> ^arg-type << until-clause after-clause >>
         ^instantiation <i>
         ^gen-pred-set.general-arg-rep <gen-rep>)
-->
   (<ga> ^general-arg-rep <gen-rep>)
}

