### GENERALIZE TEMPORAL CLAUSE 

sp {task-utils*generalize-task-argument*elaborate*generalize-task-argument*temporal-clause*as*predicate-set
   (state <s> ^generalize-task-argument <ga>)
   (<ga> ^arg-type temporal-clause
         ^instantiation <i>)
-->
   (<s> ^generalize-task-argument <gps>)
   (<gps> ^arg-type predicate-set
          ^instantiation <i>)
   (<ga> ^gen-pred-set <gps>)
}

sp {task-utils*generalize-task-argument*elaborate*generalize-task-argument*temporal-clause*result*from*predicate-set
   (state <s> ^generalize-task-argument <ga>)
   (<ga> ^arg-type temporal-clause
         ^gen-pred-set.general-arg-rep <gen-rep>)
-->
   (<ga> ^general-arg-rep <gen-rep>)
}

