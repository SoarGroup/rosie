### construct modifier

sp {task-utils*construct-task-argument*propose*construct-task-argument*modifier
   (state <s> ^construct-task-argument <arg>)
   (<arg> ^arg-type modifier
          ^source.handle <handle>
         -^created-arg)
-->
   (<s> ^operator <o> + =)
   (<o> ^name construct-task-argument
        ^argument <arg>
        ^arg-type modifier
        ^handle <handle>)
}

sp {task-utils*construct-task-argument*apply*construct-task-argument*modifier
   (state <s> ^operator <o>)
   (<o> ^name construct-task-argument
        ^argument <arg>
        ^arg-type modifier
        ^handle <handle>)
-->
   (<arg> ^created-arg <carg>)
   (<carg> ^handle <handle>)
}
