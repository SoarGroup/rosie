#### operator add-start-goal-to-tcn
# initializes a new smem structure for the starting task goal the first time we see it

# propose add-start-goal-to-tcn if no ^procedural link exists on the task-concept-network
sp {construct-task-operator*propose*add-start-goal-to-tcn
   (state <s> ^name construct-task-operator
              ^update-task-concept-network true
              ^task-concept-network <tcn>)
   (<tcn> -^goal-graph)
-->
   (<s> ^operator <o> + =)
   (<o> ^name add-start-goal-to-tcn)
}

# create the basic semantic structure for the task, 
sp {construct-task-operator*apply*add-start-goal-to-tcn
   (state <s> ^name construct-task-operator
              ^operator.name add-start-goal-to-tcn
              ^task-concept-network <tcn>)
   (<tcn> ^handle <handle>)
-->
   (<tcn> ^goal-graph <start>)
   (<start> ^handle (make-constant-symbol (concat <handle> |start|)) ^item-type start-goal)
   (<s> ^to-store <tcn> <start>)
}

        
