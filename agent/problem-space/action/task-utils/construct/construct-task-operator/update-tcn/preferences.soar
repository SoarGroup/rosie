sp {construct-task-operator*update-tcn*prefer*others*over*smem-store
   (state <s> ^name update-tcn
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << smem-query initialize-tcn add-argument-to-tcn >>)
   (<o2> ^name smem-store)
-->
   (<s> ^operator <o1> > <o2>)
}

