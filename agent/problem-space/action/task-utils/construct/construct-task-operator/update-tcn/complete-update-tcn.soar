sp {update-tcn*propose*complete-update-tcn
   (state <s> ^name update-tcn)
-->
   (<s> ^operator <o> + <)
   (<o> ^name complete-update-tcn)
}

sp {update-tcn*apply*complete-update-tcn
   (state <s> ^name update-tcn
              ^operator.name complete-update-tcn
              ^superstate <ss>)
-->
   (<ss> ^completed-update-tcn true)
}
