sp {add-node-to-goal-graph*prefer*create-node*over*smem-store
   (state <s> ^name add-node-to-goal-graph
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << create-start-node create-predicate-set-node create-terminal-node create-graph-edge >>)
   (<o2> ^name smem-store)
-->
   (<s> ^operator <o1> > <o2>)
}
