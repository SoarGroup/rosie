sp {add-node-to-goal-graph*prefer*create-node*over*smem*stores
   (state <s> ^name add-node-to-goal-graph
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << create-start-node create-intermediate-node create-predicate-set-node create-composite-node create-subtask-node create-terminal-node >>)
   (<o2> ^name << smem-store change-goal-handles >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {add-node-to-goal-graph*prefer*change*edges*over*smem*stores
   (state <s> ^name add-node-to-goal-graph
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << create-graph-edge move-graph-edge >>)
   (<o2> ^name << smem-store change-goal-handles >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {add-node-to-goal-graph*prefer*change-goal-handles*over*smem-store
   (state <s> ^name add-node-to-goal-graph
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name change-goal-handles)
   (<o2> ^name smem-store)
-->
   (<s> ^operator <o1> > <o2>)
}
