
sp {task-utils*push-task-segment*prefer*smem-query*best
   (state <s> ^name push-task-segment
              ^operator <o> +)
   (<o> ^name smem-query)
-->
   (<s> ^operator <o> >)
}

sp {task-utils*push-task-segment*prefer*attend-to-scene-change*over*add-object-to-world
   (state <s> ^name push-task-segment
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name attend-to-scene-change)
   (<o2> ^name add-object-to-world)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {task-utils*push-task-segment*prefer*others*over*add-predicate-to-world
   (state <s> ^name push-task-segment
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name << add-object-to-world attend-to-scene-change replace-task-object >>)
   (<o2> ^name add-predicate-to-world)
-->
   (<s> ^operator <o1> > <o2>)
}

