sp {anystate*copy-desired*reject*all*operators*force*SNC
   (state <s> -^name learn-copy-desired-elaboration-rule
               ^copy-desired <des>
              -^desired.duplicate-of <des>
               ^operator <o> +)
   (<des> ^pred-count > 0)
-->
   (<s> ^operator <o> -
        ^learning-chunking-rule true)
}

