sp {task-utils*copy-task-goal*reject*all*operators*force*SNC
   (state <s> -^name learn-copy-task-goal-elaboration-rule
               ^copy-task-goal <goal>
              -^desired.duplicate-of <goal>
               ^operator <o> +)
   (<goal> ^pred-count > 0)
-->
   (<s> ^operator <o> -
        ^learning-chunking-rule true)
}

