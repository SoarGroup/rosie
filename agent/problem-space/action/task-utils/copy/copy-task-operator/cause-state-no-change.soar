sp {task-utils*copy-task-operator*reject*all*operators*force*SNC
   (state <s> -^name learn-copy-task-op-elaboration-rule
              ^copy-task-operator <cto>
              ^operator <o> +)
   (<cto> ^destination <dest>
          ^copy-name <name>)
   (<dest> -^<name>)
-->
   (<s> ^operator <o> -
        ^learning-chunking-rule true)
}

