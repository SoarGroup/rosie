### COPY MODIFIER ARGUMENT

sp {task-utils*copy-task-argument*propose*copy-task-argument*modifier
   (state <s> ^copy-task-argument <arg>)
   (<arg> ^arg-type modifier
          ^source-arg.handle <h>
         -^arg-copy)
-->
   (<s> ^operator <o> + =)
   (<o> ^name copy-task-argument
        ^argument <arg>
        ^arg-type modifier
        ^handle <h>)
}

sp {task-utils*copy-task-argument*apply*copy-task-argument*modifier
   (state <s> ^operator <o>)
   (<o> ^name copy-task-argument
        ^argument <arg>
        ^arg-type modifier
        ^handle <h>)
-->
   (<arg> ^arg-copy <copy>)
   (<copy> ^handle <h>)
}
