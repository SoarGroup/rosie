sp {task-utils*instantiate-task-operator*prefer*instantiate-task-argument*over*complete-instantiate-task-operator
  (state <s> ^name instantiate-task-operator
             ^operator <o1> +
             ^operator <o2> +)
  (<o1> ^name instantiate-task-argument)
  (<o2> ^name complete-instantiate-task-operator)
-->
  (<s> ^operator <o1> > <o2>)
}
