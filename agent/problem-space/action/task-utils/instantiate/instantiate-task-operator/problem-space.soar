sp {task-utils*instantiate-task-operator*elaborate*problem-space
   (state <s> ^name instantiate-task-operator)
-->
   (<s> ^default-problem-space no
        ^problem-space <ps>)
   (<ps> ^learning.use chunking)
}
