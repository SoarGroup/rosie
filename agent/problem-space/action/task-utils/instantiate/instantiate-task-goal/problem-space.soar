sp {task-utils*instantiate-task-goal*elaborate*problem-space
   (state <s> ^name instantiate-task-goal)
-->
   (<s> ^default-problem-space no
        ^problem-space <ps>)
   (<ps> ^learning.use chunking)
}
