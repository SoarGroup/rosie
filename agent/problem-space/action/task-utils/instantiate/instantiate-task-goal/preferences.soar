sp {task-utils*instantiate-task-goal*prefer*instantiate-task-argument*over*complete-instantiate-task-goal
  (state <s> ^name instantiate-task-goal
             ^operator <o1> +
             ^operator <o2> +)
  (<o1> ^name instantiate-task-argument)
  (<o2> ^name complete-instantiate-task-goal)
-->
  (<s> ^operator <o1> > <o2>)
}

