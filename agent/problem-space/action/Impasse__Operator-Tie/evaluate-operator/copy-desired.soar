sp {problem-space*action*evaluate-operator*elaborate*problem-space*default-desired-copy*custom
   (state <s> ^name evaluate-operator
              ^problem-space <ps>)
   (<ps> ^name action
        -^action-space waypoint-search)
-->
   (<ps> ^default-desired-copy custom)
}

sp {problem-space*action*evaluate-operator*default-desired-copy*custom*elaborate*copy-desired
   (state <s> ^name evaluate-operator
              ^problem-space.default-desired-copy custom
              ^superstate.superstate.desired <ss-des>)
-->
   (<s> ^copy-desired <ss-des>)
}
