# problem-space.subtasks.source <world>
#   Specifies the subtasks to be copied (defaults to superstate.subtasks)

# If no other source specified, use superstate.subtasks

sp {problem-space*subtasks*source*elaborate*default
   (state <s> ^problem-space <ps>
              ^superstate.subtasks <subtasks>)
   (<ps> ^subtasks <subs>)
   -{(<subs> ^source <> <subtasks>)}
-->
   (<subs> ^source <subtasks>)
}