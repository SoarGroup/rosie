# problem-space.perception.attend-to-object <obj-handle>
#   Given an object handle, this will propose attend-to-perception 
#     for any perceptual discrepancies involving that object

sp {problem-space*perception*attend-to-object*propose*attend-to-perception
   (state <s> ^problem-space.perception.attend-to-object <obj-handle>
              ^top-state.perception-monitor.discrepancies.discrepancy <disc>)
   (<disc> ^object-info.object-handle <obj-handle>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name attend-to-perception
        ^discrepancy <disc>)
}
